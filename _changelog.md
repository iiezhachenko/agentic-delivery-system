# Changelog — shipped prompts (append-only)

> One compact entry per shipped prompt. COLD — append at step 6; read only for the step-7.4 drift diff-check (grep the one stale line, do not reload all). Maintenance RULES (anti-bloat 0–4 + protocol) live in `_tracker.md`.

---

> **Protocol** (every shipped prompt unless noted): isolated test (fresh separate verifier, no self-grade) → fix the PROMPT never the artifact → windowed e2e (head-clamp: window head = phase-first authored step; mechanical/non-LLM upstream excluded; fresh session per step reads PRIOR on-disk output, no re-seed) → golden promoted to `_fixtures/`. Adversarial prompts tested BOTH directions (clean + planted-defect). e2e output with benign variance is NOT promoted; goldens regenerate from golden inputs.

### 2026-06-06 — setup + Phase 0 intake spine
- Studied the 5 greenfield-delivery specs (00–04), created tracker. Inventory 40 prompts / 5 phases. Resolved D1–D4 (see Conventions); locked PR1–PR4 + caveman block + prompt skeleton. Fixtures live in `_fixtures/{greenfield-clean,compound-mixed}/`; `_test_bench/` is scratch.
- **CLASSIFIER** → `01-classification.json` (SR* ids, overall_confidence=min(subreq), threshold 0.80, escape non-greenfield). interactive:true conditional (low-confidence/compound/non-greenfield). Hardened: compound test = multi-class OR multi-system; N features of ONE new system = atomic.
- **EXTRACT** → `02-extraction.json` {E*, R*(explicit+implied, contiguous), C*, U*}. Implied = NECESSARY consequence only, merely-plausible→unknown; every item cites source+sr_ref (transcriber not author). HALT on needs_confirmation or non-greenfield.
- **GAP-DETECT** → `04-gaps.json` (ranked gaps{G*, refs, ≥2 interpretations, recommended_default verbatim∈interpretations, blast_radius, disposition ask/assume} + dismissed_unknowns + counts). Adversarial; every U* accounted (P9). Load-bearing: **blast tier** = arch (data-model SHAPE/stack/platform/external-dep/impl-mechanism) vs scope vs cosmetic, sort arch→scope→cosmetic, disposition deterministic from tier; hunt impl-forks; one gap=one topic; don't re-litigate chosen client words.
- **QUESTION-GEN** → `05-questions.md` (client-facing). Filter disposition:ask only (cosmetic skipped); **hard ≤6 cap**, mechanical select = first min(6,N) of blast-sorted 04; over-budget→deferred assumption (no silent drop). Load-bearing: **options stay in interpretations[] order, index 0→A/1→B/2→C; recommended marker moves IN PLACE, NEVER hoisted to A**; escape "Something else" last, never recommended.
- **SYNTHESIZE** → `drafts/aprd.v1.md` + `07-assumptions.json`. In: 02+04+05+06 (02 needed for E*/R*/C* id-space). 4 resolution paths: answered-letter→interpretations[index] / skipped→recommended_default / escape→client free-text verbatim / deferred→recommended_default. One A* per gap, full coverage. Load-bearing: carry E*/R*/C* verbatim, author A*/AC*; **CONSTRAINTS = exactly 02's stated_constraints, gap-fills are ASSUMPTIONS never CONSTRAINTS (no double-record)**; OUT_OF_SCOPE=declined interpretations (flips on override); ACs binary/one-outcome/concrete-observable, no fabricated edge cases; greenfield has NO class-extension block.
- **CRITIQUE** → `08-critique.json` {verdict clean|blocked, issues{I*, category, target_ref, fix_hint}}. 5 blocking categories: ambiguous-requirement, non-binary-ac, unbounded-scope, untraceable-assumption, broken-id-thread (`category` not severity — emit blockers only). Load-bearing: **resolution test** — read WHOLE contract before blocking; a fork closed by assumption+AC+OUT_OF_SCOPE is NOT a defect; cosmetic latitude within a passing AC is not a defect; anti-false-positive discipline (don't re-litigate logged decisions / demand ACs for unraised edges / OOS for unraised scope).
- **Main intake spine COMPLETE + windowed-e2e.** Golden chain 00→01→02→04→05→06→07+draft→08. (`06-answers.md` is a hand-authored client-gate fixture exercising all 4 resolution paths.)

### 2026-06-07 — Phase 0 research branch (§7) + Phase 1 start
- Resolved **D5** (research branch PRE-GAP), **D6** (VERIFY-OUTPUT→Phase 4), **D7** (Phase-1 numbering by spine order). See Open decisions. **Phase 0 prompt set COMPLETE** (9 prompts); ends at mechanical freeze (no prompt); inventory de-dup'd 40→39.
- **EXTRACT-RULES** → `03-grounding/rules-extracted.json` (flat rules{RULE*, source_ref→SRC*, tier, tool, tool_version_pinned, kind, topic, setting verbatim|null, evidence verbatim} + unfetched_sources + meta). Load-bearing: **transcribe-never-recall** (every rule needs verbatim evidence; famous-but-absent = absent; no training padding); NO reconcile/dedupe/currency-check (downstream); atomic (1 directive=1 rule); **extract NORMATIVE only — skip toolchain plumbing** (env/parserOptions/parser/plugins/extends, tsconfig path/output); graceful degradation (missing file→unfetched_sources). Golden canon fixture `_fixtures/greenfield-canon/` authored (TS/React/Node; planted 2 conflicts + 1 agreement + 1 missing source + plumbing/compound/non-normative traps).
- **RECONCILE** → `03-grounding/rules-reconciled.json` (agreed[AGR*] + conflicts[CONF* w/ positions[]] + carried unfetched + meta). Load-bearing: **topic is grouping HINT not decision key** — per-pairing relationship MERGE (same prescription) / CONFLICT (mutually exclusive) / SEPARATE (compatible-distinct; test: can one piece of code satisfy both?); a topic may yield a mix. Conflict by SEMANTICS not text; single-source=uncontested→agree; NO currency-check/NO invent (carry verbatim = VERIFY feed); recommend default per conflict by **TIER precedence** (tier1 config>tier2 prose; same-tier→null = genuine client decision, both positions stay); full accounting; **ordering by each entry's own lowest contributing RULE* index** (not topic-appearance).
- **VERIFY** → `03-grounding/rules-verified.json` = FINAL canon = RECONCILE structure carried VERBATIM + a `verification` block per agreed entry & per conflict position. Block = {status (current|deprecated|renamed|superseded|unknown-flag|not-version-bound|unverifiable), checked|null, finding, replacement, source_of_truth, confidence}. Load-bearing: **annotate-only, never re-reconcile** (same ids/order/positions/recommended_position); LLM NOT source of truth — flag never delete, `unverifiable`+low-confidence when unsure, never fabricate a verdict; null-setting prose→not-version-bound (no invented flag); **replacement is a suggestion, NEVER overwrite setting/rule** (append-only); currency does NOT change recommended_position; **sibling rules of one tool+version deprecate as a category** (consistency heuristic); re-count status_counts PER-STATUS (a swap that still sums right is the classic miscount).
- **Research sub-pipeline §7 COMPLETE + windowed-e2e.** Golden canon chain sources→manifests→extracted→reconciled→verified.
- Phase-1 head fixture authored: `aprd.frozen.md` + `aprd.lock` (mechanical freeze of golden draft; R1–R10/AC1–AC10/E1–E7/C1–C3).
- **SLICE-EXTRACT** (role 1/7) → `.roadmap/02-slices.json` (slices{S*, requirements, acceptance, value, value_basis, retires_risk, depends_on} + unsliceable + coverage + counts). Load-bearing: **verticality test** (slice vertical iff ≥1 AC black-box/user-observable, reuse aPRD AC* as oracle); **cluster by capability not layer**; full R*+AC* coverage no orphans (un-placeable→unsliceable+escape); depends_on coarse+acyclic (provisional, pending HLD re-rank); value proposed/client-owned (no fabricated model); retires_risk concrete-or-null; **controller-not-builder** — regroup existing IDs verbatim, never mint, never decide HOW; emission by lowest R* index. Guards: no frozen aPRD / non-frozen lock / non-greenfield → HALT (lock check = present+names-artifact, not hash recompute).
- **VERTICALITY-CHECK** (role 2/7) → `.roadmap/03-verticality.json` (valid[]/rejected[]{category}, verdict all_vertical|horizontal_found, counts). Adversarial reject-horizontal gate. Two inputs: 02 (AC ids) + frozen aPRD (**AC TEXT oracle — judge on AC text, not slice name**). Load-bearing: existential test (≥1 qualifying AC), record qualifying_acceptance as proof; 3 reject categories (horizontal_cut / no_acceptance / unresolved_acceptance=AC id not in aPRD); anti-FP (persistence ACs visible through surface ARE vertical; mildly-technical surface still black-box; **stay in lane = verticality only**); full accounting; no invent. Tested both directions.
- **SKELETON-IDENTIFY** (role 3/7) → `.roadmap/04-skeleton.json` (D7). Three inputs (JOIN): 03 valid[]=ELIGIBILITY + 02 bodies + frozen aPRD (seam oracle). **4 foundational seams** ingress/domain/persistence/primary-external-integration (last CONDITIONAL: absent→present:false, never invented). Skeleton test = ordered discriminator: crosses-every-present-seam-once → first-to-exercise-riskiest → dep-root (min depends_on) → thinnest. Load-bearing **RM11 boundary**: name seams, never DESIGN — NO components/stack/libs/db-engine/schema/endpoint/contract/**vendor** in ANY seam text field (even aPRD-cited "Google/GitHub" → functional TYPE only; vendor names may survive only in verbatim-carried `retires_risk`). No-fit→skeleton:null+uncovered_seams+escape; full accounting; carry IDs verbatim; stay in lane.
- **SEQUENCE** (role 4/7) → `.roadmap/05-sequence.json` (D7) {verdict sequenced|dependency_defect, sequence[]{position,id,skeleton,value,retires_risk,depends_on,cost_proxy,rationale}, ordering_basis, dependency_check, coverage, counts}. Three inputs: 04+02+03. **Skeleton pinned position 1** regardless of score; **dependency legality = HARD constraint** (greedy topological frontier fill); soft order of ready frontier = value(carried, never re-scored) → retires_risk!=null first → lower cost → lowest-S*; **cost_proxy = len(requirements)+len(acceptance)** (declared in ordering_basis, never estimated); cycle/dangling-depends_on/skeleton-with-deps → verdict:dependency_defect + route to SLICE-EXTRACT; full accounting; stay in lane (only `skeleton:true` flag on pos1).
- **FOUNDATION-CUT** (role 5/7) → `.roadmap/06-foundation-cut.json` (D7) {skeleton_id, foundation_cut{foundational_decisions[FD*]{category,needed_by,why_foundational,grounded_in}, skeleton_seams[]{seam,must_establish,grounded_in}, cross_slice_invariants[INV*]{invariant,applies_to,grounded_in}}, deferred[]{item,defer_to,reason,grounded_in}, coverage, cut_counts}. Three inputs: 04+05+aprd.frozen.md. Names the MINIMUM to decide/build once (§5.7, RM9). Load-bearing: **anti-waterfall lever — bias thin, under-cut not over-cut, when in doubt DEFER**; **skeleton drives the cut** (its seams = the spine; FDs derive from seams crossed + deployment reality, each `needed_by` S1); **RM11 name-don't-decide** — FD `category` names what Phase 2 resolves NEVER the choice, seams stay named-not-designed; **P11 invariants READ not invented** (each cites real C*/A*/E*; §6.1 examples auth/error/observability are prompts-to-look not a checklist — don't manufacture invariants the aPRD is silent on); same topic → BOTH an INV (fixed model) + an FD (open HOW) — e.g. A2 fixes "OAuth delegation, no stored creds" while "which provider + boundary" stays open; **deferred[] = thinness evidence** routing each slice's HOW to its owning slice; full accounting. Isolated PASS (fresh verifier 7/7) + full-3-step windowed-e2e PASS (no cascade). Golden chain 02→03→04→05→06.

### 2026-06-07 — Phase 1 client gate (D8 resolved)
- Resolved **D8** (author SEQUENCE-REVIEW now, defer RE-RANK; + reusable interactive two-phase pattern + data-chain window rule — see Open decisions).
- **SEQUENCE-REVIEW** (role 7/7, client gate, **interactive:true**) → `roadmap.md` (Phase A presentation) + `07-sequence-reviewed.json` (Phase B confirmed baseline). In: `05-sequence.json`. Two-phase single session: present recognition-over-recall (order + first-demo + plain per-position rationale + multiple-choice reorder) → PAUSE → on client reply validate/apply/write. Load-bearing: **legal reorder options computed deterministically from `depends_on`** (slack rule; skeleton pinned pos1 never offered; fully-forced order → confirm+escape, no fake choices); **dependency legality overrides the client (RM5)** — illegal/skeleton-move → `blocked_overrides[]`+`needs_followup:true`, legal order held; **value carried verbatim, never re-scored** (§7); coverage preserved; lane = order only (no foundation-cut/decisions/HOW shown, §9). `client_response` enum confirmed|reordered|deferred|**blocked**. Isolated PASS (3 paths) + fresh-verifier 10/10 + e2e PASS (SKELETON-IDENTIFY→SEQUENCE→SEQUENCE-REVIEW, real data-chain). The **blocked** enum value was the one fix (first run emitted off-schema `reorder_requested` for the all-illegal case). Golden chain 02→…→07 (+`roadmap.md`). **Phase 1 client surface COMPLETE; only RE-RANK remains (deferred, forward-blocked).**

### 2026-06-07 — Phase 2 ADR (COMPLETE)
- **DECISION-EXTRACT** (role 1/7, head) → `.adr/01-decision-points.json` {decision_points[DP*]{decision,category,forced_by[],candidate_blast_radius,fork_evidence,cut_ref}, checklist_coverage[10], aprd_defects[], counts}. In: aprd.frozen+lock + 06-cut. Walks fixed §4 category checklist (recognition>recall); emits every genuine FORK (forced by ≥1 aPRD elem AND live ≥2-ways AND structural blast). Load-bearing: **name the OPEN QUESTION never the answer (RM11)**; **cut SEEDS not bounds** (expand coarse FDs into one-decision-per-point DPs; adversarially hunt missed forks → cut_ref:null = thin-cut signal; INV*=fixed forces, cite never re-open); forced_by verbatim no padding (timeline/scale rarely CREATE a HOW-fork); candidate_blast_radius=proposal (TRIAGE finalizes, never trivial); collapsed-to-one closed in checklist not emitted; deterministic checklist-order emission. Isolated PASS (4 hardening rounds, each a distinct gap: cut_ref bare-null; split bundled decisions; timeline-padding guard; sub-count tally-by-walking; emission-order rule). e2e PASS (head-clamp). Golden promoted. Variance: emitted set 9–11 DPs run-to-run (creative extraction), schema-clean every run.
- **TRIAGE** (role 2/7) → `.adr/02-triage.json` {triage[per-DP verdict], resolution_queue[], slice_deferred[], deferred_queue[], conventions[], cut_gaps[], counts}. In: 01 + 06-cut. **Two-axis**: Axis1 BINDING blast_radius foundational|local|**trivial** (only TRIAGE may assign trivial); Axis2 (foundational) in-cut|not-yet vs cut. **Route = pure fn of axes**: found+in-cut→resolution_queue(OPTION-GEN); found+not-yet→slice_deferred(defer_to); local→deferred_queue(Phase 3); trivial→convention. Load-bearing: **cut FD list = authoritative in-cut set** (FD.needed_by∋skeleton_id→in-cut); **skeleton-need test** for unnamed foundationals (skeleton/INV breaks without it → in-cut + **cut_gap:true** thin-cut feedback; else not-yet→defer_to earliest needing slice); convention = announce-it's-convention never name value (RM11); carry id/decision/category/cut_ref VERBATIM; 4-queue partition + tally-by-walking; robust to 9–11-DP input. Isolated PASS (0 defects, fresh verifier 7/7). e2e PASS (clamp DECISION-EXTRACT→TRIAGE; 10-point variance set consumed clean). Variance: cut_ref:null foundationals route not-yet→S4 OR in-cut+cut_gap — both valid (architect judgment); the cut_gap mechanism is the designed catch. Golden promoted.
- **OPTION-GEN** (role 3/7) → DIR `.adr/03-options/`: per-DP `<DP-id>.json` + manifest `index.json`. In: 02-triage(resolution_queue) + 01(bodies) + aprd(forces) + 06(INV* gate) + OPTIONAL arch-canon. **≥2 REAL live alternatives per in-cut foundational, cheapest-source-first** (existing[N/A greenfield]→canon→reasoned). Load-bearing: **lane moves PARTWAY** — NAMES concrete options (RM11 boundary shifts: naming IS the job) but **still does NOT decide** (EVALUATE-DECIDE owns pick); canon graceful-degrade (arch-canon OPTIONAL; absent→source:reasoned+flagged); **anti-strawman 3-clause** (team-would-weigh AND compliant-not-DOA AND distinct); **compliance gate** — option breaching hard C*/INV* = dead-on-arrival → EXCLUDE; trade-offs NEUTRAL+bidirectional; `bears_on`=eval hooks not scores; degenerate honestly; full accounting; robust to variable queue. Isolated PASS (1 round, verifier 6/6). **e2e PASS (2 retries — fixed a REAL compliance-gate loophole):** a DP1 microservices option slipped in rationalized "to confirm non-viability" (own costs admitted INV6-banned); R1 (forbid for-completeness keywords) was **gamed** (reworded to dodge keywords); R2 re-anchored the gate on **SUBSTANCE + "the decision question bounds the option set"** (a branch DECISION-EXTRACT already closed stays closed) → excluded correctly. Variance: option-set membership/count vary run-to-run (DP4 ±MongoDB, DP6 ±SPA, DP7 single-provider vs generic-OIDC), all defensible. Golden promoted (6 files, 17 options).
- **EVALUATE-DECIDE** (role 4/7, the decider) → per-DP `.adr/03-options/<DP-id>.decision.json` + manifest `decisions-index.json`. In: 03-options/index + per-DP option files + aprd(C*/AC/assumptions-as-NFRs) + 06(INV* floor). **Scores option sets, PICKS one, records consequences.** Load-bearing: **D1/D3 live-alternatives is THE rule** — `evaluation[]` assesses EVERY option neutrally BEFORE the pick (reads same whichever won), then decision_made/rationale/rejected reference it (structure forces alternatives live, not reverse-engineered); **picks but stays in lane** (no ADR render/id, no cross-decision conflict/coverage [RECONCILE — each point decided vs FIXED forces INDEPENDENTLY → conflicts stay visible, cross-deps NOTED in follow_on], no re-source, no aPRD/cut re-open → undecided[]+Phase 0); scoring grounded in bears_on+forced_by+C*/AC*/INV* (no invented criteria); weighting explicit; rejections force-traced; traces = forced_by ∪ cited-by-name (anti-padding); degenerate→forced pick; full accounting. **Isolated PASS (3 retries — each killed a distinct GROUNDING-DISCIPLINE defect variant, converging on root):** R1 recalled dev-side market claim ("Ruby pool shrinking") → forbade recalled market/popularity args + C2≠labour-market-proxy; R2 trace-padding + verbatim-break → operational trace check + copy-by-paste rule; R3 defect mutated user-side ("Google held more broadly than GitHub" tiebreaker) → generalized ban + added **"contract-indifferent → default among equals"** move (no contract force separates two compliant options → pick a default honestly, never fabricate a tiebreaker). Verifier 7/7. e2e PASS (TRIAGE→OPTION-GEN→EVALUATE-DECIDE; 7-item variance queue incl new DP5). Variance: picks vary (DP2 Python/Node/Rails) — all defensible; live-alternatives discipline is the invariant. Golden promoted.
- **RECONCILE** (role 5/7, coherence+coverage gate — first cross-set stage) → `.adr/04-conflicts.json`. In: decisions-index + per-DP decision files(picks+traces+follow_on) + aprd(C* target) + 06(INV* floor+deferred). **Detects three things, FLAGS never re-decides (D9):** (1) cross-decision conflicts (grounded via follow_on sibling notes + pairwise INV* clash; emit only on a NAMED mutual-exclusion — tension≠conflict, honored-dependency≠conflict, FPs thrash the loop); (2) constraint violations (pick breaching hard C*/INV*); (3) coverage bidirectional (ADR→aPRD traces non-empty+real; aPRD→ADR each in-scope C* bucketed covered/deferred/premise/gap). verdict blocked if any → routes back (→EVALUATE-DECIDE; aPRD-defect gap→Phase 0); coherent → SYNTHESIZE-ADR. **Load-bearing:** **coverage target = C* ONLY; INV* are the violation hard floor NOT coverage targets** (requiring INV* coverage manufactures FP gaps — most INV* are slice-level, no foundational decision is *about* them); **`covered.by` = literal string membership in `traces[]` never topical relevance**; C3=premise; FLAG-never-re-decide is THE lane line. **Isolated PASS (2 retries, converging):** R1 INV-coverage FP (flagged INV2 a "gap") → narrowed coverage to C*; R2 `covered.by` topical mis-attribution → literal-trace string-membership rule. Verifier A–H clean. e2e PASS (OPTION-GEN→EVALUATE-DECIDE→RECONCILE; variance picks consumed clean, C1.by literal-trace-correct). Variance: clean golden yields all-empty every run (detection mechanisms prove out on negative/variance inputs). Golden promoted.
- **SYNTHESIZE-ADR** (role 6/7, the renderer) → per-decision `.adr/drafts/<NNNN>-<slug>.draft.md` (Nygard ADR §6.1) + `.adr/adr-index.json`. In: 04-conflicts (GATE — render only on `verdict:coherent`; blocked→HALT) + decisions-index (id order) + per-DP decision files (content source). **TRANSCRIBE never RE-DECIDE (D1/D6)** — only original prose is the `title`; Context=problem-not-answer (no chosen-option name, no new force), Decision=`decision_made` **verbatim-substring**, Alternatives=rejected[] + force-traced why_rejected (degenerate-honest / default-among-equals, no fabricated discriminator), Consequences transcribed. **DESIGN CALL:** renders **DRAFTS (`status:Proposed`, `.adr/drafts/`) NOT immutable `.adr/log/` Accepted** — CRITIQUE runs after + loops back (§5.8), so the record must stay mutable (D6); mechanical post-CRITIQUE freeze (non-LLM) promotes drafts→log + writes adr.lock. **Id:** monotonic ADR-NNNN from 0001 in decisions-index order; undecided[]→no id. Isolated PASS (0 retries, verifier A–G clean). **e2e PASS (1 retry — fixed a REAL drift defect):** runner paraphrased the option name in `## Decision` (lost string-auditability); rule too soft → sharpened to "verbatim as a substring, copy-by-paste, words unaltered" + CORRECT/WRONG example. Variance: titles + section phrasing vary (faithful); transcribe-not-re-decide is the invariant. Golden drafts ADR-0001..0006 + adr-index promoted.
- **CRITIQUE** (role 7/7, hostile architecture reviewer — **last Phase-2 prompt; PHASE 2 COMPLETE**) → `.adr/05-critique.json`. In: adr-index (enumerate rendered drafts) + each draft + aprd(trace/coverage oracle) + 04-conflicts(RECONCILE verdict + buckets) + 06(FD*/deferred/INV*). Adversarial final gate on the **RENDERED** drafts before mechanical freeze. **Seven blocking categories:** strawman-alternative, untraceable-adr, uncovered-constraint (**C\* only; INV\* not coverage target** — D5), contradictory-adrs, over-decided (actually local→Phase 3 — D2), unforced-decision (gold-plating, D4), not-yet (belongs to a later slice per cut deferred[] — D11). Blocking→loops to SYNTHESIZE-ADR; clean→mechanical freeze. **Load-bearing LANE vs RECONCILE:** NOT RECONCILE-twice — owns the NEW checks RECONCILE can't do (strawman/over-decided/unforced/not-yet = legitimacy of decision-making) + a backstop of trace/coverage/contradiction **on the RENDERED FORM**; does NOT re-run RECONCILE to override its `coherent` verdict. **Anti-FP (mirror Phase-0 resolution-test):** read WHOLE context before blocking; never re-litigate a logged pick; degenerate-forced ≠ strawman + default-among-equals ≠ strawman (block only a FABRICATED alt/discriminator); INV* not coverage targets; premise/deferred C* is covered; a real aPRD defect ROUTES to Phase 0 never blocks the faithful draft. **Isolated PASS (0 retries), BOTH directions:** clean golden→`clean` (6 reviewed); planted-defect fixture (8 ADRs = 4 plants + 4 clean) → all 4 caught + correctly categorized + ZERO FP (incl ADR-0005 default-among-equals correctly NOT flagged); fresh verifier re-walked all 8 → 0 missed/0 FP. e2e PASS (RECONCILE→SYNTHESIZE-ADR→CRITIQUE, clean). **PHASE 2 COMPLETE — DECISION-EXTRACT→TRIAGE→OPTION-GEN→EVALUATE-DECIDE→RECONCILE→SYNTHESIZE-ADR→CRITIQUE; freeze is mechanical (no prompt).**

### 2026-06-07 — Phase 3 HLD start
- Resolved **D9** (skeleton-first / defer-increment mode split + baselined-`.adr/log/`+`adr.lock` input form + §5.1 load folded into head, no `00-inputs.json`). See Open decisions.
- **Seeded the frozen ADR baseline fixture** (mechanical freeze by hand on golden Phase-2 drafts): `_fixtures/greenfield-clean/.adr/log/<NNNN>-*.md` (6, Accepted) + `.adr/adr.lock` (status:frozen + baselined adrs[] manifest). Pre-freeze `drafts/`+`adr-index.json` (Proposed) kept as Phase-2 goldens.
- **DERIVE-COMPONENTS** (role 1/8, head of HLD, **SKELETON pass**) → `.hld/skeleton/components.json` {components[{id,name,responsibility,owns_entities[E*],traces[R*],realizes_seam[],honors_adr[]}], edges[{from,to,reason}], coverage(bidirectional R + single-owner E + seam_realization), structural_defects/frame_conflicts/aprd_defects, counts}. In: adr.lock + .adr/log (boundary-strategy ADR = cut driver) + aprd(R*/E*) + 06(skeleton_seams seed + INV*). **First drawing stage: clusters the FULL requirement set into the full component graph (boxes + dependency edges) = the frame every slice extends + the build DAG.** **3-clause component test:** traces ≥1 R (else gold-plating→drop) AND honors frame (flat-monolith/INV6 → logical responsibility units in ONE deployable, NO distributed services/brokers/scale-topology) AND responsibility-unit-not-a-layer. Load-bearing: **APPLY boundary-strategy ADR never invent cut (H2/H12)**; **skeleton clusters FULL R-set** (whole graph; walking-skeleton slice = thinnest PATH, MODEL-FLOWS' concern); **bidirectional coverage**; **single-owner entities** (proposal; MODEL-DATA formalizes; shared infra box may own []); **edges = STRUCTURAL DIRECTION ONLY never contracts** (DEFINE-CONTRACTS owns seam spec; acyclic); realize every present seam; **name responsibility not implementation** (a frame-FIXED choice may be referenced — reference what an ADR decided, never decide what it deferred); deterministic emission = topological build order. Escapes: HALT on missing-inputs/non-greenfield; skeleton-already-frozen → dispatches to INCREMENT (D14, no longer a HALT); frame-unbuildable→frame_conflicts→Phase 2; requirement-unstructurable→aprd_defects→Phase 0. Isolated PASS (0 retries, verifier A–H). Head-clamped e2e PASS (0 retries). Variance: component cut varies (5 vs 6 components; explicit Data-Store box vs persistence-folded) — all defensible flat-monolith cuts. Golden promoted.
- **DEFINE-CONTRACTS** (role 2/8, **SKELETON pass**, **THE load-bearing stage — H1**) → `.hld/skeleton/contracts.json` {contracts[{id:CT*, between:[from,to], kind, shape, failure_modes[], traces[R*], honors_adr[], honors_inv[]}], coverage(bidirectional edge), kind_distribution, counts}. In: components.json(PRIMARY — `edges[]`=seams to contract) + aprd(R*/E*) + adr.lock + .adr/log(frame decides KIND) + 06(INV6 forbids async; deferred=schemas not to invent). **Puts the contract on every bare edge DERIVE-COMPONENTS drew.** **Kind discriminator (derive-never-default):** `sync_api`=synchronous in-process call (flat monolith INV6 = one process, no network hop); `shared_data`=read/write durable state into persistence component; `async_event`=**INV6-forbidden by default** → an async edge with no frame-permitted reason = `frame_conflicts[]`→Phase 2. Load-bearing: **one-contract-per-edge BIJECTION** (CT* in edge order); **`shape` named-not-designed** (a schema REFERENCE — E*-set + responsibility — NEVER invented field detail; cut defers schemas to slices); **≥1 grounded `failure_mode`** — store-modes for shared_data, callee-error/not-found/no-valid-session for sync — **NO inter-process network-partition/message-loss on in-process sync seams**; **≥1 verbatim R trace**; frame-fidelity escape; **kind_distribution counted by enumerating each kind's CT* members independently** + sum==contracts. **Isolated PASS (2 retries — both killed the SAME miscount class, converging):** R1 kind_distribution summed to 12 vs 11 → added sum-must-equal-contracts; **R2 the sum-check was GAMED** (runner shifted a count so the wrong 8/3 split still summed to 11) → added count-each-kind-by-enumerating-its-CT*-members. Verifier A–H all 8 PASS. e2e PASS (0 retries, clamped DERIVE-COMPONENTS→DEFINE-CONTRACTS; 8-edge variance graph → all 8 sync_api, runner DERIVED from topology not rote "store=shared_data"). Variance: kind distribution tracks the upstream cut (Data-Store box→shared_data edges; persistence-folded→all-sync_api); both correct. Golden promoted (11-edge: 7 sync_api + 4 shared_data + 0 async, CT1–CT11).
- **RESOLVE-LOCAL** (role 3/8, **SKELETON pass**, emits local ADR) → `.adr/drafts/<NNNN>-<slug>.draft.md` (Proposed `mode:slice` local-ADR drafts, ids ADR-0007+) + `.adr/deferred-decisions.json` (queue-resolution ledger). In: 02-triage(`deferred_queue[]`=local work list) + 01(DP bodies) + components.json + contracts.json (emerging structure — owning-component lookup + escalation test) + aprd + adr.lock(frozen gate + id-continuation) + .adr/log(frozen frame) + 06(deferred re-defer grounding + INV*). **Drains the LOCAL-decision queue TRIAGE routed to Phase 3, resolves each fork the SKELETON forces + emits each as a `mode:slice` local ADR DRAFT (H3).** **3 dispositions (discriminator, derive-never-default):** `resolved`=skeleton forces it now (in-skeleton owning-component + skeleton-once-cross-cutting-not-box-internal + not slice-tagged) → pick option + Proposed draft; `re-deferred`=later-slice-owned / box-internal / slice-tagged → defer_to+reason, no ADR (**default when unsure**, anti-waterfall); `escalated`=actually-foundational (forces cross-box change / changed contract kind / INV violation) → escalations[]→Phase 2. **3 open authoring calls RESOLVED:** (a) input form — reads triage `deferred_queue[]` + 01 DP bodies directly; RESOLVE-LOCAL itself WRITES `deferred-decisions.json` (D7 no-mint); (b) **THE load-bearing call — append to a FROZEN log** = drafts-then-freeze mirroring Phase 2: write Proposed drafts to `.adr/drafts/` (ids ADR-0007+ after baseline max), NEVER mutate `adr.lock` or `.adr/log/`; mechanical Phase-3 freeze promotes drafts→log AFTER the role-8 gate; (c) graceful near-empty — skeleton pass may resolve FEW/ZERO locals (most live in per-slice internals), near-empty+cleanly-re-deferred is complete. Load-bearing: drain-whole-queue+partition+accounting; verbatim DP-body carry; live-alternatives force-traced (**no recalled market/popularity claim**, default-among-equals honest); NEVER-re-decide-foundational; monotonic id continuation; **pinned bucket element shapes**; FLAG-never-fix escalation lane. **Isolated PASS (0 retries, verifier A–H, 0 defects).** **e2e PASS (1 retry — fixed a REAL PR2 schema-determinism defect):** the disposition BUCKETS had under-specified element shape — isolated emitted `resolved:["DP8","DP9"]` (bare ids), e2e emitted `resolved:[{dp_id,adr_id}]` (objects); two shapes for the field the role-8 audit consumes → fix pinned each bucket's shape (resolved=`{dp_id,adr_id}` / re_deferred=`{dp_id,defer_to}` / escalated=`{dp_id}`). Variance (borderline cross-cutting locals): DP8 OAuth-lib resolved-now OR re-deferred-to-S1-build; DP9 owner C6 OR C2 — both defensible; DP3 (slice-tagged) + DP11 (no owning component) re-defer every run. Golden promoted (2 resolved DP8/DP9 + 2 re-deferred + drafts 0007/0008).
- **MODEL-DATA** (role 4/8, **SKELETON pass**) → `.hld/skeleton/data-model.json` {persistence{adr_refs,store,...}, entities[{id,name,logical_description,owner,owner_basis,persisted,accessed_by[{component,access,via:CT*}],relationships[{to,cardinality,basis}],field_schema_deferred_to,honors_inv[],traces[R*]}], ownership{E*:C*}, coverage, ownership_defects[], counts}. In: aprd.frozen(ENTITIES E1–E7 + durable-vs-derived + relationships) + adr.lock/log(**Persistence ADR-0003** = store + relational chain) + components.json(`owns_entities[]`=PROPOSED owner to formalize) + contracts.json(single-owner proof — shared_data write seams) + 06(INV* floor + deferred field-schemas). **Builds the authoritative logical data model; FORMALIZES (confirm/verify) DERIVE-COMPONENTS' `owns_entities[]`, does NOT re-propose.** **Ownership rule (4 clauses):** (1) owner = component whose responsibility GOVERNS the entity (sole writer), read from `owns_entities[]`; (2) ownership = LOGICAL AUTHORITY not physical storage (a Data Store box owns []); (3) `persisted` (durable, has store-write seam) vs `derived` (computed, no write seam — invoice E4, owner=sole producer); (4) ambiguity = BOUNDARY DEFECT → flag+route (two-owner/orphan→DERIVE-COMPONENTS, shared-write→DEFINE-CONTRACTS), never re-cut. Load-bearing: **single-owner verified two ways** (walk `owns_entities[]` + walk contracts sole-writer==owner, `accessed_by` makes it auditable); **named-not-designed** (logical entities/relationships/cardinality only, NEVER field columns/types/DDL — deferred per slice); **relationship reciprocity** (persisted↔persisted bidirectional listings must agree; derived→source aggregation is one-directional, exempt); honors INV2/3/4/5; FLAG-never-fix lane. **Isolated PASS (0 retries, verifier A–H, 0 defects).** **e2e PASS (0 cascade) — 1 minor verifier blemish hardened (2 fixes, both the DERIVED entity E4):** (R1) E2↔E7 cardinality mismatch → added persisted↔persisted reciprocity rule; (R2) the rule over-reached on E4→E3 aggregation + E4 field-schema slice varied → refined: derived entity exempt from back-listing, derived field-schema defers to the PRODUCING slice (invoice→S3). Variance is upstream (the component cut + which contracts are shared_data-read) + derived-entity edge cases — all bounded by the reciprocity + derived rules. Golden promoted (7 entities: E1→C2, E2/E5/E6/E7→C3, E3→C4, E4→C5 derived).
- **MAP-NFR** (role 5/8, SKELETON pass) → `.hld/skeleton/nfr-mechanisms.json` (5-disposition NFR inventory + M*). **Load-bearing:** under INV6/A13 the classic scale mechanisms are FORBIDDEN → near-empty mechanism set is CORRECT (most NFRs satisfied-by-frame; genuine tension → frame_conflicts → Phase 2, never invent the forbidden M*). Isolated PASS (0 retries) + e2e PASS (0 cascade, held across a variant upstream cut). Golden promoted (8 NFRs, mechanisms=[]). Schema/rules/test → `MAP-NFR.md`.
- **MODEL-FLOWS** (role 6/8, SKELETON pass, **centerpiece validation §5.7**) → `.hld/skeleton/flows.json` {flows[F1]{slice:S1,path:[C*],steps[{from,to,via:CT*,seam,external}],via:[CT*],failure_path{trigger,exercises:CT*:mode,arrives_at},traces:[R*/AC*],honors_inv} + seam_coverage(per skeleton_seam → realizing C* + via) + composes_against_contracts + structural_defects/frame_conflicts/aprd_defects + counts}. In: components.json(seam→C* + edges) + contracts.json(PRIMARY — CT* the hops compose against) + aprd(R*/AC* oracle) + 06-cut(skeleton_seams the path MUST cross + INV*) + adr.lock/log. **Draws the ONE thinnest walking-skeleton path (S1) touching every foundational seam once + its failure variant; executes the contracts on paper.** **Load-bearing — the compose test:** flow walks EXISTING contracts → every inter-component hop maps to a real CT* (direction-aware `between` match); a hop with no CT* or an uncovered seam = STRUCTURAL DEFECT to NAME (→DEFINE-CONTRACTS), never a CT* MODEL-FLOWS invents (lane); **external boundary / folded seam** (a foundational seam realized by an on-path C* with no separate inter-component hop — external OAuth inside C2, or persistence folded into C1) is CROSSED with `via:null`, NOT a defect (the false-positive trap); failure variant MANDATORY, reuses a CT*'s declared `failure_mode` (reference not invent); AC* = arrival oracle; honor INV* (no async/queue hop). **Isolated PASS (0 retries, fresh verifier CLEAN A–K).** **e2e PASS (0 cascade) across a VARIANT upstream cut** (5-comp persistence-folded: path collapsed to [C5,C1]/CT7, 3 seams folded into C1, OAuth-timeout failure — all handled, 0 false-positive defects). **One fix — cosmetic determinism (pre-e2e):** verifier flagged seam_coverage emitted in traversal order not skeleton_seams[] order → tightened Rule 10 + schema comment to pin skeleton_seams[] array order (re-run stable). Variance: path/via/failure track the upstream cut ([C6,C2,C1]+CT8/CT1 golden vs [C5,C1]+CT7 folded) — all compose true. Golden promoted (F1, path [C6,C2,C1], via CT8/CT1, 4 seams crossed). Schema/rules/test → `MODEL-FLOWS.md`.
- **DERIVE-TESTS** (role 7/8, SKELETON pass, design-layer oracle + build DAG, §5.9) → `.hld/skeleton/test-specs.json` (per-CT shape+failure-mode specs + per-F AC-arrival spec) + `.hld/skeleton/build-dag.json` (edge topo-sort). **Load-bearing: design-layer oracle ≠ aPRD acceptance oracle** — reference AC* by id NEVER re-author (Phase 0 owns text), emit SPECS never CODE (Phase 4 owns code); every CT*→one shape+per-failure-mode test (bijection, failure_mode verbatim + expected_behavior from the mode's own consequence), every F*→AC-arrival + reused failure_path; build DAG = topo sort of `components.json` edges (edge `from`-depends-on-`to`, wave = 1+max(deps), ascending C* tiebreak), emitted ONCE, cycle→DERIVE-COMPONENTS. Isolated PASS (0 retries, fresh verifier A–H) + e2e PASS (0 cascade, DEFINE-CONTRACTS→MODEL-FLOWS→DERIVE-TESTS data-chain window per D8). Variance: failure_assertions count tracks live contracts (e2e 28 vs golden 26 — walk-to-count, correct, not a defect). Golden promoted (11 contract tests, 26 failure assertions, F1, build_order C1→C6 / 5 waves). Schema/rules/test → `DERIVE-TESTS.md`.
- **RECONCILE/CRITIQUE** (role 8/8, hostile gate on the assembled skeleton — **PHASE-3 SKELETON CHAIN COMPLETE**, §5.10) → `.hld/skeleton/critique.json` (verdict clean|blocked + blocking issues over 7 categories w/ per-issue `routes_to`; clean→mechanical skeleton freeze, blocked→DERIVE-COMPONENTS). **Load-bearing: RE-DERIVE every check from the PRIMARY arrays — a producer's self-reported coverage/summary block is NOT evidence (a gate that trusts the all-green summary catches nothing).** Anti-FP mirror Phase-0/2 (INV* = violation floor NOT coverage target; derived/via-null/satisfied-by-frame/re-deferred-with-reason are NOT defects) — full categories + rules in `RECONCILE-CRITIQUE.md`. **Isolated PASS 1 retry BOTH directions — retry killed a self-report-trust MISS** (orphan R + missing failure_path slipped past until recompute-from-primary-fields added; clean→clean fresh-verifier 7/7, planted 7/7 caught 0 FP). e2e PASS (0 cascade, MODEL-FLOWS→DERIVE-TESTS→RECONCILE/CRITIQUE; benign flow-variance). Golden promoted. **Mechanical skeleton freeze (no prompt) next.**

### 2026-06-07 — DRY retrofit + full-pipeline harness (D10)
- Diagnosed prompt bloat root cause (skeleton mandated 3–5 homes per fact; no single-source rule). Rewrote the Standard prompt skeleton + added binding authoring rules AB1–AB6 (see Conventions / D10). Retrofitted ALL 26 Phase 0–3 prompts: **4433→3135 lines (−29%)**, substance preserved (per-agent reports), structural sanity verified. Re-test owed (skipped by operator) — goldens unchanged.
- **Authored `_pipeline-run.md`** — full-chain e2e test harness (CLASSIFIER→MODEL-DATA, the implemented spine), complement to `_prompt-run.md`'s windowed test. Runs every implemented step in spine order via clean-room runners, handles client gates (G1, SEQUENCE-REVIEW) + mechanical freezes (F1, F2), verifies schema+id-thread+invariants (variance-tolerant), records per-spawn token usage, reports a per-step pass/break map. Diagnostic only — no authoring/fixing in that loop.
- **First full-from-raw run finding (Phase-0 spine):** all seams CLASSIFIER→…→CRITIQUE held (id-thread intact); terminal = CRITIQUE GATE-blocked (true-positive, arch fork G6 left open). **Root cause = client-gate fixture brittleness, NOT prompt drift:** the human gate G1 can't replay deterministically because QUESTION-GEN asks the first min(6,N) blast-sorted gaps and GAP-DETECT's gap ORDERING varies benignly → a static `06-answers.md` keyed to golden Q-numbers binds to the wrong gap. **Resolution: split the harness into two files (DRY — base + overlay).** `_pipeline-run.md` = **MODE A** (DEFAULT: seed golden `aprd.frozen.md`, run Phase 1→3) **+ the shared-mechanics base** (chain table, runner spawning, verification, token logging, F2 freeze, report, contract). `_pipeline-run-modeB.md` = **MODE B** (full-from-raw, validate Phase 0): thin overlay = Phase-0 prelude (steps 1–6 + the golden-`04-gaps.json` PIN before QUESTION-GEN + `06-answers.md` G1 inject + F1 freeze) then hands off to the base from step 7 (base task-2 handoff guard skips re-seeding when `aprd.frozen.md` already on disk). Long-term: re-key client-gate fixtures by `G*`/gap-ref not `Q<n>` (removes the pin). Cross-phase 1/2/3 drift NOT yet exercised — run MODE A next.

### 2026-06-07 — sibling spines sketched (out of current scope)
- **Sketched two sibling pipeline design specs** for work the greenfield CLASSIFIER escapes (non-greenfield input / non-software output): `_initial_design/05` **Spine D — documentation** (existing system + doc goal → verified doc set; generalist, input-adaptive via doc-type/input-kind playbook overlays) + `_initial_design/06` **Spine A — architecture review** (existing system + complaints → diagnosed review + recommended ADRs). Both match the spec format + carry the two-loop / skeleton-increment / done-is-a-test conventions + a per-role reuse map (VERBATIM/OVERLAY/NEW). **Load-bearing finding: near-zero new code** — Spine D overlays the research sub-pipeline (transcribe-never-recall over a *system*, RECONCILE-conflict→DRIFT) + slicing + CRITIQUE/VERIFY; Spine A runs HLD roles **read-mode** + the Phase-2 ADR engine verbatim, new work = DIAGNOSE (symptom→root-cause) only. **Scope: NOT this tracker.** Build order greenfield → D → A; each spine gets its own tracker when authored. Tracker Mission + Source-specs table re-scoped to 00–04.

### 2026-06-07 — Phase 4 Build start (D11 resolved)
- Resolved **D11** (skeleton-build-first / defer slice-build + mechanical skeleton freeze by hand). See Open decisions. Seeded frozen-skeleton fixture: `_fixtures/greenfield-clean/.hld/skeleton.lock` + `.hld/skeleton.frozen.md`.
- **BUILD-PLAN** (role 1/8, head, **skeleton-build mode**) → `.build/skeleton/build-plan.json` {build_set, build_order, parallel_groups, build_units[{provides_contracts, consumes_seams[real|mocked], mocked_deps, traces}], mock_map, lock_set, coverage, counts}. In: skeleton.lock(gate) + build-dag + flows + components + contracts + adr.lock + aprd.lock. **Load-bearing: PLAN against the frozen DAG, build/decide nothing — build set = walking-skeleton path (thin, H14); build_order = build-dag order FILTERED (carried, never re-sorted); real|mocked = pure fn of build-set membership; lock_set [] in skeleton-build (one path, no concurrent slice).** Deterministic transform. Isolated PASS (0 retries) + e2e PASS (0 cascade, phase-first single-step window seeded from golden frozen skeleton; two independent clean rooms → byte-identical verified output). Golden promoted. Schema/rules/test → `BUILD-PLAN.md`.
- **MATERIALIZE-ORACLE** (role 2/8, **test-author, distinct from builder B4**, skeleton-build mode, §5.3) → `.build/skeleton/oracle/` — executable pytest (contract/flow/acceptance) + `conftest.py` (contract-level mocks) + `mutation-certification.json` + `oracle.json` manifest + FROZEN `oracle.lock`. In: build-plan(scope) + test-specs(CT*/F* source) + aprd.frozen(AC* text) + components(high-blast) + frozen locks + adr/log(stack). **Load-bearing: materialize "done" into an IMMUTABLE oracle and implement NOTHING — suite starts fully RED (targets unimplemented), builder may green-it never edit-it; scope = build-plan real seams only (CT1/CT8, not mocked CT9-11); failure tests reuse test-specs failure_mode verbatim; held-out split = visible+held_out same-property/different-unguessable-input (B7); mutation-certify high-blast (C2/auth); stack=Python/ADR-0002 but tests target contract-level interfaces — framework pick is IMPLEMENT LLD behind the contract (B8).** Isolated PASS (0 retries, fresh verifier CLEAN A–I) + e2e PASS (0 cascade, BUILD-PLAN→MATERIALIZE-ORACLE clamped window; build-plan byte-identical to golden). Variance: test-code module-naming/import strategy vary run-to-run (manifest is the stable anchor) — all red-first, all bijection-clean. Golden promoted. Schema/rules/test → `MATERIALIZE-ORACLE.md`. **Hardened 2026-06-07 (via IMPLEMENT e2e): assert SUT-observable behavior, never a mock's configured `return_value`/`side_effect` attr** (`assert not mock.save.return_value` vs a truthy conftest default is unsatisfiable by any impl — a materialization defect); golden `test_CT1.py` store-unavailable assertion corrected to `mock.save_identity.called`.
- **INTEGRATE** (role 4/8, **integrator — composes the path, greens the flow test, B2/B3**, skeleton-build mode, §5.6) → composition root `src/freelancer_app/wsgi.py` (+ external `oauth_provider.py` seam) + `.build/skeleton/integration-record.json`. **Load-bearing: prove the boxes that composed on paper compose in CODE — swap on-path hops mock→real (membership-driven), KEEP later-slice deps + the external boundary mocked via the frozen conftest (the false-swap trap), write the composition root = the WSGI app/routing IMPLEMENT deferred (B8); green the FROZEN flow test happy+failure, NEVER edit it — a non-composing flow routes to IMPLEMENT (component bug) / Phase 3 (bad contract), never an edit (B4/B5); the FRAMEWORK is CARRIED from build-record lld_notes, not re-picked; verification executed|static-trace, runtime gap ≠ escape.** Isolated PASS 1 retry — killed a framework-fabrication defect (runner picked frameworkless "pure-Python WSGI" vs ADR-0002's named stack + the build-record's pinned Django → added carry-framework-from-build-record rule). e2e PASS (0 cascade, MATERIALIZE-ORACLE→IMPLEMENT×3→INTEGRATE) held across a strongly variant fresh oracle (Flask + `src.app.create_app` DI-factory + `src.*` namespaces vs golden Django + `freelancer_app.wsgi`) — read the actual frozen surface + carried the actual pinned framework, composed clean. Golden promoted (Django composition root, F1 happy+failure green). Schema/rules/test → `INTEGRATE.md`.
- **DIAGNOSE** (role 5/8, **standalone self-heal-vs-escape adjudicator, B6**, skeleton-build mode, §5.8) → `.build/skeleton/diagnosis.json` {verdict self-heal|escape|flaky-quarantine + confirmed classification + flaky/stall/reflection blocks + routable_diagnosis|self_heal|quarantine}. In: a verifying role's BLOCKED record + frozen oracle/contracts/ADR/aPRD/locks + built code. **Load-bearing: escape ONLY on a true STALL after a reflection pass — never on raw count, never on a misread spec, never on a flaky red — an escape MUST carry a routable diagnosis; re-derive from the frozen inputs, the producer's provisional escape is a HINT not evidence.** Ordered discriminator + the input-form/standalone/test calls → `DIAGNOSE.md` + `D13`. PASS (0 retries) — isolated fresh-verifier 4/4 across the decision space (misread→self-heal, progress[escape-on-count trap]→self-heal, test-order-flaky→quarantine, async-CT1-vs-INV6→escape Phase 3 w/ mis-route corrected); e2e PASS (no-op STOP on golden GREEN records + producer's exact blocked schema, `attempts[]`-absent→`basis:producer-asserted`). Golden reds → `_fixtures/greenfield-build-reds/`. Variance: a stale contract label vs a satisfiable frozen oracle reads self-heal (oracle=done-authority) OR escape (contract-vs-frame) — both defensible.
- **VERIFY-OUTPUT** (role 6/8, **authoritative verification-ladder gate + NFR-wiring check, B7**, skeleton-build mode, §5.7/§8) → `.build/skeleton/verification.json` {per-layer contract/flow/acceptance{visible,held_out}/class_ext/nfr + per-AC visible+held_out + verdict verified|blocked + (blocked) escape{failing[],failure_signature,provisional classification,route:self-heal→DIAGNOSE}}. In: integration-record + build-record (PRIMARY) + FROZEN oracle(lock+manifest+tests+conftest) + src/ + nfr-mechanisms + components + frozen locks. **Load-bearing: AUTHORITATIVE run of "done" — RE-RUN/RE-TRACE every layer against the frozen oracle + the code on disk; a producer's self-reported `pass` is a CLAIM not evidence (acceptance visible+held_out run by NO producer — VERIFY-OUTPUT runs them first; held_out is the anti-overfit lever B7); any red → blocked + failing id routed to self-heal/DIAGNOSE, FLAG never fix, never edit a frozen test (B1/B4/B5); NFR-wiring gate checks M* in `mechanisms[]` ONLY (mechanisms:[] → vacuous pass — frame-satisfied NFRs are NOT M*, anti-FP mirror MAP-NFR/H5); class-ext fires only what the oracle materialized (greenfield → n/a); method executed|static-trace, runtime gap ≠ red (D12).** PASS (0 retries) BOTH directions: isolated clean→verified (4 layers green, held_out legit-pass, grounded traces, no frozen edits) + planted-overfit (entry-page 500 for host≠visible-localhost)→blocked (AC1 held_out:fail/visible:pass caught while green build-record/integration-record both claimed pass — held_out + don't-trust-the-records both proven; code unfixed, oracle untouched, routed DIAGNOSE). e2e PASS (0 cascade, IMPLEMENT×3→INTEGRATE→VERIFY-OUTPUT — data-chain window per D8, DIAGNOSE is the self-heal branch off VER not a forward producer; held across a variant fresh build where C2 wrote oauth_provider.py vs golden INTEGRATE). Golden promoted. Schema/rules/test → `VERIFY-OUTPUT.md`.
- **IMPLEMENT** (role 3/8, **builder — cannot edit oracle, B4**, skeleton-build mode, §5.5) → `src/freelancer_app/<module>/*.py` (component code, the only LLD) + `.build/skeleton/build-record.json` (per-unit: files, implements_contracts, contract_tests greened, traces, status, escape|null + provenance/commits). In: build-plan + FROZEN oracle(lock+manifest+tests+conftest) + contracts + components + data-model + frozen locks + adr/log. **Load-bearing: make the pre-authored IMMUTABLE oracle green, ZERO acceptance authority, NEVER edit a test — needing to = ESCAPE with a routable diagnosis (B5); ONE component/invocation auto-selected; contract tests partition PER TEST FUNCTION by SUT not per file (D12); build only your namespace, no module/trace bleed; LLD+framework pick live here (B8); verification executed|static-trace, runtime gap ≠ escape.** Isolated PASS 2 retries — R1 partition (runner claimed whole test file via oracle.json provider map → C1 wrote C2's code + greened R5 tests; sharpened to per-SUT + no-bleed); R2 verification-method (runner escaped to Phase 1 on the missing interpreter → runtime gap is harness concern, static-trace not escape). Escape path also validated (R0: correct B5 route on an unsatisfiable frozen assertion → drove the MATERIALIZE-ORACLE hardening above). Fresh verifier CLEAN A–F (7 contract tests satisfiable, clean partition, no frozen edits, no bleed). e2e PASS (0 cascade, BUILD-PLAN→MATERIALIZE-ORACLE→IMPLEMENT; held across a variant oracle namespace — IMPLEMENT reads the actual frozen namespace, not golden-hardcoded). Variance: namespace + per-component obligation count track the oracle variant (e2e C1 had 0 driven-SUT tests, still provided its import surface). Golden promoted (built skeleton `src/` 8 files + `build-record.json`, 3 green units / 7 tests). Schema/rules/test → `IMPLEMENT.md`.
- **CRITIQUE** (role 7/8, **adversarial anti-cheat semantic-diff reviewer of the built diff, B7**, skeleton-build mode, §5.7/§8 — **last before DEMO-GEN**) → `.build/skeleton/critique.json` {verdict clean|blocked + blocking issues[] over 5 categories (hardcoded-fixture, swallowed-failure, stub-branch, under-complexity, gold-plating) w/ per-issue routes_to; clean → internal gate + DEMO-GEN, blocked → self-heal loop}. In: verification.json(GATE, verdict==verified) + build-record/integration-record(the diff inventory) + build-plan + src/(the DIFF) + contracts(honor oracle) + FROZEN oracle tests+conftest(fixture-literal oracle) + aprd(trace oracle) + components + locks. **Load-bearing: a GREEN oracle is NOT proof of honest code — RE-READ the code; build-record `green`/VERIFY `verified` say WHAT passed never WHY; you catch the cheats held-out can't (lane vs VERIFY-OUTPUT, which RUNS the ladder — you inspect the CODE for cheating the green); JUDGE PRODUCTION behavior not only test-greening — a fabricated success/echoed fixture-literal blocks even on a mocked/untested path; the dominant skeleton-build FP guard: an HONEST deferral stub returns NEUTRAL/deny (None/raise), a stub that FABRICATES success or echoes a fixture literal is a cheat (H14/§5.4).** Isolated PASS 1 retry BOTH directions — **retry killed a mocked-path miss**: the category-3 stub FP guard waved through a `uid-mock` fixture-echo / auth-fabrication in a mocked module → sharpened categories 1+3 to fire on fabrication regardless of test-greening (judge production behavior). Planted-cheat fixture `_fixtures/greenfield-build-reds/critique-cheats/` (3 cheats: session_checker fixture-echo, wsgi swallow, gold-plated module) → blocked, all caught + correct categories + 0 FP on honest controls (fresh verifier confirmed both directions). e2e PASS (0 cascade, INTEGRATE→VERIFY-OUTPUT→CRITIQUE — data chain per D8, DIAGNOSE is the self-heal branch not a forward producer; held clean across a strongly-variant fresh composition: views/urls/settings split vs golden monolithic wsgi.py). Golden promoted. Schema/rules/test → `CRITIQUE.md`.

### 2026-06-07 — process note (no spec impact)
- **The Agent tool always starts a FRESH agent; there is no SendMessage to continue a paused one.** A continuation attempt via an `Agent` spawn with `to:<id>` in the body did NOT route to the existing runner — it spawned a fresh agent that went off-script (authored an unverified RE-RANK + fixtures + edited the tracker); reverted all of it. **Lesson:** test interactive Phase B by appending `[CLIENT REPLY] …` to a fresh runner's pasted prompt, not by trying to continue a paused agent.

### 2026-06-08 — Phase 4 demo gate (PHASE 4 COMPLETE)
- **DEMO-GEN** (role 8/8, **client demo+accept gate, interactive:true two-phase D8 — LAST Phase-4 prompt**) → `.build/skeleton/demo/demo.md` (Phase A client-facing UI click-through) + `demo/demo.json` (Phase B immutable acceptance record + learnings → Phase 1 re-rank, B10). In: verification.json(GATE, verdict==verified) + critique.json(GATE, verdict==clean) + integration-record(running composition/routes) + build-record(traces/mocked_deps) + flows(F1 narrative + demonstrable-AC set) + aprd(AC text oracle) + locks. **Load-bearing: a demo shows ONLY what the verified build RUNS — each demoed AC traces to a GREEN verification per_ac + a real composition route; NEVER fabricate a screen/AC (demonstrable set = F1.traces ∩ verified per_ac = AC1+AC5, NOT the feature ACs of unbuilt slices — showing one is the demo-fabrication cheat); recognition-over-recall client touch (watch+accept, no authoring); a not-quite is recorded + routed (Phase 1 priority / Phase 0 WHAT), never an edit (§5.9/B5).** Isolated PASS 1 retry — pinned `learnings` element shape to PLAIN-STRING (reject run drifted to objects; same PR2 field-shape-determinism class as RESOLVE-LOCAL); all paths exercised (Phase-A no-reply / Phase-B accept→delivered / Phase-B reject→Phase 1 / already-accepted guard→STOP), fresh-verifier 7/7 (no-fabrication, grounding, plain-language, graceful-failure all clean). e2e PASS (0 cascade, VERIFY-OUTPUT→CRITIQUE→DEMO-GEN data-chain per D8). Golden `demo/` promoted. Schema/rules/test → `DEMO-GEN.md`. **PHASE 4 COMPLETE — skeleton-build chain BUILD-PLAN→…→DEMO-GEN done. The greenfield delivery pipeline's once/skeleton pass is now authored + tested across ALL 5 phases; remaining = deferred modes only (RE-RANK D8 · Phase-3 INCREMENT D9 · Phase-4 SLICE-BUILD D11).**

### 2026-06-08 — Phase 1 RE-RANK (PHASE 1 COMPLETE)
- **RE-RANK** (role 6/7, living-roadmap re-ranker, authored out of phase order per D8 — **PHASE 1 COMPLETE**) → `.roadmap/08-rerank.json`. In: 07-sequence-reviewed(base order + value/risk/coarse depends_on) + build-dag(the real COMPONENT DAG, §5.11) + components(traces/realizes_seam = projection key) + 02-slices(requirements) + skeleton demo(accepted-status + learnings). **Load-bearing: re-order REMAINING slices ONLY on material new info (RM6/§14 anti-thrash) — project the real component DAG onto slices (seam-realizers = skeleton-built/completed; each remaining component → its slice by characteristic [slice-unique] requirement), refine the coarse slice deps; a removed dep that frees a risk-retiring slice triggers an RM5 re-order, no material change → verdict:unchanged; completed slices pinned (§5.9), value carried verbatim (client-owned), defects route never patch.** Isolated PASS 1 retry — killed completed-prereq delta-noise (list a satisfied completed prereq as `confirmed` so depends_on_delta isolates the genuine refinement; PR2 field-semantics determinism, cf RESOLVE-LOCAL/DEMO-GEN); fresh-verifier re-derived clean. e2e PASS (0 cascade, DERIVE-COMPONENTS→DERIVE-TESTS→RE-RANK) across a VARIANT cut (5-comp persistence-folded → real DAG drew an Invoice→Time-Logging edge → S3's coarse S2-dep CONFIRMED not removed → verdict:unchanged — the correct OPPOSITE of the golden outcome, proving the re-rank derives from the actual DAG not the memorized answer). Golden promoted (verdict:re_ranked; remaining S4→S3→S2 — S3 promoted on the removed coarse S3→S2 dep + risk-first). Schema/rules/test → `RE-RANK.md`. **PHASE 1 COMPLETE; RE-RANK was the last deferred phase-order role — remaining = increment/slice modes only (Phase-3 INCREMENT D9 · Phase-4 SLICE-BUILD D11).**

### 2026-06-08 — Phase 3 INCREMENT chain start (D14)
- **DERIVE-COMPONENTS increment mode** (role 1/8, head of the Phase-3 slice loop; `DERIVE-COMPONENTS.md` now DUAL-MODE, dispatched on frozen-skeleton presence — D14) → `.hld/slices/<slice_id>/components.json`. Auto-selects next un-incremented slice from `08-rerank` `remaining_sequence`; names the touched subgraph (introduced box + reused deps/ingress) extending the FROZEN skeleton; `new_components`=[] in greenfield (full R-set already homed). **Load-bearing: extend-never-redraw (H14) + touched-set EXCLUDES other-slices' boxes** (a future-slice consumer of the introduced box is not this slice's). Isolated PASS 1 retry — killed over-broad touched-set rule pulling downstream-slice boxes C4/C5 into S4 (fresh verifier 10/10). e2e PASS (0 cascade — head-clamped single step, golden-seeded; 2 independent rooms structurally identical, only benign reused emission-order variance). Golden `.hld/slices/S4/` promoted. Schema/rules/test → `DERIVE-COMPONENTS.md` Part B; design call → D14.
- **DEFINE-CONTRACTS increment mode** (role 2/8; `DEFINE-CONTRACTS.md` now DUAL-MODE, D14 pattern) → `.hld/slices/<slice_id>/contracts.json`. Names the slice's contract surface = frozen contracts on edges incident to an introduced box, carried VERBATIM; `new_contracts`=[] in greenfield (skeleton contracted the full edge set). **Load-bearing: slice contract surface = edges incident to an introduced box WHERE THE OTHER ENDPOINT IS ALSO IN `touched_components`** — inherits DERIVE-COMPONENTS' D14 touched-set as the membership gate; an in-edge from a caller introduced by a different slice (C4->C3/CT5, C5->C3/CT7) is EXCLUDED (that seam is the caller's increment). Isolated PASS 1 retry — killed the D14 over-inclusion of future-slice in-edges (CT5/CT7); fresh verifier 8/8. e2e PASS (0 cascade, DERIVE-COMPONENTS increment→DEFINE-CONTRACTS increment, golden frozen-skeleton seeded; threaded the real on-disk components increment clean, output input-identical). Golden `.hld/slices/S4/contracts.json` promoted. Schema/rules/test → `DEFINE-CONTRACTS.md` Part B.
- **RESOLVE-LOCAL increment mode** (role 3/8; `RESOLVE-LOCAL.md` now DUAL-MODE, D14 pattern + D15 calls) → `.hld/slices/<slice_id>/deferred-decisions.json`. Per-slice local-queue drain: queue = skeleton-ledger `re_deferred[]` with `defer_to==slice` (role-deferred skipped); greenfield → empty (correct, cf `new_components`=[]). **Load-bearing: inherits the skeleton-resolved locals on its touched boxes BY REFERENCE (H14, never re-opened) + routes foundational `slice_deferred` forks OUT to Phase-2 (never resolved here)** — design calls in D15. Isolated PASS (0 retries, fresh verifier 8/8). e2e PASS (0 cascade, DERIVE-COMPONENTS→DEFINE-CONTRACTS→RESOLVE-LOCAL increment, golden frozen-skeleton seeded; threaded real on-disk upstream, counts walked to the real 2-inherited/1-routed). Golden `.hld/slices/S4/deferred-decisions.json` promoted (S4: queue empty, ADR-0007/0008 inherited on C2, DP5 routed). Schema/rules/test → `RESOLVE-LOCAL.md` Part B.
- **DERIVE-TESTS increment mode** (role 7/8; `DERIVE-TESTS.md` now DUAL-MODE, D14 pattern + D19 calls) → `.hld/slices/<slice_id>/test-specs.json`. Slice design-layer oracle = the NEW flow test `T-F*` (asserts_ac/failure_path/traces carried VERBATIM from the slice flow — the genuinely-new artifact, cf MODEL-FLOWS) + the frozen contract tests its touched seams INHERIT by reference (`inherited_contract_tests` — id/target/source_ref only, assertions never copied, H14); `new_contract_tests`=[] greenfield; **NO build-DAG (skeleton-only, H7 — increment emits one output not two).** **Load-bearing: inherit frozen `T-CT*` by reference + flow test is new + exclude other-slices' CT* (the D14 trap at the test level).** Isolated PASS (0 retries, fresh verifier 9/9). e2e PASS (0 cascade, DEFINE-CONTRACTS→MODEL-FLOWS→DERIVE-TESTS increment, golden frozen-skeleton seeded; `asserts_ac` carried the on-disk flow's actual AC set verbatim — proving derivation-from-input not a memorized golden). Golden `.hld/slices/S4/test-specs.json` promoted (regenerated from golden inputs; e2e benign variance not promoted). Schema/rules/test → `DERIVE-TESTS.md` Part B; design call → D19.
