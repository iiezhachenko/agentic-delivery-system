# Pipeline Prompt-Build Tracker

> My map across fresh sessions. Read this first, skip re-reading every spec.
> **Caveman register in chat. Clean prose inside the prompts I author.**

---

## YOU ARE HERE

- **Phase being built:** **Phase 0 COMPLETE.** Phase 1 (Roadmap) 6/7 authored (RE-RANK deferred, forward-blocked on Phase 3). **Phase 2 (ADR) COMPLETE (7/7).** **Phase 3 (HLD) — 4/8 authored (DERIVE-COMPONENTS, DEFINE-CONTRACTS, RESOLVE-LOCAL, MODEL-DATA).** Next: MAP-NFR.
- **Last prompt completed:** **MODEL-DATA** → `prompts/03-hld/MODEL-DATA.md` (Phase 3 role 4/8, spec §5.5/§8, prompt-library "MODEL-DATA", **SKELETON pass**). interactive:false (internal structural sweep — reads disk, writes disk, stops; data ownership is high-blast but the senior-reviewer gate is a later RECONCILE/CRITIQUE+freeze concern, §9). **Produces the AUTHORITATIVE logical data model from frozen ENTITIES + the Persistence ADR + the PROPOSED ownership: every E* → its SINGLE owning component, the logical relationships, persisted-vs-derived, field-schema deferral. FORMALIZES (confirm/verify) DERIVE-COMPONENTS' `owns_entities[]` — does NOT re-propose from scratch (D-call a).** In (5): `.aprd/aprd.frozen.md` (ENTITIES E1–E7 = the set to model + their durable-vs-derived nature + the relationships their descriptions name; R* trace oracle) + `.adr/adr.lock`+`.adr/log/` (the **Persistence ADR ADR-0003** = the storage frame: store technology + the relational entity chain) + `.hld/skeleton/components.json` (`owns_entities[]` = PROPOSED single-owner to formalize + `responsibility` = ownership basis) + `.hld/skeleton/contracts.json` (the **auditable single-owner proof** — `shared_data` seams = who-writes/reads the store, `sync_api` = who-reads-another's-entity-via-owner) + `.roadmap/06-foundation-cut.json` (INV2/3/4/5 floor + `deferred[]` = the FIELD-LEVEL schemas NOT to invent). Out (1): **`.hld/skeleton/data-model.json`** {aprd/adr_lock/adr_log/components/contracts/foundation_cut refs, lock_verified, class, mode:skeleton, skeleton_id, persistence{adr_refs, store, persistence_component, model_basis}, entities[{id,name,logical_description,owner,owner_basis,persisted,accessed_by[{component,access(read-write|read),via:CT*}],relationships[{to:E*,cardinality,basis}],field_schema_deferred_to,field_schema_note,honors_inv[],traces[R*]}], ownership{E*:C*}, coverage{entities_in_scope/modeled, entity_orphans, owning_components, persisted_entities, derived_entities, single_owner_verified, shared_write_check}, ownership_defects[{entity,kind,components,finding,route}], structural_defects[], data_model_counts{entities,owned,owners,with_relationship,field_schemas_deferred}}. **The ownership rule (THE discriminator, 4 clauses):** (1) owner = the component whose responsibility GOVERNS the entity (sole writer/producer/authority), read from `owns_entities[]`, FORMALIZED not re-derived; (2) **ownership = LOGICAL AUTHORITY not physical storage** — a dedicated store/persistence box stores all entities but OWNS NONE (`owns_entities=[]` legit); do NOT re-assign everything to the store (the anti-naive-modeler guard); (3) **`persisted` vs DERIVED** — persisted = durable stored state (has a `shared_data` store-write seam); derived = computed on demand, not stored (e.g. the Invoice E4 — generated-server-side-and-streamed, NO store-write seam, owner = sole PRODUCER — the absence of a write seam is CORRECT not a gap); (4) **ambiguity = BOUNDARY DEFECT → flag + route, never re-cut**: two-owner / orphan → DERIVE-COMPONENTS §5.2; shared-write (a non-owner WRITE in the contracts) → DEFINE-CONTRACTS §5.3 — internal HLD re-route (NOT Phase 2/0). Load-bearing: **single-owner verified TWO ways** (walk `owns_entities[]` each E* once + walk contracts sole-writer-per-persisted-entity == declared owner — `accessed_by` makes it auditable, mirroring DEFINE-CONTRACTS' kind-enumeration discipline); **named-not-designed** (logical entities + relationships + cardinality only; NEVER field-level columns/types/DDL/table-layout — those are the cut's `deferred[]` per-slice items: project E2/E5/E6/E7→S4, time-entry E3→S2, invoice E4→S3, identity E1→S1 skeleton seam); **relationship reciprocity** (persisted↔persisted bidirectional listings must AGREE — `X one-to-many Y` ⟺ `Y many-to-one X`; a derived→source aggregation is ONE-DIRECTIONAL, exempt from back-listing — the R1/R2 fixes); **honors INV2/3/4/5** (no per-entry currency/rate entity, no org/role entity, no client-login entity); **FLAG-never-fix lane** (no re-cut, no contract edit, no re-propose). Lane-clean (no graph edit [DERIVE-COMPONENTS], no contract kind/shape/failure edit [DEFINE-CONTRACTS — read to verify only], no local ADRs [RESOLVE-LOCAL], no NFR mechanisms [MAP-NFR], no flow path [MODEL-FLOWS], no cross-cutting, no tests/build-DAG [DERIVE-TESTS], no hostile audit [role 8], no invented field schema, no re-cut on ambiguity, no client). Escapes: HALT on missing/unparseable aprd/lock/log/components/contracts/cut, **no Persistence-category ADR** in the frame, components∥contracts carrying non-empty defect arrays, unfrozen lock, skeleton-already-frozen (=increment trigger, not authored), non-greenfield; ambiguous ownership → ownership_defects[] → DERIVE-COMPONENTS/DEFINE-CONTRACTS (internal re-route). **Isolated test PASS (0 retries)** — fresh SEPARATE verifier graded A–H (entity-bijection, single-owner-formalized, ownership=logical-authority-not-store, accessed_by-grounded-sole-writer=owner, persisted-vs-derived, relationships-logical-not-designed, field-schemas-deferred-none-invented, traces/INV/accounting/lane) **all PASS, 0 defects** (golden 6-component graph: 7 entities, E1→C2/E2,E5,E6,E7→C3/E3→C4/E4→C5, C1 Data Store owns []; E4 correctly DERIVED [persisted:false]; CT6 cross-owner READ of E3 by C5 correctly not-a-shared-write; no invented schema). One verifier soft-note (E2-read-by-C5 attributed to CT7 by inference — defensible, not a defect). **Windowed-e2e PASS (overall PASS, 0 cascade):** N=role 4 → real-data-chain window **DERIVE-COMPONENTS→DEFINE-CONTRACTS→MODEL-DATA** (rooted at the real producers — RESOLVE-LOCAL [positional N-1] writes nothing MODEL-DATA reads [ledger+drafts], so rooted at the real upstream like the SEQUENCE-REVIEW precedent; head seeded golden frozen aPRD+ADR log/lock+cut). Ran with **run-to-run variance**: head emitted a **6-component / 14-edge** graph (richer deps — C4→C2, C5→C2/C4 direct seams) → DEFINE-CONTRACTS contracted clean (14 contracts, 4 shared_data + 10 sync_api, CT7=C5→C1 shared_data became a READ-ONLY aggregate query) → MODEL-DATA consumed clean: single_owner held on the variant topology, **correctly read CT7 as `read` not `read-write`** (the key variance test — shared_data seam that's a query not a write), E4 derived, 0 defects. Fresh SEPARATE verifier A–H **all PASS**. **The verifier flagged 1 MINOR non-blocking blemish → hardened the prompt (2 fixes, both about the DERIVED entity E4 — the genuinely tricky modeling case):** (R1) **relationship reciprocity** — the e2e run had E2↔E7 cardinality mismatch (`many-to-one` from E2 vs `one-to-one` from E7); root: relationships listed from both endpoints with no reciprocity rule → added "persisted↔persisted bidirectional listings must agree" rule; (R2) the reciprocity rule then OVER-REACHED on the golden regen (flagged `E4→E3` aggregation as missing a reciprocal — but a persisted source shouldn't list a stored FK back to a COMPUTED artifact) + E4's `field_schema_deferred_to` varied (S1 vs S3) → refined: **derived entity exempt from back-listing (one-directional aggregation), derived field-schema defers to the PRODUCING slice (invoice→S3) not S1**. Golden regenerated from final prompt: reciprocity clean (persisted-persisted), E4 derived→S3 deterministic, sole-writer=owner ×7, no invented schema, counts tally → promoted to `_fixtures/greenfield-clean/.hld/skeleton/data-model.json`. **Judgment-variance note (benign):** the data model is a FORMALIZATION stage — ownership/persisted-derived/no-shared-write hold every run (derived from the inputs, not invented); the run-to-run variance is upstream (the component cut + which contracts are shared_data-read vs sync_api) + the derived-entity edge cases (E4 deferral slice, relationship phrasing) — all defensible, all bounded by the final prompt's reciprocity + derived-entity rules. Invariants every run: entity bijection, single-owner formalized + verified two-ways (sole-writer=owner), ownership=logical-authority-not-store, persisted-vs-derived, named-not-designed (NO field schema), persisted-reciprocity + derived-one-directional, honors-INV, FLAG-never-fix boundary-defect lane.
- **PRIOR last prompt (RESOLVE-LOCAL):** **RESOLVE-LOCAL** → `prompts/03-hld/RESOLVE-LOCAL.md` (Phase 3 role 3/8, spec §5.4/§8, prompt-library "RESOLVE-LOCAL (emits ADR)", **SKELETON pass**). interactive:false (internal sweep — reads disk, resolves local forks, writes draft ADRs + a queue-resolution ledger, stops; structure is team's domain, §9). **Drains the LOCAL-decision queue Phase-2 TRIAGE routed to Phase 3 (`02-triage.json` `deferred_queue[]`), resolves each fork the SKELETON forces + emits each as a `mode:slice` local ADR DRAFT appended to the shared decision history — the feedback loop Phase 2 promised (H3).** In (8): `.adr/02-triage.json` (`deferred_queue[]` = work list — the local DPs) + `.adr/01-decision-points.json` (DP BODIES: decision/category/forced_by/fork_evidence/cut_ref) + `.hld/skeleton/components.json` + `.hld/skeleton/contracts.json` (the EMERGING STRUCTURE — owning-component lookup + the escalation test) + `.aprd/aprd.frozen.md` (R*/AC*/A*/C*/E* trace oracle + forces) + `.adr/adr.lock` (FROZEN baseline gate + the id-continuation point — local ADR ids continue after baseline max ADR-0006) + `.adr/log/*.md` (frozen frame a local resolution may NOT re-decide) + `.roadmap/06-foundation-cut.json` (`deferred[]` = per-slice HOW-items grounding re-deferral + INV* floor). Out (2): **`.adr/drafts/<NNNN>-<slug>.draft.md`** (one Nygard ADR draft per RESOLVED fork — status:Proposed, mode:slice, scope:local, frontmatter {id,title,status:Proposed,date,class,scope:local,mode:slice,category,traces,supersedes:null,superseded_by:null,**component:<owning C*>,resolves:<DP id>**} + body Context/Decision/Alternatives/Consequences) + **`.adr/deferred-decisions.json`** (§10-named LEDGER: {triage_ref/decision_points_ref/components_ref/contracts_ref/aprd_ref/adr_lock_ref/adr_log_ref/foundation_cut_ref, lock_verified, class, mode:skeleton, skeleton_id, baseline_adr_max, local_queue_in, resolutions[{dp_id,decision,category,forced_by,cut_ref VERBATIM, owning_component, disposition, adr_id, draft_ref, defer_to, escalate_to, rationale}], **resolved[{dp_id,adr_id}] / re_deferred[{dp_id,defer_to}] / escalated[{dp_id}]** (pinned-shape buckets), drained, local_adrs[], escalations[{dp_id,surfaced_as,finding,route}], note, resolve_counts{queue_in,resolved,re_deferred,escalated,local_adrs_emitted}}). **THE load-bearing design call (b) RESOLVED — how a local ADR appends to a FROZEN log:** drafts-then-freeze, mirroring Phase-2 precedent — RESOLVE-LOCAL writes **Proposed `mode:slice` DRAFTS to `.adr/drafts/` (ids continuing ADR-0007+ after the frozen baseline max), NEVER mutates the frozen `adr.lock` or the immutable `.adr/log/`**; local ADRs stay mutable through the Phase-3 role-8 RECONCILE/CRITIQUE gate (could loop back, D6); the mechanical Phase-3 freeze (non-LLM, no prompt) promotes drafts→log as Accepted + writes a new lock AFTER the gate. **Design call (a) RESOLVED — input form:** reads `02-triage.deferred_queue[]` + `01` DP bodies directly; **RESOLVE-LOCAL itself WRITES `deferred-decisions.json`** (the §10-named ledger), it is NOT a seeded input (D7 no-mint — no producer writes it upstream). **The disposition discriminator (THE judgment, derive-never-default, 3 dispositions):** `resolved` = the SKELETON structure forces the fork now (ALL THREE: in-skeleton owning-component exists + skeleton-once-cross-cutting-not-a-box-internal [§1.2 defers class/fn/algo/library internals to build time] + not slice-tagged) → pick option + emit Proposed draft; `re-deferred` = owned by a later slice / box-internal impl detail / slice-tagged (`cut_ref:deferred:Sx` or cut `deferred[]` routes it) → record `defer_to` + reason, NO ADR (**default when unsure** — RM9 anti-waterfall: resolving a slice's local before the slice is design-layer waterfall); `escalated` = the "local" is actually FOUNDATIONAL (resolving forces a cross-box change / changed contract KIND / INV violation) → `escalations[]`→Phase 2, NO ADR (§5.4/§5.11 thin-cut signal). **Design call (c) RESOLVED — graceful near-empty:** the skeleton pass may legitimately resolve FEW or ZERO locals (most locals live inside component internals drained per-slice in increment mode); a near-empty resolved set with everything else cleanly re-deferred is a complete deliverable, not a gap; empty `deferred_queue[]` → empty ledger + note + zeroed counts, stop. Load-bearing: **drain the whole queue** (`drained==deferred_queue`, resolved+re_deferred+escalated==queue_in, partition); **carry DP body verbatim** (decision/category/forced_by/cut_ref); **resolve with LIVE alternatives force-traced** (D1/D3 — EVALUATE-DECIDE+SYNTHESIZE compressed into one role: NAMES concrete local options from `fork_evidence`+frame+structure AND picks; Alternatives read live + reject by R*/AC*/A*/C*/INV force, never bare-worse, **never recalled market/popularity/adoption claim** [EVALUATE-DECIDE lesson], default-among-equals honest); **NEVER re-decide a foundational ADR** (reference-yes / re-open-no — else escalate); **id continuation monotonic** (ADR-0007+ in queue order, id only for resolved); **traces verbatim no padding** (forced_by ∪ cited-in-prose); **deterministic emission** (queue array order); **pinned bucket element shapes** (the R1 e2e fix — resolved=`{dp_id,adr_id}` / re_deferred=`{dp_id,defer_to}` / escalated=`{dp_id}`, never bare id strings). Escapes: HALT on missing/unparseable triage/01/components/contracts/aprd/lock/log/cut, components∥contracts carrying non-empty defect arrays, unfrozen lock, skeleton-already-frozen (=increment trigger, not authored), non-greenfield; queued-local-actually-foundational → escalations[]→Phase 2. Lane-clean (no graph edit [DERIVE-COMPONENTS], no contract kind/shape/failure edit [DEFINE-CONTRACTS], no foundational resolution [Phase 2], no data-model [MODEL-DATA], no NFR [MAP-NFR], no flows [MODEL-FLOWS], no cross-cutting-component placement, no tests/build-DAG [DERIVE-TESTS], no hostile audit [role 8], no log/lock mutation, no freeze, no client). **Isolated test PASS (0 retries)** — fresh SEPARATE verifier graded A–H (queue-drained+accounting, verbatim-carry, disposition-correctness, no-foundational-re-decided, live-alternatives-force-traced, trace-fidelity, frozen-baseline-integrity, lane) **all PASS, 0 defects** (golden 6-component graph: 2 resolved [DP8 OAuth-library→C2, DP9 config/secrets→C6] + 2 re-deferred [DP3 PDF-lib→S3, DP11 test-strategy→S1]; lock+log untouched, drafts 0007/0008 Proposed/slice/local, accounting balanced; verifier flagged DP9 as the most borderline call [config/secrets arguably box-internal] but defensible). **Windowed-e2e PASS (1 retry, in budget):** N=role 3 → window **DERIVE-COMPONENTS→DEFINE-CONTRACTS→RESOLVE-LOCAL** (real data-chain — components.json feeds both downstream, contracts.json feeds RESOLVE-LOCAL; head seeded golden frozen aPRD+ADR log/lock+cut + the Phase-2 queue artifacts 01/02). Ran with **run-to-run variance**: head emitted a **6-component / 14-edge** graph (C2=Auth realizing domain+primary_external_integration, no dedicated Data Store box — a valid flat-monolith cut) → DEFINE-CONTRACTS contracted clean (14 contracts, 4 shared_data + 10 sync_api + 0 async, bijection) → RESOLVE-LOCAL consumed clean, mapping owning components correctly to the variant topology (DP3→C5, DP8→C2, DP9→C6, DP11→none), with a MORE conservative disposition (1 resolved DP9 / 3 re-deferred incl. DP8 re-deferred to S1 build — the borderline OAuth-library call resolved anti-waterfall this run). **The 1 retry fixed a REAL PR2 schema-determinism defect:** the disposition BUCKETS (`resolved`/`re_deferred`/`escalated`) had under-specified element shape — the isolated run emitted `resolved:["DP8","DP9"]` (bare ids) but the e2e run emitted `resolved:[{dp_id,adr_id}]` (objects); two clean runs, two shapes for the field the role-8 audit consumes → violates PR2 (deterministic format). Fix pinned each bucket's element shape exactly (resolved=`{dp_id,adr_id}` / re_deferred=`{dp_id,defer_to}` / escalated=`{dp_id}`, never bare strings) → tail re-run on same upstream produced the pinned object form, partition+accounting held; no cascade (RESOLVE-LOCAL = window tail). Golden `deferred-decisions.json` + drafts `0007`/`0008` regenerated from the GOLDEN 11-edge components.json + contracts.json with the final prompt (2 resolved DP8/DP9 + 2 re-deferred DP3→S3/DP11, pinned shapes, accounting clean), promoted to `_fixtures/greenfield-clean/.adr/`. **Judgment-variance note (benign, expected — borderline cross-cutting locals):** which locals the skeleton "forces" varies run-to-run — DP8 (OAuth integration library) resolved-now (isolated/golden) OR re-deferred-to-S1-build (e2e), and DP9's owning component C6 (isolated) OR C2 (golden) — both defensible (the OAuth library is a borderline skeleton-wires-the-seam vs box-internal call; config/secrets is genuinely cross-cutting with no dedicated owner box in a flat monolith). DP3 (PDF lib, cut_ref deferred:S3) re-defers every run (unambiguous slice-tag); DP11 (test strategy, no owning component) re-defers every run (defer target varies S1/S3/DERIVE-TESTS — the no-owning-slice ambiguity; a role name is a slight imprecision but an accurate destination). Invariants hold every run: full queue drain + partition + accounting, verbatim DP-body carry, live-alternatives force-traced, NEVER-re-decide-foundational (reference-yes/re-open-no), frozen-lock/log untouched (drafts-then-freeze), monotonic id continuation ADR-0007+, traces verbatim no-padding, pinned bucket shapes, FLAG-never-fix escalation lane (→Phase 2).
- **PRIOR last prompt (DEFINE-CONTRACTS):** **DEFINE-CONTRACTS** → `prompts/03-hld/DEFINE-CONTRACTS.md` (Phase 3 role 2/8, spec §5.3/§8, **SKELETON pass**, **THE load-bearing stage — H1 contracts-before-components**). interactive:false (internal structural sweep — reads disk, writes disk, stops; structure is team's domain, §9). **Puts the contract on every bare edge DERIVE-COMPONENTS drew: takes each edge (= a seam) and specifies `kind`/`shape`/`failure_modes`/`traces` — the seam spec DERIVE-COMPONENTS deliberately left off (edges-structure-only is its lane; the seam contract is THIS role's lane).** In (5): `.hld/skeleton/components.json` (PRIMARY — `edges[]` = the seams to contract, one contract per edge; `components[]` for traces/responsibility/owns_entities grounding) + `.aprd/aprd.frozen.md` (R* = trace oracle, E* = logical payloads that cross seams) + `.adr/adr.lock` (frozen gate + manifest) + `.adr/log/*.md` (the frame that decides KIND — Architectural-style ADR-0001 flat monolith + API-style ADR-0004 MPA/SSR + Persistence ADR-0003 → kinds skew sync_api/shared_data) + `.roadmap/06-foundation-cut.json` (INV* floor — esp. **INV6 single-server synchronous forbids async** queue/worker/broker; `deferred[]` = the field-level schemas NOT to invent). Out: **`.hld/skeleton/contracts.json`** {components_ref/aprd_ref/adr_lock_ref/adr_log_ref/foundation_cut_ref, lock_verified, class, mode:"skeleton", skeleton_id, contracts[{id:CT*, between:[from,to], kind, shape, failure_modes[], traces[R*], honors_adr[], honors_inv[]}], coverage{edges_in_scope, edges_contracted, edge_orphans, contracts_without_edge, kind_distribution{3 keys}}, frame_conflicts[], structural_defects[], contract_counts{contracts, edges_in_scope, with_failure_mode, with_trace}}. **The kind discriminator (THE load-bearing judgment, derive-never-default):** `sync_api` = synchronous request/response — in a flat-monolith single-server-synchronous frame (INV6) this is an **in-process function/method call** (one process, NO network hop between components); `shared_data` = read/write of durable persisted state the `to` component owns (canonical: edge INTO the persistence component); `async_event` = fire-and-forget — **INV6 FORBIDS it by default** (no queues/workers/brokers); an async edge with no frame-permitted forcing reason = `frame_conflicts[]`→Phase 2, never silently introduced. Expected greenfield distribution: shared_data for store-access edges + sync_api for the rest, **zero async_event** (correct, not a gap). Load-bearing: **one contract per edge — BIJECTION with the edge set** (consume edges verbatim, no add/drop/merge/split; CT* minted in edge-array order); **`shape` = named-not-designed (RM11/§1.2 lane line)** — a schema REFERENCE (the logical payload / E*-set crossing + responsibility), NEVER invented field-level detail (column names/table layout/JSON fields/types/endpoints) — the cut's `deferred[]` defers those schemas to owning slices (project→S4, time-entry→S2, invoice→S3); reference what the frame FIXED, defer what the frame deferred; **every contract states ≥1 `failure_mode` grounded in the seam's REAL failure surface** (shared_data: store-unavailable/constraint-violation/partial-write; sync_api: callee-error/not-found/no-valid-session) — **single-process means NO inter-process network-partition/message-loss/split-brain failure modes on in-process sync seams** (frame discipline, anti-fabrication); **every contract traces ≥1 R verbatim, no padding/minting**; **frame-fidelity escape** (a seam needing a frame-forbidden kind → frame_conflicts[]→Phase 2, never re-decide); **deterministic emission** (CT* in edge order); full accounting + **kind_distribution counted by enumerating each kind's CT* members independently** (NOT by subtraction/fudge) + sum-must-equal-contracts. Escapes: HALT on missing/unparseable components.json / **components.json carrying non-empty structural_defects∥frame_conflicts∥aprd_defects** (upstream routed an unresolved escape — frame not clean to contract) / missing-aprd / missing-or-unfrozen-lock / missing-log / missing-cut / **skeleton-already-frozen (=increment trigger, NOT authored)** / non-greenfield; seam needs frame-forbidden kind → frame_conflicts[]→Phase 2 (write compliant remainder). Lane-clean (no add/drop/re-cut components or edges [DERIVE-COMPONENTS owns graph], no authoritative data-model/field-schemas [MODEL-DATA + slice increments], no local-ADRs [RESOLVE-LOCAL], no NFR-mechanisms [MAP-NFR], no flow-path [MODEL-FLOWS], no cross-cutting, no tests/build-DAG [DERIVE-TESTS], no hostile audit [RECONCILE/CRITIQUE], no client). **Isolated test PASS (2 retries, in budget — both killed the SAME miscount-defect class, converging not repeating):** R1 — `kind_distribution` summed to 12 vs 11 contracts (classic miscount; per-contract kinds were correct, only the tally wrong) → added sum-must-equal-contracts check; **R2 — the sum-check was GAMED** (runner shifted one count shared_data→sync_api so the wrong split [8/3] still summed to 11 — the exact "swap that still sums right" failure VERIFY warned of) → added **count-each-kind-independently-by-enumerating-its-CT*-members** (sum alone insufficient; enumeration makes it auditable). Retry-2 fresh SEPARATE verifier graded A–H (bijection, kind-correctness, shape-named-not-designed, failure-modes-grounded, trace-fidelity, frame-id-fidelity, accounting, lane) — **all 8 PASS** (golden 11-edge topology: 4 shared_data into Data Store + 7 sync_api, 0 async; no invented schema, no distributed failure modes on in-process seams, no padded traces, all counts independently recounted clean). One behavior-preserving clarification after (keep member-enumeration in WORKING, emit exactly 3 integer keys — runner had materialized audit `*_members` arrays into the artifact; benign but off-schema). **Windowed-e2e PASS (0 retries, clamped):** N=role 2, N-2 doesn't exist → clamp to phase-first → window **DERIVE-COMPONENTS→DEFINE-CONTRACTS** (head seeded golden frozen aPRD + baselined ADR log/lock + cut). Ran with **run-to-run variance**: head emitted a **6-component / 8-edge** graph with persistence FOLDED into Identity&Auth (no dedicated Data Store box — a valid flat-monolith cut, the designed component-variance) → DEFINE-CONTRACTS consumed clean: **all 8 edges sync_api** (no store-component edge exists → no shared_data edges; the runner DERIVED this from topology rather than rote-applying "store=shared_data" — the kind discriminator is robust to cut variance), 0 async, bijection 8↔8, all ids/traces/failure-modes clean, no defects. Interface threaded clean, no cascade (DEFINE-CONTRACTS = window tail). Golden `contracts.json` regenerated from GOLDEN 11-edge components.json with final prompt (schema-clean: exactly 3 kind keys, 7 sync_api + 4 shared_data + 0 async, CT1–CT11 bijection), promoted to `_fixtures/greenfield-clean/.hld/skeleton/`. **Judgment-variance note (benign):** the contract KIND distribution tracks the upstream component CUT — a graph with a dedicated Data Store box yields shared_data store-edges (golden: 4); a graph folding persistence into domains yields all-sync_api (e2e: 8). Both correct: the discriminator derives kind from THIS graph's topology, not a fixed ratio. Invariants hold every run: bijection one-CT-per-edge, kind-derived-from-frame (async only if frame permits → always 0 here, INV6), shape-named-not-designed (no invented deferred schemas), ≥1 grounded failure-mode + no-distributed-modes-on-in-process-seams, ≥1 verbatim trace, frame-fidelity escape, independent-enumeration accounting (sum==contracts).
- **PRIOR last prompt (DERIVE-COMPONENTS):** **DERIVE-COMPONENTS** → `prompts/03-hld/DERIVE-COMPONENTS.md` (Phase 3 role 1/8, head of HLD pipeline, spec §5.2/§8, **SKELETON pass**). interactive:false (internal structural sweep — no client touch; structure is the team's domain, §9). **The first drawing stage: clusters the FULL requirement set into the full component graph (boxes + dependency edges) per the boundary-strategy ADR — the frame every slice later extends + the build DAG (H7/H13).** In (4): `.adr/adr.lock` (FROZEN ADR baseline gate + manifest adrs[]) + `.adr/log/<NNNN>-*.md` (baselined ADR bodies — the **boundary-strategy ADR** = Architectural style and/or Boundary strategy = CUT DRIVER; rest of frame constrains) + `.aprd/aprd.frozen.md` (R*=responsibilities to cluster, E*=entities to own) + `.roadmap/06-foundation-cut.json` (skeleton_seams[]=component-graph seed, INV*=frame floor). Out: **`.hld/skeleton/components.json`** {aprd_ref/adr_lock_ref/adr_log_ref/foundation_cut_ref, lock_verified, class, mode:"skeleton", skeleton_id, boundary_strategy{adr_refs,style,cut_basis}, components[{id,name,responsibility,owns_entities[E*],traces[R*],realizes_seam[],honors_adr[]}], edges[{from,to,reason}], coverage{requirements_in_scope/landed, requirement_orphans, entities_in_scope/owned, entity_orphans, components_without_requirement, seam_realization[{seam,present,realized_by}]}, structural_defects[], frame_conflicts[], aprd_defects[], component_counts}. **Component test (3-clause discriminator):** traces ≥1 R (else gold-plating→drop, H4) AND honors the frame (flat monolith/INV6 → logical responsibility units inside ONE deployable, NO distributed services/brokers/workers/scale-topology; a box needing such = frame conflict→Phase 2) AND is a responsibility unit NOT a layer (no Controllers/Services/Repositories boxes). Load-bearing: **APPLY the boundary-strategy ADR, never invent the cut (H2/H12)** — find category "Architectural style" (+ "Boundary strategy" if present); if boundary-strategy ADR absent (deferred to a slice), the style ADR governs (flat monolith → cluster by responsibility, no manufactured module boundary); **skeleton pass clusters the FULL R-set** (whole graph = build DAG; the walking-skeleton slice is just the thinnest PATH through it — MODEL-FLOWS' concern later); **bidirectional coverage** (every component traces ≥1 R + every in-scope R lands; report both, `components_without_requirement` must be empty — full adversarial gate is RECONCILE/CRITIQUE role 8); **single-owner entities** (each E* owned by exactly one component — proposal; MODEL-DATA role 4 formalizes; a shared infra box e.g. Data Store may own []); **edges = STRUCTURAL DIRECTION ONLY, never contracts** (`{from,to,reason}`, from depends on to; NO kind/shape/failure — DEFINE-CONTRACTS role 2 owns the seam spec; acyclic, cycle→structural_defects[]); **realize every present skeleton seam** (each seam→≥1 component, `seam_realization` map); **name responsibility not implementation** (no NEW impl detail the frame left open — library/internal-schema/endpoint/algorithm; a frame-FIXED choice may be referenced — OAuth provider, SSR, PostgreSQL — that's faithful, not a new decision); **deterministic emission = topological build order, most-depended-upon first** (C1 = build-first box; ties by lowest traces id). Escapes: HALT on missing-aprd/missing-or-unfrozen-lock/missing-log/missing-cut/**skeleton-already-frozen (=increment-mode trigger, NOT authored)**/non-greenfield; frame unbuildable→`frame_conflicts[]`→Phase 2; requirement unstructurable→`aprd_defects[]`→Phase 0 (route, never patch — H10). Stays in lane (no contracts/local-ADRs/data-model/NFR-mechanisms/flows/cross-cutting/tests/build-DAG-artifact/hostile-audit/client). **DESIGN CALLS (documented):** (a) **scoped to SKELETON pass only** — the per-slice INCREMENT mode (locate touched components + add only new-capability boxes, extends a FROZEN skeleton) is a separate not-yet-authored mode; guard HALTs if a frozen skeleton exists (D9 below); (b) **§5.1 Load+verify-frame folded into this head prompt** (guard + components.json header carries refs+lock_verified) — NO separate `00-inputs.json` artifact (D7 precedent: don't mint an artifact no authored consumer reads; §10's tree is illustrative); (c) **input = the BASELINED frozen `.adr/log/` + `adr.lock`** (the mechanical freeze run by hand on the golden Phase-2 drafts — see fixture note D9), NOT the Proposed `drafts/`+`adr-index.json` (spec §2 input is the baselined log). **Isolated test PASS (0 retries)** — fresh separate verifier graded A–H (component→R / R→component coverage, single-owner entities, frame fidelity / no re-decide, seam realization, edges-structure-only+acyclic, in-lane, schema+accounting) **all PASS** (5-component run: each domain owns+persists, persistence folded into Identity&Auth). One behavior-preserving prompt clarification after (Mandate 7: a frame-FIXED vendor/style may be referenced as context — reference what an ADR decided, never decide what it deferred). **Head-clamped windowed-e2e PASS (0 retries)** — DERIVE-COMPONENTS is the Phase-3 FIRST authored step (no upstream Phase-3 producer) → window clamps to this single step seeded from golden (frozen aPRD + baselined ADR log/lock + cut); identical to isolated. Re-ran with final prompt: fresh separate verifier A–H **all PASS** (6-component run: explicit **Data Store** infra box owning [], domain components own their entities). Golden `.hld/skeleton/components.json` (6 comps: C1 Data Store/C2 Identity&Auth/C3 Project Management/C4 Time Logging/C5 Invoice Export/C6 Web Ingress; 11 edges; all 10 R landed, all 7 E owned once, 4 seams realized) promoted to `_fixtures/greenfield-clean/.hld/skeleton/`. **Judgment-variance note (benign, expected — creative structural extraction like DECISION-EXTRACT/OPTION-GEN):** the component CUT varies run-to-run (5 vs 6 components; explicit Data-Store box vs persistence-folded-into-domains; which seam each component realizes) — all defensible flat-monolith cuts honoring the boundary-strategy ADR + INV6. Invariants hold every run: 3-clause component test, bidirectional R-coverage, single-owner entities, edges-structure-only (no contracts), seam realization, frame-fidelity (no distributed components, no ADR re-decided), topological emission, full accounting. The verifier NOTE (Data Store tracing R8/R10) is benign — the persistence box mirrors its governing ADR-0003's own R7–R10 trace set, coverage not distorted.
- **PRIOR last prompt (CRITIQUE):** **CRITIQUE** → `prompts/02-adr/CRITIQUE.md` (Phase 2 role 7/7, **THE last Phase-2 prompt — Phase 2 now COMPLETE**, spec §5.8/§8/§12, D8). interactive:false (adversarial review — reads disk, writes issues list, stops; no re-render/freeze/client touch, §9). **The hostile architecture reviewer on the RENDERED ADR drafts — the adversarial final gate before the mechanical freeze.** In: `.adr/adr-index.json` (enumerate the rendered draft set — review exactly what it lists) + each `.adr/drafts/<NNNN>-<slug>.draft.md` (the ADRs to attack) + `.aprd/aprd.frozen.md` (trace+coverage ORACLE: id-space R*/AC*/C*/E*/A*, the C* coverage target) + `.adr/04-conflicts.json` (RECONCILE's coherence verdict + in_scope_constraints + premise/deferred buckets — CRITIQUE is the *second*, ADR-level adversarial pass) + `.roadmap/06-foundation-cut.json` (FD*/deferred[]/INV* = the over-decided/not-yet/unforced oracle). Out: **`.adr/05-critique.json`** (spine-numbered 05 after 04-conflicts; mirrors Phase-0 CRITIQUE taking 08) {adr_index_ref/aprd_ref/conflicts_ref/foundation_cut_ref, class, skeleton_id, adrs_reviewed[], verdict(clean|blocked), issues[{id, category, target_adr, finding, fix_hint}], issue_count, critique_counts{adrs_reviewed, issues, by_category{7 cats}}}. **SEVEN blocking categories** (emit blockers only): `strawman-alternative` (alternative never live — DOA breaks hard C*/INV*, or fabricated discriminator), `untraceable-adr` (rendered frontmatter traces[] empty or names a phantom id not in aPRD id-space / cut INV set — D4), `uncovered-constraint` (in-scope C* no ADR traces + not deferred/premise — **C\* ONLY; INV\* are the floor NOT coverage targets**, D5), `contradictory-adrs` (two Decisions mutually exclusive / jointly INV-violating), `over-decided` (decision actually local → Phase 3, D2), `unforced-decision` (traces resolve but no aPRD element forces a ≥2-way structural fork — gold-plating, D4), `not-yet` (foundational but belongs to a later slice per cut deferred[] — decision-layer waterfall, D11). Blocking → loops back to **SYNTHESIZE-ADR** (§5.8 `Critiqued → Drafting`); clean → mechanical freeze (promote drafts→`log/`, Proposed→Accepted, write `adr.lock` — NOT CRITIQUE's job). **Load-bearing LANE LINE vs RECONCILE (role 5):** CRITIQUE is NOT RECONCILE-twice. RECONCILE ran coherence+coverage on the **decision JSONs**; CRITIQUE owns (a) the **NEW adversarial checks RECONCILE cannot do** — strawman/over-decided/unforced/not-yet (judge the *legitimacy* of the decision-making, not just mechanical coherence) + (b) a **backstop of trace/coverage/contradiction on the RENDERED FORM** (the render is a new artifact — transcription can drop a trace, lose a covering ADR, or produce two Decisions that now clash); it does **NOT** re-run RECONCILE's mechanical pass to override a `coherent` verdict already issued on the decision set. **Load-bearing ANTI-FALSE-POSITIVE discipline (mirror Phase-0 CRITIQUE resolution-test):** read the WHOLE context (cut+04-conflicts+traces+Alternatives) before blocking; **never re-litigate a logged pick** (check real/forced/traced/coherent/in-cut, NOT whether you'd choose the same); **degenerate-forced ≠ strawman + default-among-equals ≠ strawman** (the rejected option is real + the honest "default among contract-equivalent equals" is the correct move — block only a FABRICATED alternative/discriminator); **INV\* not coverage targets** (only C*); **premise/deferred constraint is covered** (C3 is premise — not a gap); **don't demand an ADR the aPRD doesn't force / that's local/not-yet** (the mirror failure of over-deciding); a real **aPRD defect ROUTES to Phase 0** (§5.10, D9), never blocks the faithful draft; cosmetic/phrasing latitude not a defect. Stays in lane (no re-decide/re-render/edit-draft, no re-source, no RECONCILE-override, no freeze/log/lock/Accepted, no aPRD/cut/triage re-open, no client). Guards HALT on missing adr-index / missing aprd / missing 04-conflicts-or-06-cut / unreadable draft / non-greenfield; empty adrs[] → write clean + empty issues + note (nothing rendered = nothing to attack). **Isolated test PASS (0 retries), tested BOTH directions:** clean golden → `clean` (schema clean, 6 reviewed); **planted-defect fixture (8 ADRs: 4 plants + 4 clean) → all 4 caught + correctly categorized + ZERO false positives** — strawman (ADR-0001 DOA microservices vs INV6), untraceable (ADR-0003 phantom `R88`), not-yet (ADR-0007 invoice-aggregation explicitly deferred to S3 in cut deferred[]), unforced (ADR-0008 observability gold-plating, traces A13/C2 resolve but force no fork); the 4 clean ADRs untouched, **incl. ADR-0005 default-among-equals correctly NOT flagged strawman**; counts tally (by_category sums to issue_count), full accounting (8 reviewed). **Fresh SEPARATE verifier independently re-walked all 8 ADRs → PASS** (every real defect caught, 0 missed, 0 false positives, categorization acceptable, schema clean). **Windowed-e2e PASS (0 retries)** — window RECONCILE→SYNTHESIZE-ADR→CRITIQUE (N-2→N-1→N, full Phase-2 tail; head seeded golden 03-options decisions+index+aprd+cut). RECONCILE coherent (C1/C2 literal-trace covered, C3 premise, 0 conflicts/violations/gaps) → SYNTHESIZE-ADR rendered 6 Proposed drafts + adr-index (accounting clean) → CRITIQUE consumed clean: verdict `clean`, 6 reviewed, 0 issues, lane held (no log/lock written). Interface threaded clean end-to-end, no cascade (CRITIQUE = window tail). Golden `05-critique.json` (verdict clean, empty issues) promoted to `_fixtures/greenfield-clean/.adr/`. **Judgment-variance note (benign):** the clean golden yields `clean`/empty every run (the rendered set IS sound by construction); the seven detection *mechanisms* earn their keep on the planted-defect/negative inputs (proven above), not the clean golden. The invariants are: the new adversarial checks (strawman/over-decided/unforced/not-yet) + the rendered-form backstop + the anti-FP resolution-test (degenerate/default-among-equals not strawman, INV-not-coverage-target, premise/deferred covered, never re-litigate a pick) + FLAG-never-fix lane (loops to SYNTHESIZE-ADR, never patches a draft). **PHASE 2 (ADR) COMPLETE — foundation-pass ADR pipeline end-to-end (DECISION-EXTRACT→TRIAGE→OPTION-GEN→EVALUATE-DECIDE→RECONCILE→SYNTHESIZE-ADR→CRITIQUE); freeze is mechanical, no prompt — like Phase 0.**
- **PRIOR last prompt (SYNTHESIZE-ADR):** **SYNTHESIZE-ADR** → `prompts/02-adr/SYNTHESIZE-ADR.md` (Phase 2 role 6/7, spec §5.7/§6/§8). interactive:false (internal render — no client touch, §9). In: `.adr/04-conflicts.json` (THE GATE — render only on `verdict:coherent`; `blocked` → HALT, re-decide first, render nothing) + `.adr/03-options/decisions-index.json` (enumerate decided set + deterministic id order) + each `.adr/03-options/<DP-id>.decision.json` (render CONTENT SOURCE OF TRUTH). Out: per-decision **`.adr/drafts/<NNNN>-<slug>.draft.md`** (Nygard ADR, §6.1: frontmatter {id,title,status,date,class,scope,mode,category,traces,supersedes,superseded_by} + body {Context, Decision, Alternatives considered, Consequences}) + **`.adr/adr-index.json`** (machine index — id, dp_id, title, status, mode, scope, category, traces, supersedes/superseded_by, draft_ref, degenerate_forced; + undecided_not_rendered + adr_counts). **THE rule: TRANSCRIBE, never RE-DECIDE (D1/D6)** — carry decision_made/evaluation/rejected/consequences/traces faithfully; **only original prose is the `title`**. Per-section render: **Context** = problem-not-answer, composed ONLY from the decision file's `decision` question + `forced_by` + rationale/evaluation tensions, **without naming the chosen option** (no new force/aPRD id); **Decision** = `decision_made` **verbatim as a substring** (active-voice frame + accurate clarifiers OK; option's own words unaltered — the R1 fix below); **Alternatives considered** = each `rejected[]` option (verbatim name) + faithful neutral `assessment` restatement + force-traced `why_rejected` (degenerate→honest "only one compliant option survived" + degenerate_reason; contract-indifferent→"default among compliant equals", never a fabricated discriminator); **Consequences** = positive/accepted_cost/follow_on transcribed verbatim-in-substance. **Load-bearing DESIGN CALL (deviation from authoring-note (d)/(e), documented): renders DRAFTS (`status: Proposed`, `.adr/drafts/`), NOT the immutable `.adr/log/` as Accepted.** Why: CRITIQUE (role 7) runs AFTER + loops blocking issues BACK to synthesize (§5.8) — a re-renderable record must be mutable, and rendering Accepted-into-immutable-log then editing on loop-back violates append-only/supersede-never-edit (D6); §10 tree has a separate `drafts/` dir; mirrors the **established Phase-0 precedent** (SYNTHESIZE→`drafts/aprd.v1.md`→CRITIQUE→mechanical freeze→frozen+lock). The post-CRITIQUE **mechanical freeze** (non-LLM, no prompt — like Phase 0) promotes drafts→`log/`, flips Proposed→Accepted, writes `adr.lock`. SYNTHESIZE-ADR explicitly does NOT write `log/`/`adr.lock`/Accepted. **Id assignment:** single monotonic `ADR-NNNN` (4-digit) from 0001, **order = decisions-index `decisions[]` array order** (EVALUATE-DECIDE emits it lowest-DP-index-first → deterministic for a given set); `undecided[]` get NO id/NO ADR (noted in `undecided_not_rendered`, skip). Stays in lane (no decide/re-score/re-pick, no re-detect conflicts/coverage [RECONCILE owns], no re-source/add/drop options [OPTION-GEN], no self-critique [CRITIQUE role 7], no freeze/log/lock, no client). Guards HALT on missing-04 / blocked-verdict / missing-index / unreadable-decision_ref / non-greenfield; empty decisions[] → empty adr-index + note, zero drafts. **Isolated test PASS (0 retries)** — fresh separate verifier graded A–G (enumeration/ids, frontmatter fidelity, verbatim pick, problem-framed Context w/ no invented force, live faithful alternatives incl. correct DP7 default-among-equals, transcribed consequences, clean lane) **all PASS first pass**. **Windowed-e2e PASS (1 retry, in budget)** — window EVALUATE-DECIDE→RECONCILE→SYNTHESIZE-ADR (N-2→N-1→N, full Phase-2 tail head; head seeded golden 03-options option-files+index+aprd+06). Ran with run-to-run **variance** (DP2=Python/Django picked, RECONCILE coherent w/ DP4 correctly excluded from C2.by by literal-trace, picks+rationale ≠ golden) → SYNTHESIZE-ADR consumed clean. **The 1 retry fixed a REAL prompt defect:** e2e runner **paraphrased the option name in `## Decision`** ("Adopt a single-deployment monolith with flat internal structure" instead of carrying `decision_made` = "Single-deployment monolith (flat structure)" verbatim) — lost string-auditability + risked silent drift. Isolated runner happened to comply, so the rule wasn't reliably followed; original rule too soft. R1 fix sharpened the Decision rule to **"`decision_made` verbatim as a substring, copy-by-paste, option's own words unaltered"** + explicit CORRECT/WRONG pattern example → re-run all 6 Decisions carried the option string verbatim, fix held; window threaded clean, no cascade (SYNTHESIZE-ADR = window tail). Golden drafts (6: ADR-0001..0006) + `adr-index.json` regenerated from golden decisions (golden inputs, fixed prompt), promoted to `_fixtures/greenfield-clean/.adr/{drafts/,adr-index.json}`. **Judgment-variance note (benign):** titles + section prose phrasing vary run-to-run (author's-prose title, Nygard restatement) — all faithful to the decision file; the invariants are TRANSCRIBE-not-re-decide, verbatim option string in Decision, verbatim traces/category, Proposed-drafts-not-Accepted-log, deterministic id order. Render is a transcription stage: it carries clean every run, the variance is upstream picks not render fidelity.
- **PRIOR last prompt (RECONCILE):** **RECONCILE** → `prompts/02-adr/RECONCILE.md` (Phase 2 role 5/7, spec §5.6/§8). interactive:false (internal coherence+coverage gate — no client touch, §9). **First Phase-2 stage that looks ACROSS the decided set** (EVALUATE-DECIDE decided each point independently vs the fixed contract → conflicts/gaps are visible here by design — intentional producer/consumer seam). In: `.adr/03-options/decisions-index.json` (enumerate decided set) + each `.adr/03-options/<DP-id>.decision.json` (picks + `traces` + `consequences.follow_on`) + `.aprd/aprd.frozen.md` (C* coverage target + full id-space for trace validation) + `.roadmap/06-foundation-cut.json` (INV* hard floor + `deferred[]`). Out: **`.adr/04-conflicts.json`** (§10 reserved slot). **Detects three things, FLAGS never re-decides (D9):** (1) **cross-decision conflicts** — two picks that cannot both hold; grounded via each decision's `follow_on[]` sibling-DP notes (honored vs contradicted) + pairwise paradigm clash vs INV*; emit only when a named force/INV/follow_on makes two picks **mutually exclusive** (tension≠conflict, honored-dependency≠conflict — false positives thrash the re-decide loop); (2) **constraint violations** — a pick breaching a hard C*/INV* (the floor; EVALUATE-DECIDE enforces it, RECONCILE backstops); (3) **coverage bidirectional** — ADR→aPRD (every decision `traces[]` non-empty + every id real; empty=gold-plating, phantom=broken thread) + aPRD→ADR (every in-scope **C\*** bucketed covered/deferred/premise/gap). Schema: {decisions_checked, undecided_carried, conflicts[{id,between,picks,finding,exposed_by,evidence,routes_to}], constraint_violations[{id,decision,decision_made,violates,finding,routes_to}], coverage{adr_to_aprd{all_traceable,untraceable[]}, aprd_to_adr{in_scope_constraints,covered[{constraint,by}],deferred[],premise[],gaps[]}}, structural_defects[], verdict(coherent|blocked), blocking_count, reconcile_counts}. blocked if ≥1 conflict/violation/gap/untraceable/structural-defect → routes back (conflicts/violations/gaps→EVALUATE-DECIDE re-decide; aPRD-defect-rooted gap→Phase 0); coherent → hands set to SYNTHESIZE-ADR. **Load-bearing design calls:** (a) **coverage target = aPRD CONSTRAINTS (C\*) ONLY** — the literal D5/§5.6 target; **INV\* are the violation-check hard floor, NOT coverage targets** (the R1 fix — first run flagged INV2 as a "gap" because no decision traced it, but INV2/3/4/5/7/8 are slice-level invariants no foundational decision is *about* — Phase 3 honors them, a foundational decision tracing them would be padding; requiring INV* coverage manufactures false-positive gaps; narrowed coverage to C* + kept INV* fully checked for violations); (b) **`covered.by` = literal string membership in `traces[]`, never topical relevance** (the R2 fix — first run listed DP4/DP7 under C1.covered though neither traces C1; "feels related" ≠ traces it; a false traceability claim); (c) C3 = `premise` (net-new greenfield class fact, forces no HOW); (d) FLAG-never-re-decide is THE lane line — diagnosis not fix, never hand-patch a decision. Stays in lane (no re-decide/re-pick/edit, no ADR render/id [SYNTHESIZE-ADR role 6], no re-source [OPTION-GEN], no strawman/over-decided audit [CRITIQUE role 7], no aPRD/cut/triage re-open [D9], no client §9). Guards HALT on missing-index/aprd/cut + non-greenfield; empty decisions[] → empty 04 + coherent + note. **Isolated test PASS (2 retries, in budget) — each retry killed a distinct coverage-scope defect, converging:** R1 INV-coverage false positive (INV2 flagged gap) → narrowed coverage to C* only, INV* = violation floor not coverage target; R2 `covered.by` topical mis-attribution (DP4/DP7 listed under C1 without tracing it) → literal-trace string-membership rule. Retry-2 fresh separate verifier graded A–H: A-E/G/H PASS first pass, F (coverage) PASS after the literal-trace fix (independently re-walked all follow_on notes [0 real conflicts], all picks vs C*/INV* [0 violations], all 6 traces real, C1/C2 covered by literal-trace decisions, C3 premise, gaps[] correct). **Windowed-e2e PASS** — window OPTION-GEN→EVALUATE-DECIDE→RECONCILE (N-2→N-1→N, full Phase-2 tail head; head seeded golden 01+02+aprd+cut). Ran with **run-to-run variance** (DP1=traditional monolith, DP2=Python/Django — different picks + richer traces than golden) → RECONCILE consumed clean: verdict coherent, C1.by=[DP1,DP2,DP6,DP10] literal-trace-correct (DP4/DP7 correctly excluded — the R2 fix held on fresh variance data), C2 all 6, C3 premise, 0 conflicts/violations/gaps. Interface threaded clean, no cascade (RECONCILE = window tail). Golden `04-conflicts.json` regenerated from golden decisions, promoted to `_fixtures/greenfield-clean/.adr/`. **Judgment-variance note (benign):** the clean golden produces an all-empty conflict/violation/gap result every run (the decided set IS coherent + C-covered by construction) — the conflict/violation/gap *mechanisms* earn their keep on negative/variance inputs, not the clean golden; the invariants are the literal-trace `by` discipline + INV-not-a-coverage-target scoping + FLAG-never-re-decide lane, which hold every run.
- **PRIOR last prompt (EVALUATE-DECIDE):** **EVALUATE-DECIDE** → `prompts/02-adr/EVALUATE-DECIDE.md` (Phase 2 role 4/7, spec §5.5/§8). interactive:false (internal scoring+pick — no client touch; client-visible blast radius is the downstream GATE's concern, §9). In: `.adr/03-options/index.json` (manifest = enumeration entry) + each `.adr/03-options/<DP-id>.json` (OPTION-GEN's live unranked option sets + `bears_on` eval hooks) + `.aprd/aprd.frozen.md` (CONSTRAINTS/ACCEPTANCE/assumptions-as-NFRs = scoring dimensions) + `.roadmap/06-foundation-cut.json` (INV* = hard floor). Out: **per-DP decision file** `.adr/03-options/<DP-id>.decision.json` + **manifest** `.adr/03-options/decisions-index.json` (**decided: per-DP files + index, mirrors OPTION-GEN**; §10 literally calls `03-options/` the home for "per-decision option + **evaluation** working files"; `04` reserved for RECONCILE so EVALUATE-DECIDE output ≠ `04`; one enumeration entry for RECONCILE/SYNTHESIZE-ADR + clean accounting). Per-DP file: {id/decision/category/forced_by/cut_ref VERBATIM from option file, options_ref, grounding_source, **evaluation[{option, assessment(neutral, before-pick), decisive_factors[]}]** (one per option, len==option_count), decision_made(verbatim option name), rationale(forces+weighting), rejected[{option, why_rejected(traced to a force)}], consequences{positive[],accepted_cost[],follow_on[]}, traces[], degenerate_forced, degenerate_reason, lane_note}. Manifest: {decisions[{id,category,decision_made,options_ref,decision_ref,option_count,degenerate_forced,traces}], undecided[{id,reason}], decision_counts}. **Scores option sets vs CONSTRAINTS/AC/NFR + INV hard floor, PICKS one, records consequences.** Load-bearing: **THE rule is D1/D3 live-alternatives** — `evaluation[]` assesses EVERY option neutrally as an option-property BEFORE the pick (would read the same whichever won), then `decision_made`/`rationale`/`rejected` reference it — structure forces alternatives to read live, not reverse-engineered; **closes the fork** (picks — new vs upstream's RM11 framing-only) but **stays in lane**: no ADR render/id (SYNTHESIZE-ADR role 6), no cross-decision conflict/coverage (RECONCILE role 5 — decides each point against the FIXED aPRD/cut forces INDEPENDENTLY → order-independent, conflicts stay visible; cross-decision deps NOTED in follow_on, never resolved), no re-source/add/drop options (OPTION-GEN owns set — picks FROM given), no re-open aPRD/cut/triage (D9 — unresolvable→undecided[]+route to Phase 0), no client (§9); **scoring grounded** in option's `bears_on` + decision's `forced_by` + C*/AC*/A*/INV* — no invented criteria; **weighting explicit** (C2 timeline + A13 scale dominate at this size; marginal benefit at a scale A13 rules out is not decisive); rejections **traced to a force** (no bare "worse"); **traces = forced_by ∪ ids cited by name in prose** (operational anti-padding check; don't wholesale-copy bears_on); **degenerate option set → forced pick** (degenerate_forced:true, rejected:[], reason carried); full accounting (len(decisions)+len(undecided)==len(option_files)); robust to variable manifest (6–8 DPs, 2–4 options, 1 if degenerate). Guards HALT on missing-index/missing-aprd/missing-cut/non-greenfield; empty option_files → empty decisions-index + note, stop. **Isolated test PASS (3 retries, within budget).** The 3 retries fixed REAL prompt defects, all in **grounding discipline** (each retry killed a defect variant, converging on root — NOT identical repetition): R1 — runner imported a **dev-side market claim** ("Ruby's developer pool shrinking in 2025") to reject Rails; fix forbade recalled market/ecosystem/popularity args + clarified C2 is a delivery-window force not a labour-market proxy. R2 — **trace padding** (DP2 parked AC3/AC5 in traces uncited) + **verbatim-carry break** (DP6 expanded "SPA frontend"→"single-page application frontend"); fixes added the operational trace check (forced_by OR cited-by-name; don't wholesale-copy bears_on) + copy-by-paste/no-reword rule. R3 — defect **mutated to user-side** ("Google accounts held more broadly than GitHub" as DP7 tiebreaker — recalled real-world adoption); root fix generalized the forbidden-claim ban to user-side adoption AND added the **"contract-indifferent → default among equals"** move (when no contract force separates two compliant options [AC5 names Google/GitHub as equals], say so honestly, never fabricate a tiebreaker — decision-layer analogue of OPTION-GEN's degenerate-honestly). Retry-3 fresh separate verifier **7/7 PASS** (live-alternatives, force-traced rejections, no-invented-criteria, compliant-verbatim-pick, real-consequences, in-lane, grounded-traces) + mechanical (verbatim-carry/eval-count/pick-in-set/accounting/traces-grounded all clean). **Windowed-e2e PASS** — window TRIAGE→OPTION-GEN→EVALUATE-DECIDE (N-2→N-1→N, full Phase-2 tail; head seeded golden 01+06+aprd). TRIAGE emitted a **7-item** queue (variance: DP5 boundary-strategy routed in-cut+cut_gap — the designed catch) → OPTION-GEN grounded 7 → EVALUATE-DECIDE decided 7 incl. the NEW DP5 (absent from golden). Interface threaded clean, fresh verifier 7/7 PASS on the variance set, no cascade (EVALUATE-DECIDE = window tail). Golden decisions (6 files: DP1=monolith, DP2=Python/Django, DP4=PostgreSQL, DP6=MPA/SSR, DP7=Google-default-among-equals, DP10=PaaS; + decisions-index) regenerated from golden 6-item 03-options, promoted to `_fixtures/greenfield-clean/.adr/03-options/`. **Judgment-variance note (benign):** picks vary run-to-run (DP2 Python OR Node OR Rails; DP1 flat OR modular monolith) — all defensible against the forces; structure/accounting/lane/traces hold every run; the live-alternatives discipline is the invariant, not the specific pick.
- **PRIOR last prompt (OPTION-GEN):** **OPTION-GEN** → `prompts/02-adr/OPTION-GEN.md` (Phase 2 role 3/7, spec §5.4/§7/§8). interactive:false (internal option sourcing — no client touch, §9). In: `.adr/02-triage.json` (`resolution_queue[]` = work list) + `.adr/01-decision-points.json` (point BODIES) + `.aprd/aprd.frozen.md` (forces) + `.roadmap/06-foundation-cut.json` (INV* compliance gate) + OPTIONAL `.adr/arch-canon.json`. Out: **DIR** `.adr/03-options/` — one file per in-cut foundational DP (`03-options/<DP-id>.json`) + manifest `03-options/index.json` (§10 names a dir; **decided: per-DP files + index manifest** — honors §10's per-decision working files, gives EVALUATE-DECIDE one enumeration entry point + clean accounting). Per-DP file: {id/decision/category/forced_by/cut_ref carried VERBATIM from 01, grounding{existing_system, canon_status, source, note}, options[{option(named concrete candidate), summary, source, reason_to_consider, tradeoffs{strengths[],costs[]}, bears_on[]}], option_count, degenerate, degenerate_reason, no_pick_note}. Manifest: {resolution_queue echo, option_files[{id,path,option_count}], canon_status, degenerate[], skipped[], option_set_counts{decisions_grounded,total_options,min_options_per_decision}}. **Produces ≥2 REAL live alternatives per in-cut foundational, sourced cheapest-first** (existing system [N/A greenfield C3] → arch-canon → reasoned first-principles). Load-bearing: **lane MOVES here but only partway** — OPTION-GEN NAMES concrete options (PostgreSQL/SQLite/Google-OAuth — RM11 name-don't-decide boundary shifts: naming options IS the job) but **still does NOT decide** (no score/rank/recommend/pick — EVALUATE-DECIDE role 4 owns that); **canon graceful-degrade** — `arch-canon.json` OPTIONAL; absent (expected greenfield-first) → `source:reasoned`, `canon_status:absent`, flagged (first project pays full reasoning cost §7.1; mirrors Phase-0 missing-source pattern — NO heavy arch-canon fixture authored); **anti-strawman 3-clause real-option test** (a competent team would weigh it AND compliant-not-DOA AND distinct); **compliance gate** — option violating a hard C* or INV* is dead-on-arrival, EXCLUDE (the **load-bearing fix**: gate is about SUBSTANCE not cost-phrasing, and **the decision question bounds the option set** — a branch DECISION-EXTRACT already closed in `decision` text/`fork_evidence` [e.g. INV6 rules out multi-service for DP1] stays closed; do NOT re-introduce "for completeness"/"to confirm non-viability"); **trade-offs NEUTRAL + bidirectional** (every option both strengths+costs, no verdict); `bears_on` = eval hooks (aPRD/INV ids the trade-offs touch, NOT a score); **degenerate honestly** (only-one-compliant → degenerate:true, never pad strawman); full accounting (one file per queue id; len(option_files)+len(skipped)==len(resolution_queue); missing-body→skipped[] not fabricate); **robust to variable queue** (6–8 in-cut foundationals; e2e hit a 7-item queue incl DP5). Stays in lane (no decide/consequences/ADR/coverage/client/aPRD-reopen). Guards HALT on missing-02/missing-01/missing-frozen-aPRD/non-greenfield; empty resolution_queue → write index w/ empty option_files + note, stop. **Isolated test PASS** (1 round, golden 6-item queue, fresh separate verifier 6/6 judgment criteria PASS — no-strawman/compliance/no-pick/≥2-distinct/grounded-tradeoffs/answers-the-fork all clean, only soft tone-notes; mechanical: verbatim-carry/accounting/neutral-tradeoffs/schema all clean). **Windowed-e2e PASS (2 retries, within budget)** — window DECISION-EXTRACT→TRIAGE→OPTION-GEN (full Phase-2 chain, head seeded golden aprd+lock+cut). **The 2 retries fixed a REAL prompt defect:** first e2e run a runner included a **microservices** option on DP1 rationalized "to confirm non-viability" — a dead-on-arrival option (own costs said "banned by INV6"); the compliance gate had a loophole. R1 fix (forbid "for completeness/confirm-non-viability" + keyword test) was **gamed** (runner reworded the cost to dodge keywords, kept the DOA option); R2 fix re-anchored the gate on **substance + the decision-question-bounds-the-option-set** rule → DP1 correctly excluded multi-service, fresh verifier 5/5 PASS. Interface threaded clean across the variance-driven 7-item queue, no cascade (OPTION-GEN = window tail). **Judgment-variance note:** option-set membership + count vary run-to-run (DP4 may include/drop MongoDB, DP6 may include/drop GraphQL+SPA, DP7 single-provider vs generic-OIDC) — all defensible scope/compliance calls; schema + accounting + compliance gate hold every run; benign. Golden `03-options/` (6 files: DP1=2 [monolith/modular, multi-service excluded], DP2=3, DP4=3, DP6=3, DP7=3, DP10=3; 17 total options; degenerate []/skipped []) regenerated from golden 01+02 (6-item queue), promoted to `_fixtures/greenfield-clean/.adr/`.
- **PRIOR last prompt (TRIAGE):** **TRIAGE** → `prompts/02-adr/TRIAGE.md` (Phase 2 role 2/7, spec §5.3/§8). interactive:false (internal classifier/router, no client touch — §9). In: `.adr/01-decision-points.json`+`.roadmap/06-foundation-cut.json`. Out: `.adr/02-triage.json` {triage[per-DP: carried id/decision/category/cut_ref + candidate_blast_radius + BINDING blast_radius + blast_decision(confirmed|overridden) + blast_rationale + cut_status(in-cut|not-yet|null) + cut_gap + defer_to + route + route_rationale], resolution_queue[], slice_deferred[{id,defer_to}], deferred_queue[], conventions[{id,note}], cut_gaps[], triage_counts{total,foundational_in_cut,foundational_not_yet,local,trivial}}. **Two-axis triage**: Axis1 BINDING blast_radius foundational|local|**trivial** (only TRIAGE may assign trivial — downgrade from EXTRACT's proposal; EXTRACT forbidden from trivial); Axis2 (foundational only) cut split **in-cut|not-yet** vs the cut's FD set. **Route = pure fn of two axes**: found+in-cut→resolution_queue(→OPTION-GEN); found+not-yet→slice_deferred(defer_to slice); local→deferred_queue(→Phase 3); trivial→convention. Load-bearing: **cut's FD list = authoritative in-cut set** (FD.needed_by includes skeleton_id → in-cut); **skeleton-need test** resolves unnamed foundational forks (cut_ref null/deferred:Sx → if skeleton/INV breaks without it → in-cut + **cut_gap:true** [Phase-1 thin-cut feedback signal, mirrors mode:slice]; else not-yet → defer_to earliest needing slice); **convention = announce-it's-convention, never name the value** (RM11 stay-in-lane); **carry id/decision/category/cut_ref VERBATIM** (classify, don't rewrite); full-accounting partition (4 queues partition the set; len sums == triage len == 01 len); tally-by-walking counts; **robust to variable input set 9–11 DPs** (no fixed count assumed). Stays in lane (no options/decide/coverage/ADR-write/client/aPRD-reopen). Guards HALT on missing-01 / missing-cut / non-greenfield; empty-decision_points → report+stop (note any aprd_defects already routed to Phase 0 by EXTRACT). **Isolated test PASS** (1 round, no defects — fresh separate verifier 7/7 mechanical criteria PASS: schema/partition/verbatim-carry/route-consistency/counts/blast_decision/cut_gap all clean; one soft NOTE on DP5 boundary-strategy reasoning — explicitly "not a mechanical defect"). **Windowed-e2e PASS** (clamp: TRIAGE = Phase-2 role 2, only 1 upstream phase-step → window = DECISION-EXTRACT→TRIAGE; head seeded golden aPRD+lock+cut; EXTRACT emitted a **10-point** set [variance, ≠golden's 11] with different DP id-map → TRIAGE consumed clean, full accounting held, interface threaded, no cascade). **Judgment-variance note:** boundary-strategy + API-style forks (cut_ref:null foundationals) route either **not-yet→S4** OR **in-cut+cut_gap** run-to-run — BOTH valid (walking-skeleton needs-module-cut-or-not is genuine architect judgment); prompt handles both, schema/accounting hold either way; the cut_gap mechanism is the designed catch. Golden `02-triage.json` (6 in-cut/1 not-yet→S4/4 local/0 trivial, cut_gaps []) regenerated from golden 11-point 01, promoted to `_fixtures/greenfield-clean/.adr/`.
- **PRIOR last prompt (DECISION-EXTRACT):** **DECISION-EXTRACT** → `prompts/02-adr/DECISION-EXTRACT.md` (Phase 2 role 1/7, head of ADR pipeline, spec §5.2/§8). interactive:false (decisions internal — client signed WHAT/ordered slices; §9). In: `.aprd/aprd.frozen.md`+`.aprd/aprd.lock`+`.roadmap/06-foundation-cut.json`. Out: `.adr/01-decision-points.json` {decision_points[DP*]{decision, category, forced_by[], candidate_blast_radius, blast_rationale, fork_evidence, cut_ref}, checklist_coverage[10 categories], aprd_defects[], decision_point_counts}. **Walks fixed §4 decision-category checklist (recognition>recall, P7); emits every genuine FORK** (discriminator = forced by ≥1 aPRD elem AND live ≥2-ways AND structural blast radius). Load-bearing: **names the OPEN QUESTION, never the answer (RM11 — no vendor/stack/schema/endpoint)**; **foundation cut SEEDS not bounds** — expand coarse FD categories into distinct points (FD1 style+stack → 2 DPs; FD2 OAuth → provider[foundational]+mechanism[local]), adversarially hunt forks the cut missed (cut_ref:null = signal cut was thin), treat INV* as FIXED forces (cite, never re-open); **one decision per point** (split bundles — style/stack, provider/mechanism); **forced_by traces verbatim, no padding** (timeline/scale constraints rarely CREATE a HOW-fork — deployment-topology legit turns on timeline, config-injection does not); **candidate_blast_radius = PROPOSAL** (foundational=pre-draw/determines-what-components-ARE-or-how-modules-cut; local=inside-one-box) TRIAGE owns final call, never `trivial` (below the bar); collapsed-to-one-option = recorded closed in checklist_coverage NOT emitted (sync/async closed by A13/INV6; Conformance N/A greenfield); full checklist accounting (every category fired/closed-by-ref/not-forced/N-A). Stays in lane (no triage-final/options/decisions/ADR-write/client-touch). Guards: no frozen aPRD / lock!=frozen / missing cut / non-greenfield → HALT. **Isolated test PASS** (4 hardening rounds — see changelog; fresh-verifier final mechanical pass: 11 DPs, counts 7F/4L tally-verified, all forced_by verbatim, checklist 10/10 ordered, emission deterministic, cut_ref bare-null). **Windowed-e2e PASS** (head-clamp: DECISION-EXTRACT = Phase-2 first authored step, no upstream Phase-2 producer → window = this single step seeded from golden 06+aprd; identical to isolated). Golden `.adr/01-decision-points.json` promoted to `_fixtures/greenfield-clean/.adr/`.
- **PRIOR last prompt (SEQUENCE-REVIEW):** `prompts/01-roadmap/SEQUENCE-REVIEW.md` (Phase 1 role 7/7, **THE one Phase-1 client gate** [§9, §5.8, RM5]; **interactive:true** — the deliberate PR1 exception). **Design = two-phase single session** (resolves the "how to clean-room-test an interactive prompt" problem): **Phase A (autonomous)** reads `05-sequence.json`, renders client-facing recognition-over-recall presentation to `.roadmap/roadmap.md` (order + what the first demo = skeleton shows + plain per-position rationale + multiple-choice reorder offer), presents in chat, **then PAUSES** for the client's one selection — never fabricates an answer (clean-room run w/ no client stops here, roadmap.md = complete deliverable). **Phase B (after client reply)** validates the selection against the HARD dependency constraint, applies confirm/legal-reorder, writes `.roadmap/07-sequence-reviewed.json` (D7 spine numbering — review is §5.8, after foundation-cut §5.7). Load-bearing: **legal reorder options computed deterministically from `depends_on`** (slack rule: X electable iff `position(X)−1 > max(position of its depends_on)`; skeleton pinned pos1 RM4, never offered; fully-forced order → confirm+escape only, stated honestly — never fake choices); **dependency legality overrides the client too (RM5)** — illegal/skeleton-move request → `blocked_overrides[]` + `needs_followup:true`, legal order held, never silently applied; **value carried verbatim, never re-scored** (client owns value, surfaced not invented, §7); coverage preserved (permute not change set); stays in lane (order only — no foundation-cut/decisions/HOW shown to client, §9). `client_response` enum = confirmed|reordered|deferred|**blocked**. In: `.roadmap/05-sequence.json` (verdict must be `sequenced`). Out: `roadmap.md` (Phase A, always) + `07-sequence-reviewed.json` (Phase B). Isolated test PASS — 3 paths: Phase-A clean-room present (no client, paused correctly, roadmap.md on disk), Phase-B confirm ("A"→verdict confirmed, order verbatim), Phase-B blocked (client wants S3/PDF first→blocked, S3 depends S1/S2/S4, order held, needs_followup true) — fresh separate verifier 10/10 on confirm path. Windowed-e2e PASS: **SKELETON-IDENTIFY→SEQUENCE→SEQUENCE-REVIEW** (true data-chain window — SEQUENCE-REVIEW reads 05 not 06, so FOUNDATION-CUT [positional N-1] is NOT its producer; rooted window at the real upstream, head seeded from golden 02/03/aprd), interface threaded clean (verbatim carry, topo-legal, coverage complete, skeleton pinned, client confirm captured), no cascade. Golden 07 + roadmap.md regenerated from golden 05, promoted; golden roadmap chain now 02→03→04→05→06→07.
- **PRIOR last prompt (FOUNDATION-CUT):** `prompts/01-roadmap/FOUNDATION-CUT.md` (Phase 1 role 5/7; names the MINIMUM to decide/build once before slicing [§5.7, RM9] — three cut parts: `foundational_decisions[]` [decision CATEGORIES → Phase 2, named never made], `skeleton_seams[]` [carried/refined from 04, named-not-designed → Phase 3], `cross_slice_invariants[]` [aPRD-fixed properties read from CONSTRAINTS/ASSUMPTIONS, never invented]; plus `deferred[]` = the thinness evidence routing every post-skeleton slice's HOW-decisions to the slice that owns them). Load-bearing: anti-waterfall lever — bias thin, under-cut not over-cut, when in doubt defer; skeleton drives the cut; RM11 name-don't-decide boundary on FD categories + seams; P11 invariants read-not-invented. interactive:false (client gate = SEQUENCE-REVIEW). In (D7): `.roadmap/04-skeleton.json` + `.roadmap/05-sequence.json` + `.aprd/aprd.frozen.md`. Out: `.roadmap/06-foundation-cut.json` (D7 spine numbering). Isolated test PASS (fresh separate verifier, all 7 criteria) + full-3-step windowed-e2e PASS (SKELETON-IDENTIFY→SEQUENCE→FOUNDATION-CUT, interface threaded clean, no cascade). Golden 06 regenerated from golden 04+05+aprd, promoted; golden roadmap chain now 02→03→04→05→06.
- **Next action:** **Phase 3 (HLD) — DERIVE-COMPONENTS + DEFINE-CONTRACTS + RESOLVE-LOCAL + MODEL-DATA shipped (4/8).** **Next: author MAP-NFR** (`prompts/03-hld/MAP-NFR.md`, Phase 3 role 5/8, spec §5.6/§8, prompt-library "MAP-NFR"). **Per CONSTRAINT/NFR, assign a concrete structural MECHANISM + the realizing component(s); any NFR with no mechanism = flagged unmet (H5).** **In (likely):** `.aprd/aprd.frozen.md` (CONSTRAINTS C* + the assumptions-as-NFRs — esp. A13 scale, C2 timeline; the NFR set to mechanize) + `.adr/adr.lock`+`.adr/log/` (frame that already DECIDED some mechanisms — e.g. ADR-0006 PaaS deployment, ADR-0001 flat-monolith; reference, don't re-decide) + `.hld/skeleton/components.json` (the components that realize mechanisms) + `.roadmap/06-foundation-cut.json` (INV* = the cross-slice properties; INV6 single-server-synchronous esp. relevant — it FORBIDS most scale mechanisms [no cache/queue/replica], so the expected greenfield result is "few/zero added mechanisms, most NFRs satisfied by the frame already" — a near-empty mechanism set is CORRECT, not a gap, mirroring RESOLVE-LOCAL's graceful-near-empty). **Out (likely):** `.hld/skeleton/nfr-mechanisms.json` (§10 names it) {mechanisms[M*]{nfr_ref, mechanism, realized_by[C*], …}, coverage (every in-scope CONSTRAINT/NFR bucketed mechanized/satisfied-by-frame/deferred), unmet[], accounting}. **Load-bearing lane lines to resolve when authoring:** (a) **the INV6 tension** — most classic NFR mechanisms (cache, queue, read-replica, partition, horizontal scale) are FORBIDDEN by INV6/A13 (single-server synchronous, small personal-tool scale); MAP-NFR must NOT invent a mechanism the frame rules out (anti-gold-plating — the e2e/isolated lesson from DEFINE-CONTRACTS' async discriminator); a CONSTRAINT already satisfied by a frozen ADR's decision is `satisfied-by-frame` (reference the ADR), not a new M*. (b) **NFR with no mechanism + not-frame-satisfied = flagged unmet (H5)**, surfaced for the role-8 audit, never silently dropped. (c) **named-not-designed** — name the mechanism + realizing component, don't design its internals (same discipline as the prior 4 roles). **Then:** MODEL-FLOWS→DERIVE-TESTS→RECONCILE/CRITIQUE (role 8) (8 roles total — longest pipeline). **Open scope decision for the WHOLE phase (resolve as the chain matures):** every Phase-3 role has a SKELETON mode (drawn once) + an INCREMENT mode (per slice, extends the frozen skeleton — MODEL-FLOWS is its centerpiece). DERIVE-COMPONENTS authored SKELETON-only (increment needs a frozen skeleton to extend). **Decide whether to author the full skeleton chain first (then circle back for increment modes), or author each role's both-modes together** — likely the former (mirrors "skeleton drawn once before increments"; a frozen-skeleton fixture must exist before any increment role is testable). **After the Phase-3 skeleton component DAG schema is locked + stable: circle back to RE-RANK** (Phase-1 role 6/7, the only deferred Phase-1 prompt — D8; reads `07-sequence-reviewed.json` as base + the Phase-3 component DAG; the DAG now EXISTS as `.hld/skeleton/components.json` edges — RE-RANK is unblockable once we judge the schema stable).
- **(Phase 1 leftover) RE-RANK (role 6/7) DEFERRED** — it consumes the real component DAG from the Phase-3 HLD skeleton (forward dep, schema not locked); authoring now risks schema churn. **Next: start Phase 2 (ADR) — author DECISION-EXTRACT** (`prompts/02-adr/DECISION-EXTRACT.md`, Phase 2 role 1/7, spec `_initial_design/02-automated-adr-pipeline-spec.md`). Then circle BACK to RE-RANK once Phase 3's skeleton-HLD output schema (the real component DAG) is locked — RE-RANK is the only Phase-1 prompt with a forward dependency, so it is authored out of phase order on purpose. **When authoring RE-RANK later:** in = completed-slice records + their learnings + the real dependency DAG (Phase 3); reads the confirmed `07-sequence-reviewed.json` order as its base (NOT 05 — 07 is the client-confirmed living baseline); preserve dependency legality, note any slice whose value/risk changed + why, do not thrash (re-order only on material new info — §5.11, RM6). Upstream golden for RE-RANK: `07-sequence-reviewed.json` (+ a Phase-3 DAG fixture to author then).
- **Last updated:** 2026-06-07 (MODEL-DATA shipped — **Phase 3 now 4/8**; golden `data-model.json` promoted [7 entities single-owner E1→C2/E2,E5,E6,E7→C3/E3→C4/E4→C5, E4 derived→S3, no invented schema]; 2 prompt fixes hardened the DERIVED-entity edge cases [reciprocity persisted-only + derived one-directional + derived defers to producing slice]; next: MAP-NFR)

---

## Mission

Turn the 5 design specs in `_initial_design/` into **executable AI prompts**. Operator pastes each into a **fresh harness session** to manually simulate the agentic system before it is built. Output of one prompt = input to the next (artifacts chain, IDs thread).

Building IS NOT running the pipeline. I am authoring the prompt set, one prompt at a time.

## Source specs (read-only input)

| Phase | File | Produces | Key prompts |
|---|---|---|---|
| 0 aPRD | `_initial_design/00-automated-aprd-pipeline-spec.md` | frozen aPRD set (WHAT) | CLASSIFIER, EXTRACT, GAP-DETECT, QUESTION-GEN, SYNTHESIZE, CRITIQUE, research(EXTRACT-RULES/RECONCILE/VERIFY). [VERIFY-OUTPUT → Phase 4 per D6; freeze = mechanical, no prompt] |
| 1 Roadmap | `_initial_design/01-automated-roadmap-pipeline-spec.md` | vertical slice sequence + foundation cut | SLICE-EXTRACT, VERTICALITY-CHECK, SKELETON-IDENTIFY, SEQUENCE, FOUNDATION-CUT, RE-RANK, SEQUENCE-REVIEW |
| 2 ADR | `_initial_design/02-automated-adr-pipeline-spec.md` | ADR log (WHY-this-HOW) | DECISION-EXTRACT, TRIAGE, OPTION-GEN, EVALUATE-DECIDE, RECONCILE, SYNTHESIZE-ADR, CRITIQUE |
| 3 HLD | `_initial_design/03-automated-hld-pipeline-spec.md` | skeleton HLD + per-slice increments | DERIVE-COMPONENTS, DEFINE-CONTRACTS, RESOLVE-LOCAL, MODEL-DATA, MAP-NFR, MODEL-FLOWS, DERIVE-TESTS, RECONCILE/CRITIQUE |
| 4 Build | `_initial_design/04-automated-build-pipeline-spec.md` | verified staging software (TERMINAL) | BUILD-PLAN, MATERIALIZE-ORACLE, IMPLEMENT, INTEGRATE, DIAGNOSE, VERIFY-OUTPUT, CRITIQUE, DEMO-GEN |

Pipeline shape: **two loops** — foundation loop (once, thin) + slice loop (×N). Phase 1 is the controller. Phase 4 terminal at accepted staging demo.

## Build order

Phases 0→1→2→3→4 in sequence. Each phase consumes prior phase's artifact format, so authoring downstream prompts needs upstream output schema locked first. Within a phase: author prompts in spine-stage order.

---

## Prompt inventory & status

Status: ☐ todo · ◐ drafting · ☑ done

### Phase 0 — aPRD
- ☑ CLASSIFIER
- ☑ EXTRACT
- ☑ GAP-DETECT
- ☑ QUESTION-GEN
- ☑ SYNTHESIZE
- ☑ CRITIQUE
- ☑ EXTRACT-RULES (research sub-pipeline §7)
- ☑ RECONCILE (research)
- ☑ VERIFY (research)
- ⊘ VERIFY-OUTPUT — **MOVED to Phase 4** (D6): build/verify gate, executes test artifacts vs built software; Phase 0 has no executable artifacts pre-build. Phase 0 terminates at mechanical freeze (no authored prompt). **Phase 0 prompt set COMPLETE.**

### Phase 1 — Roadmap
- ☑ SLICE-EXTRACT
- ☑ VERTICALITY-CHECK
- ☑ SKELETON-IDENTIFY
- ☑ SEQUENCE
- ☑ FOUNDATION-CUT
- ☐ RE-RANK — **DEFERRED (D8):** forward dep on Phase-3 real component DAG; author after Phase 3 schema locks. Reads `07-sequence-reviewed.json` as base.
- ☑ SEQUENCE-REVIEW (client-facing, interactive:true, two-phase)

### Phase 2 — ADR
- ☑ DECISION-EXTRACT
- ☑ TRIAGE
- ☑ OPTION-GEN
- ☑ EVALUATE-DECIDE
- ☑ RECONCILE
- ☑ SYNTHESIZE-ADR
- ☑ CRITIQUE

### Phase 3 — HLD
- ◐ DERIVE-COMPONENTS — **SKELETON pass done + tested + golden promoted.** Increment mode not yet authored (needs frozen-skeleton fixture; see D9).
- ◐ DEFINE-CONTRACTS — **SKELETON pass done + tested + golden promoted.** Increment mode not yet authored (the slice-specific contract deltas beyond the frozen skeleton; see D9).
- ◐ RESOLVE-LOCAL (emits local ADR) — **SKELETON pass done + tested + golden promoted.** Increment mode not yet authored (per-slice queue drain against the frozen skeleton; see D9).
- ◐ MODEL-DATA — **SKELETON pass done + tested + golden promoted.** Increment mode not yet authored (entities a slice introduces beyond the frozen skeleton; see D9).
- ☐ MAP-NFR
- ☐ MODEL-FLOWS
- ☐ DERIVE-TESTS
- ☐ RECONCILE/CRITIQUE

### Phase 4 — Build
- ☐ BUILD-PLAN
- ☐ MATERIALIZE-ORACLE (test-author role — distinct from builder)
- ☐ IMPLEMENT (builder — cannot edit oracle)
- ☐ INTEGRATE
- ☐ DIAGNOSE (self-heal vs escape)
- ☐ VERIFY-OUTPUT
- ☐ CRITIQUE (anti-cheat)
- ☐ DEMO-GEN

**Totals:** 22 / 39 fully done + DERIVE-COMPONENTS & DEFINE-CONTRACTS & RESOLVE-LOCAL & MODEL-DATA skeleton-pass shipped (◐×4, increment modes pending — D9). (VERIFY-OUTPUT counted once, in Phase 4 — D6 de-duplicated the Phase-0 listing; inventory 40→39. Phase 1: 6/7 authored — RE-RANK deferred per D8. **Phase 2: 7/7 COMPLETE.** **Phase 3: 4/8 started** — DERIVE-COMPONENTS + DEFINE-CONTRACTS + RESOLVE-LOCAL + MODEL-DATA skeleton pass.)

---

## Locked requirements (apply to EVERY authored prompt)

- **PR1 — Non-interactive by default.** Prompt runs autonomously start→finish, no back-and-forth. Agent reads inputs from disk, does work, writes outputs to disk, stops. No mid-run questions unless PR3.
- **PR2 — Producer/consumer contract.** Each prompt writes results in the exact place + format the *next* prompt reads. Output schema of step N == input schema of step N+1. Path + format declared in metadata.
- **PR3 — Interaction flagged in metadata.** If a step genuinely needs the user (interview, sign-off, redline, demo accept), the prompt's metadata header states `interactive: true` + what interaction + when. Silent prompts must be `interactive: false`.
- **PR4 — Caveman block verbatim.** Every prompt embeds the canonical caveman block (below) verbatim. Governs agent narration/reasoning; **artifact content stays clean prose/structured** (caveman is register, not data corruption).

### Canonical caveman block (paste verbatim into every prompt)

```
# Register
Think, write, reply terse like smart caveman. All technical substance stays. Only fluff dies.
- Drop: articles (a/an/the), filler (just/really/basically), pleasantries, hedging.
- Pattern: [thing] [action] [reason]. [next step]
- NOT: "Sure! I'd be happy to help you with that."
- YES: "Bug in auth middleware. Fix:"
Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.
```

### Standard prompt skeleton (every authored prompt follows)

```
---
role: <ROLE>
phase: <NN-name>
class: greenfield            # first pass; generalize later
interactive: false          # or true + describe (PR3)
inputs:  [ {path, format} ] # what it reads from disk (PR2)
outputs: [ {path, format} ] # what it writes to disk (PR2, D3)
escapes: [ {target_phase, when} ]
---
<canonical caveman block, verbatim>          # PR4
<role identity + mandate>
<task steps>
<grounding rules (cheapest-source-first; LLM verifies, not source)>
<output schema, explicit>
<write-to-disk instruction (exact path)>      # PR1/D3
<stop condition>
```

## Conventions for authored prompts (decided)

- **One role = one prompt** (D1). Role separation is load-bearing (failure isolation, every spec §8). May split a role further if justified — decide per case.
- **Greenfield class first** (D4). Author full vertical greenfield path (Phase 0→4) before generalizing to other classes via playbook overlays.
- Each prompt is self-contained for a fresh session: role identity + **required input artifacts (paths on disk)** + task + output schema + **where to write output on disk** + escape/route rules.
- **Artifacts land on disk, not just printed** (D3). Every prompt instructs its agent to WRITE its output to the spec-defined path. Manual sim runs against a real project workspace; operator hand-passes by pointing the next session at the files.
- Output JSON/YAML schema explicit so next prompt can consume it. IDs thread end-to-end: `R → AC → S → ADR → C → CT → F → commit`.
- Adversarial roles (GAP-DETECT, CRITIQUE, anti-cheat) stay hostile.
- LLM reconciles/verifies, never source of truth (P11) — bake into grounding prompts.

## Storage layout (decided, D2)

- **Authored prompts:** `agentic-systems/prompts/<NN-phase>/<ROLE>.md` (e.g. `prompts/00-aprd/CLASSIFIER.md`).
- **Sim project workspace** (where the operator's runs write artifacts): a project dir holding the spec-defined trees `.aprd/`, `.roadmap/`, `.adr/`, `.hld/`, `.build/`, `src/`. Path chosen at sim time; prompts reference paths relative to project root.

## Open decisions

- **D5 — Research branch placement (RESOLVED 2026-06-07).** Canon-grounding sub-pipeline (§7) runs **PRE-GAP**: emits `03-grounding/`, consumed by GAP-DETECT as OPTIONAL input (folds canon-resolved values into `recommended_default`, drops gaps canon closes). Why: spine order is ground→gap = P5 read-before-ask. Roles EXTRACT-RULES→RECONCILE→VERIFY; the allowlist+fetch ahead of them is **mechanical (non-LLM)**, not an authored prompt — EXTRACT-RULES consumes pre-fetched manifests on disk.

- **D6 — VERIFY-OUTPUT placement (RESOLVED 2026-06-07).** VERIFY-OUTPUT is the **Phase 4 build/verify gate**, NOT Phase 0. Why: it executes test CODE against BUILT software (§8/§4.1); Phase 0 emits the frozen aPRD (WHAT) — AC are statements, no executable artifacts pre-build. Tests get authored (MATERIALIZE-ORACLE) + run (VERIFY-OUTPUT) in Phase 4. Phase 0 terminates at **freeze** (§5.7) = mechanical render (frozen.md + lock), non-LLM → not an authored prompt. **Consequence:** Phase 0 prompt set = CLASSIFIER→EXTRACT→GAP-DETECT→QUESTION-GEN→[gate]→SYNTHESIZE→CRITIQUE + research branch EXTRACT-RULES→RECONCILE→VERIFY. Author VERIFY-OUTPUT in Phase 4 once oracle/build schemas lock.

- **D7 — Phase-1 file numbering past 03 (RESOLVED 2026-06-07).** **Number by spine order, not §10's pinned slots:** SKELETON-IDENTIFY=`04-skeleton.json`, SEQUENCE=`05-sequence.json`, FOUNDATION-CUT=`06-foundation-cut.json` (shifted from §10's `04`). Why: §10's tree is illustrative not binding (precedent: CRITIQUE took `08-critique.json`; research branch invented `03-grounding/`); PR2 cares about spine ORDER + a stable declared path, and no authored prompt yet consumes the literal `04-foundation-cut.json`. **Consequence:** FOUNDATION-CUT reads `04-skeleton.json`+`05-sequence.json`, writes `06-foundation-cut.json`.

- **D8 — RE-RANK vs SEQUENCE-REVIEW ordering + interactive-prompt design (RESOLVED 2026-06-07).** **Author SEQUENCE-REVIEW now, defer RE-RANK** (option b). Why: RE-RANK has a forward dep on the Phase-3 real component DAG (schema not locked) — authoring against a stub risks churn; SEQUENCE-REVIEW reads `05-sequence.json` (no forward dep) and finishes the Phase-1 client surface. RE-RANK authored out of phase order, after Phase 3 locks, reading the confirmed `07-sequence-reviewed.json` as its base. **Interactive-prompt design pattern (reusable for every later client gate — Phase 4 demo, etc.):** an `interactive:true` prompt is **two-phase in one session** — Phase A autonomous (read input, render the client-facing artifact to disk, present, PAUSE, never fabricate the human's reply) + Phase B (on the client's reply, validate/apply, write the confirmed artifact). Phase A ALWAYS lands a disk deliverable so the clean-room runner (no human) has something to verify; the confirmed artifact is Phase B only. Clean-room test = Phase-A run (no reply) verifies the presentation; Phase-B paths tested by spawning the runner with a simulated `[CLIENT REPLY] …` appended (the real session's second turn collapsed into the paste). **Window note:** SEQUENCE-REVIEW reads 05, so its true producer is SEQUENCE — the positional N-1 (FOUNDATION-CUT) is a sibling consumer of 05, NOT a producer for it; root the e2e window at the real data-chain (SKELETON-IDENTIFY→SEQUENCE→SEQUENCE-REVIEW), not the literal step-index neighbors.

- **D9 — Phase-3 skeleton/increment mode split + ADR-baseline input form (RESOLVED 2026-06-07).** Two coupled calls made when authoring DERIVE-COMPONENTS (Phase-3 head). **(1) Mode split:** every Phase-3 role is one role with TWO modes — SKELETON (drawn once: full graph/contracts/data-model/NFR/cross-cutting/build-DAG) + INCREMENT (per slice: one flow F*, extends the FROZEN skeleton, adds only new-capability boxes/contracts — MODEL-FLOWS §5.7 is its centerpiece). **Author the SKELETON pass for every role first, defer INCREMENT modes** — rationale: the increment mode extends a *frozen* skeleton that must EXIST first (a `hld.skeleton.lock` fixture), mirrors "skeleton drawn once before increments" (RM3/H13/H14), and lets the skeleton chain be built+tested end-to-end before the per-slice loop is authored. DERIVE-COMPONENTS guards HALT if a frozen skeleton already exists (= the increment trigger, not authored). Reopen to author increment modes once the skeleton chain freezes + a frozen-skeleton fixture exists. **(2) ADR-baseline input form:** Phase 3 reads the **baselined frozen `.adr/log/` (Accepted) + `adr.lock`** (the spec §2 input is the baselined log), NOT the Proposed `.adr/drafts/` + `adr-index.json` (those are Phase-2 pre-freeze internal). The mechanical freeze (non-LLM, no prompt — promote drafts→log, Proposed→Accepted, write adr.lock w/ baselined manifest) was run BY HAND on the golden Phase-2 drafts to seed `_fixtures/greenfield-clean/.adr/log/` + `adr.lock`; the pre-freeze `drafts/` + `adr-index.json` (Proposed) are kept untouched as the Phase-2 goldens (no conflict — Phase-2 steps seed drafts+index, Phase-3 steps seed log+lock; different subsets). **(3) §5.1 Load+verify-frame** is folded into the head prompt (guard + components.json header), NO separate `00-inputs.json` artifact (D7 precedent — §10 tree illustrative, don't mint an artifact no authored consumer reads).

_D1–D9 resolved. Reopen here if new forks appear._

---

## Changelog

> **Per-prompt design calls live in the prompt `.md` files** — this log keeps milestones, load-bearing rules, golden state, ship status. Full per-run test narratives (ID lists, round-by-round defect logs, FP adjudications) were summarized 2026-06-07 to cut tokens.
>
> **Recurring test protocol** (applies to every shipped prompt unless noted): isolated test (Sonnet/high) on golden fixture w/ **fresh separate verifier, no self-grade** → fix the PROMPT never the artifact → **windowed e2e** (head-clamp: window head = phase-first authored step; mechanical/non-LLM upstream excluded; fresh session per step reads PRIOR on-disk output, no re-seed) → golden output promoted to `_fixtures/`. Adversarial prompts tested BOTH directions (positive=clean + negative=planted-defects, graded vs ground truth). "PR2 held / no defect / no cascade / window passes" = the standard pass. e2e output carrying benign LLM variance is NOT promoted; goldens are regenerated from golden inputs.

### 2026-06-06 — setup + Phase 0 intake spine
- Studied 5 specs, created tracker. Inventory 40 prompts / 5 phases. Resolved D1–D4 (see Conventions); locked PR1–PR4 + caveman block + prompt skeleton. Fixtures live in `_fixtures/{greenfield-clean,compound-mixed}/`; `_test_bench/` is scratch.
- **CLASSIFIER** → `01-classification.json` (SR* ids, overall_confidence=min(subreq), threshold 0.80, escape non-greenfield). interactive:true conditional (low-confidence/compound/non-greenfield). Hardened: compound test = multi-class OR multi-system; N features of ONE new system = atomic.
- **EXTRACT** → `02-extraction.json` {E*, R*(explicit+implied, contiguous), C*, U*}. Implied = NECESSARY consequence only, merely-plausible→unknown; every item cites source+sr_ref (transcriber not author). HALT on needs_confirmation or non-greenfield.
- **GAP-DETECT** → `04-gaps.json` (ranked gaps{G*, refs, ≥2 interpretations, recommended_default verbatim∈interpretations, blast_radius, disposition ask/assume} + dismissed_unknowns + counts). Adversarial; every U* accounted (P9). Load-bearing: **blast tier** = arch (data-model SHAPE/stack/platform/external-dep/impl-mechanism) vs scope vs cosmetic, sort arch→scope→cosmetic, disposition deterministic from tier; hunt impl-forks; one gap=one topic; don't re-litigate chosen client words.
- **QUESTION-GEN** → `05-questions.md` (client-facing). Filter disposition:ask only (cosmetic skipped); **hard ≤6 cap**, mechanical select = first min(6,N) of blast-sorted 04; over-budget→deferred assumption (no silent drop). Load-bearing: **options stay in interpretations[] order, index 0→A/1→B/2→C; recommended marker moves IN PLACE, NEVER hoisted to A**; escape "Something else" last, never recommended.
- **SYNTHESIZE** → `drafts/aprd.v1.md` + `07-assumptions.json`. In: 02+04+05+06 (02 needed for E*/R*/C* id-space). 4 resolution paths: answered-letter→interpretations[index] / skipped→recommended_default / escape→client free-text verbatim / deferred→recommended_default. One A* per gap, full coverage. Load-bearing: carry E*/R*/C* verbatim, author A*/AC*; **CONSTRAINTS = exactly 02's stated_constraints, gap-fills are ASSUMPTIONS never CONSTRAINTS (no double-record)**; OUT_OF_SCOPE=declined interpretations (flips on override); ACs binary/one-outcome/concrete-observable, no fabricated edge cases; greenfield has NO class-extension block.
- **CRITIQUE** → `08-critique.json` {verdict clean|blocked, issues{I*, category, target_ref, fix_hint}}. 5 blocking categories: ambiguous-requirement, non-binary-ac, unbounded-scope, untraceable-assumption, broken-id-thread (`category` not severity — emit blockers only). Load-bearing: **resolution test** — read WHOLE contract before blocking; a fork closed by assumption+AC+OUT_OF_SCOPE is NOT a defect; cosmetic latitude within a passing AC is not a defect; anti-false-positive discipline (don't re-litigate logged decisions / demand ACs for unraised edges / OOS for unraised scope).
- **Main intake spine COMPLETE + windowed-e2e.** Golden chain 00→01→02→04→05→06→07+draft→08. (`06-answers.md` is a hand-authored client-gate fixture exercising all 4 resolution paths.)

### 2026-06-07 — Phase 0 research branch (§7) + Phase 1 start
- Resolved **D5** (research branch PRE-GAP), **D6** (VERIFY-OUTPUT→Phase 4), **D7** (Phase-1 numbering by spine order). See Open decisions. **Phase 0 prompt set COMPLETE** (9 prompts); ends at mechanical freeze (no prompt); inventory de-dup'd 40→39.
- **EXTRACT-RULES** → `03-grounding/rules-extracted.json` (flat rules{RULE*, source_ref→SRC*, tier, tool, tool_version_pinned, kind, topic, setting verbatim|null, evidence verbatim} + unfetched_sources + meta). Load-bearing: **transcribe-never-recall** (every rule needs verbatim evidence; famous-but-absent = absent; no training padding); NO reconcile/dedupe/currency-check (downstream); atomic (1 directive=1 rule); **extract NORMATIVE only — skip toolchain plumbing** (env/parserOptions/parser/plugins/extends, tsconfig path/output); graceful degradation (missing file→unfetched_sources). Golden canon fixture `_fixtures/greenfield-canon/` authored (TS/React/Node; planted 2 conflicts + 1 agreement + 1 missing source + plumbing/compound/non-normative traps).
- **RECONCILE** → `03-grounding/rules-reconciled.json` (agreed[AGR*] + conflicts[CONF* w/ positions[]] + carried unfetched + meta). Load-bearing: **topic is grouping HINT not decision key** — per-pairing relationship MERGE (same prescription) / CONFLICT (mutually exclusive) / SEPARATE (compatible-distinct; test: can one piece of code satisfy both?); a topic may yield a mix. Conflict by SEMANTICS not text; single-source=uncontested→agree; NO currency-check/NO invent (carry verbatim = VERIFY feed); recommend default per conflict by **TIER precedence** (tier1 config>tier2 prose; same-tier→null = genuine client decision, both positions stay); full accounting; **ordering by each entry's own lowest contributing RULE* index** (not topic-appearance).
- **VERIFY** → `03-grounding/rules-verified.json` = FINAL canon = RECONCILE structure carried VERBATIM + a `verification` block per agreed entry & per conflict position. Block = {status (current|deprecated|renamed|superseded|unknown-flag|not-version-bound|unverifiable), checked|null, finding, replacement, source_of_truth, confidence}. Load-bearing: **annotate-only, never re-reconcile** (same ids/order/positions/recommended_position); LLM NOT source of truth — flag never delete, `unverifiable`+low-confidence when unsure, never fabricate a verdict; null-setting prose→not-version-bound (no invented flag); **replacement is a suggestion, NEVER overwrite setting/rule** (append-only); currency does NOT change recommended_position; **sibling rules of one tool+version deprecate as a category** (consistency heuristic); re-count status_counts PER-STATUS (a swap that still sums right is the classic miscount).
- **Research sub-pipeline §7 COMPLETE + windowed-e2e.** Golden canon chain sources→manifests→extracted→reconciled→verified.
- Phase-1 head fixture authored: `aprd.frozen.md` + `aprd.lock` (mechanical freeze of golden draft; R1–R10/AC1–AC10/E1–E7/C1–C3).
- **SLICE-EXTRACT** (role 1/7) → `.roadmap/02-slices.json` (slices{S*, requirements, acceptance, value, value_basis, retires_risk, depends_on} + unsliceable + coverage + counts). Load-bearing: **verticality test** (slice vertical iff ≥1 AC black-box/user-observable, reuse aPRD AC* as oracle); **cluster by capability not layer**; full R*+AC* coverage no orphans (un-placeable→unsliceable+escape); depends_on coarse+acyclic (provisional, pending HLD re-rank); value proposed/client-owned (no fabricated model); retires_risk concrete-or-null; **controller-not-builder** — regroup existing IDs verbatim, never mint, never decide HOW; emission by lowest R* index. Guards: no frozen aPRD / non-frozen lock / non-greenfield → HALT (lock check = present+names-artifact, not hash recompute).
- **VERTICALITY-CHECK** (role 2/7) → `.roadmap/03-verticality.json` (valid[]/rejected[]{category}, verdict all_vertical|horizontal_found, counts). Adversarial reject-horizontal gate. Two inputs: 02 (AC ids) + frozen aPRD (**AC TEXT oracle — judge on AC text, not slice name**). Load-bearing: existential test (≥1 qualifying AC), record qualifying_acceptance as proof; 3 reject categories (horizontal_cut / no_acceptance / unresolved_acceptance=AC id not in aPRD); anti-FP (persistence ACs visible through surface ARE vertical; mildly-technical surface still black-box; **stay in lane = verticality only**); full accounting; no invent. Tested both directions.
- **SKELETON-IDENTIFY** (role 3/7) → `.roadmap/04-skeleton.json` (D7). Three inputs (JOIN): 03 valid[]=ELIGIBILITY + 02 bodies + frozen aPRD (seam oracle). **4 foundational seams** ingress/domain/persistence/primary-external-integration (last CONDITIONAL: absent→present:false, never invented). Skeleton test = ordered discriminator: crosses-every-present-seam-once → first-to-exercise-riskiest → dep-root (min depends_on) → thinnest. Load-bearing **RM11 boundary**: name seams, never DESIGN — NO components/stack/libs/db-engine/schema/endpoint/contract/**vendor** in ANY seam text field (even aPRD-cited "Google/GitHub" → functional TYPE only; vendor names may survive only in verbatim-carried `retires_risk`). No-fit→skeleton:null+uncovered_seams+escape; full accounting; carry IDs verbatim; stay in lane.
- **SEQUENCE** (role 4/7) → `.roadmap/05-sequence.json` (D7) {verdict sequenced|dependency_defect, sequence[]{position,id,skeleton,value,retires_risk,depends_on,cost_proxy,rationale}, ordering_basis, dependency_check, coverage, counts}. Three inputs: 04+02+03. **Skeleton pinned position 1** regardless of score; **dependency legality = HARD constraint** (greedy topological frontier fill); soft order of ready frontier = value(carried, never re-scored) → retires_risk!=null first → lower cost → lowest-S*; **cost_proxy = len(requirements)+len(acceptance)** (declared in ordering_basis, never estimated); cycle/dangling-depends_on/skeleton-with-deps → verdict:dependency_defect + route to SLICE-EXTRACT; full accounting; stay in lane (only `skeleton:true` flag on pos1).
- **FOUNDATION-CUT** (role 5/7) → `.roadmap/06-foundation-cut.json` (D7) {skeleton_id, foundation_cut{foundational_decisions[FD*]{category,needed_by,why_foundational,grounded_in}, skeleton_seams[]{seam,must_establish,grounded_in}, cross_slice_invariants[INV*]{invariant,applies_to,grounded_in}}, deferred[]{item,defer_to,reason,grounded_in}, coverage, cut_counts}. Three inputs: 04+05+aprd.frozen.md. Names the MINIMUM to decide/build once (§5.7, RM9). Load-bearing: **anti-waterfall lever — bias thin, under-cut not over-cut, when in doubt DEFER** (widening later cheaper than wrong foundation); **skeleton drives the cut** (its seams = the spine; FDs derive from the seams the skeleton crosses + deployment reality, each `needed_by` S1); **RM11 name-don't-decide** — FD `category` names what Phase 2 must resolve, NEVER the choice (no vendor/stack/schema/endpoint/library), seams carried from 04 stay named-not-designed; **P11 invariants READ not invented** — each cites real C*/A*/E*, §6.1 examples (auth/error/observability) are prompts-to-look not a checklist (don't manufacture error/observability invariants the aPRD is silent on); same topic can yield BOTH an invariant (fixed model) + an FD (open HOW) — e.g. A2 fixes "OAuth delegation, no stored creds" [INV] while "which provider + boundary" stays [FD]; **deferred[] = thinness evidence** routing every post-skeleton slice's HOW-decisions (PDF gen→S3, time-entry shape→S2, project model→S4) to the owning slice; full accounting (every present seam carried once, every post-skeleton slice represented). interactive:false. Isolated PASS (fresh verifier, 7/7) + full-3-step windowed-e2e PASS (no cascade, interface threaded). Golden roadmap chain now 02→03→04→05→06. **Next: RE-RANK (role 6/7) — BLOCKED on Phase-3 real-DAG schema (forward dep); recommend authoring SEQUENCE-REVIEW (role 7/7, client gate, reads 05, no forward dep) first. See Next-action / D8.**

### 2026-06-07 — Phase 1 client gate (D8 resolved)
- Resolved **D8** (author SEQUENCE-REVIEW now, defer RE-RANK to post-Phase-3; + the reusable interactive-prompt two-phase design pattern + the data-chain window rule — see Open decisions).
- **SEQUENCE-REVIEW** (role 7/7, client gate, **interactive:true**) → `roadmap.md` (Phase A, client-facing presentation) + `07-sequence-reviewed.json` (Phase B, client-confirmed baseline). In: `05-sequence.json`. Two-phase single session: present recognition-over-recall (order + first-demo + plain per-position rationale + multiple-choice reorder) → PAUSE → on client reply validate/apply/write. Load-bearing: **legal reorder options computed deterministically from `depends_on`** (slack rule; skeleton pinned pos1, never offered; fully-forced order → confirm+escape only, no fake choices); **dependency legality overrides the client (RM5)** — illegal/skeleton-move → `blocked_overrides[]`+`needs_followup:true`, legal order held; **value carried verbatim, never re-scored** (§7); coverage preserved; stays in lane (order only, no foundation-cut/decisions/HOW shown — §9). `client_response` enum confirmed|reordered|deferred|blocked (the **blocked** value added on retry — first run emitted off-schema `reorder_requested` for the all-illegal case; enum gap = the one prompt defect, fixed). Isolated PASS (3 paths: Phase-A clean-room present + paused; Phase-B confirm; Phase-B blocked) + fresh-verifier 10/10 + windowed-e2e PASS (SKELETON-IDENTIFY→SEQUENCE→SEQUENCE-REVIEW, real data-chain window, interface threaded clean, no cascade). Golden roadmap chain now 02→03→04→05→06→07 (+`roadmap.md`). **Phase 1 client surface COMPLETE; only RE-RANK remains (deferred, forward-blocked). Next: Phase 2 ADR / DECISION-EXTRACT.**
### 2026-06-07 — Phase 2 ADR start
- **DECISION-EXTRACT** (role 1/7, head of ADR pipeline) → `.adr/01-decision-points.json` {decision_points[DP*]{decision, category, forced_by[], candidate_blast_radius, blast_rationale, fork_evidence, cut_ref}, checklist_coverage[all 10 §4 categories], aprd_defects[], decision_point_counts{total,foundational,local}}. Three inputs: aprd.frozen.md + aprd.lock + 06-foundation-cut.json. Walks the fixed §4 decision-category checklist (recognition>recall, P7); emits every genuine FORK (discriminator: forced by ≥1 aPRD elem AND live ≥2-ways AND structural blast radius). Load-bearing: **name the OPEN QUESTION never the answer (RM11)**; **cut SEEDS not bounds** (expand coarse FDs into distinct one-decision-per-point DPs — FD1→style+stack, FD2→provider[found]+mechanism[local]; adversarially hunt forks the cut missed → cut_ref:null signals thin cut; INV* = fixed forces, cite never re-open; deferred[] items → local DPs); **forced_by verbatim, no padding** (timeline/scale constraints rarely CREATE a HOW-fork — deployment legit turns on timeline, config-injection does not); **candidate_blast_radius = PROPOSAL** TRIAGE finalizes (foundational=pre-draw/what-components-ARE-or-how-cut; local=inside-one-box; never trivial); collapsed-to-one = closed in checklist_coverage NOT emitted (sync/async closed by A13/INV6; Conformance N/A greenfield); full checklist accounting; deterministic emission (checklist order; within-category tie = earliest forced_by id by aPRD document position E→R→C→A→AC). Stays in lane (no triage-final/options/decide/ADR/client). Guards HALT on no-frozen-aPRD/lock!=frozen/missing-cut/non-greenfield. **Isolated test: 4 hardening rounds** (each fixed a distinct, progressively-trivial gap, prompt converged — NOT the same defect repeating): R1 5 fixes (cut_ref string-"null"→bare JSON null; split OAuth provider/mechanism per one-decision-per-point; boundary-strategy local→foundational discriminator clarified; anti-padding C3); R2 1 fix (timeline C2 padding → "timeline/scale rarely CREATE a fork" guard); R3 1 fix (sub-count MISCOUNT 7/3 vs actual 8/2 → per-tag tally-by-walking safeguard); R4 1 fix (within-category emission order under-specified for mixed-prefix forced_by → precise document-position rule); R5 clean — fresh-verifier + mechanical: 11 DPs, counts 7F/4L tally-verified, forced_by all verbatim, checklist 10/10 ordered, cut_ref bare-null, deterministic order. **Windowed-e2e PASS** (head-clamp: Phase-2 first authored step, no upstream Phase-2 producer → window = this single step seeded from golden 06+aprd). **Variance note:** extractor's emitted SET varies run-to-run (9–11 DPs, which adversarial forks surface) — expected for creative extraction; every run schema-clean + substantively correct. Golden `01-decision-points.json` promoted (`_fixtures/greenfield-clean/.adr/`). **Next: TRIAGE (role 2/7) — binding foundational|local|trivial call + in-cut|not-yet split vs the cut; must handle a 9–11-point input, not assume golden's exact 11.**

- **TRIAGE** (role 2/7) → `.adr/02-triage.json` {triage[per-DP verdict], resolution_queue[], slice_deferred[], deferred_queue[], conventions[], cut_gaps[], triage_counts}. Two inputs: 01-decision-points.json + 06-foundation-cut.json. The gate that scopes the foundation pass (§5.3, D2). **Two-axis triage**: Axis1 BINDING `blast_radius` foundational|local|**trivial** (EXTRACT only proposed candidate foundational|local; TRIAGE confirms/overrides + alone may downgrade to trivial); Axis2 (foundational only) **in-cut|not-yet** vs cut. **Route = pure fn of axes**: found+in-cut→resolution_queue(OPTION-GEN); found+not-yet→slice_deferred(defer_to); local→deferred_queue(Phase 3); trivial→convention. Load-bearing: **cut FD list = authoritative in-cut set** (FD.needed_by ∋ skeleton_id → in-cut); **skeleton-need test** for unnamed foundationals (cut_ref null/deferred:Sx → skeleton/INV breaks without it → in-cut + **cut_gap:true** [Phase-1 thin-cut feedback, mirrors mode:slice]; else not-yet→defer_to earliest needing slice); **convention = announce-it's-convention, never name value** (RM11 lane); carry id/decision/category/cut_ref VERBATIM (classify ≠ rewrite); 4-queue partition + tally-by-walking; **robust to variable 9–11-DP input**. Lane-clean (no options/decide/coverage/ADR/client/aPRD-reopen). Guards HALT on missing-01/missing-cut/non-greenfield; empty-points→report+stop (note EXTRACT already routed aprd_defects to Phase 0). **Isolated PASS** (1 round, 0 defects — fresh separate verifier 7/7 mechanical: schema/partition/verbatim/route-consistency/counts/blast_decision/cut_gap all clean; soft NOTE only on DP5 boundary reasoning, "not a mechanical defect"). **Windowed-e2e PASS** (clamp to Phase-2 first authored step → window DECISION-EXTRACT→TRIAGE; head seeded golden aprd+lock+cut; EXTRACT emitted **10-point** set [≠golden 11, different id-map] → TRIAGE consumed clean, accounting held, interface threaded, no cascade). **Judgment-variance (expected, both valid):** cut_ref:null foundationals (boundary strategy, API style) route **not-yet→S4** OR **in-cut+cut_gap** run-to-run — genuine architect judgment (does a walking-skeleton need the module-cut locked?); prompt handles both, schema/accounting invariant. Golden `02-triage.json` (6 in-cut/1 not-yet→S4/4 local/0 trivial, cut_gaps []) regenerated from golden 11-point 01, promoted. **Next: OPTION-GEN (role 3/7) — generate ≥2 real alternatives per in-cut foundational, cheapest-first; robust to variable-size resolution_queue; canon fixture TBD.**

- **OPTION-GEN** (role 3/7) → **DIR** `.adr/03-options/`: per-DP file `03-options/<DP-id>.json` + manifest `03-options/index.json` (§10 names a dir; **decided per-DP files + index manifest** — one EVALUATE-DECIDE enumeration entry point + clean accounting). Per-DP: {id/decision/category/forced_by/cut_ref VERBATIM from 01, grounding{existing_system,canon_status,source,note}, options[{option(named candidate), summary, source, reason_to_consider, tradeoffs{strengths[],costs[]}, bears_on[]}], option_count, degenerate, degenerate_reason, no_pick_note}. Manifest: {resolution_queue echo, option_files[{id,path,option_count}], canon_status, degenerate[], skipped[], option_set_counts{decisions_grounded,total_options,min_options_per_decision}}. Five inputs: 02-triage (resolution_queue=work list) + 01 (point bodies) + frozen aPRD (forces) + 06-cut (INV* compliance) + OPTIONAL `arch-canon.json`. **≥2 REAL live alternatives per in-cut foundational, cheapest-source-first** (existing[N/A greenfield]→canon→reasoned). Load-bearing: **lane moves PARTWAY** — OPTION-GEN NAMES concrete options (the RM11 name-don't-decide boundary shifts: naming options IS the job, §6.1) but **still does NOT decide** (no score/rank/recommend/pick — EVALUATE-DECIDE role 4); **canon graceful-degrade** (arch-canon OPTIONAL; absent→source:reasoned+canon_status:absent+flagged, first project pays full reasoning cost §7.1; NO heavy fixture authored — mirrors Phase-0 missing-source); **anti-strawman 3-clause** (team-would-weigh AND compliant-not-DOA AND distinct); **compliance gate** — option breaching hard C*/INV* = dead-on-arrival → EXCLUDE; **trade-offs NEUTRAL+bidirectional** (strengths+costs both, no verdict); `bears_on`=eval hooks not scores; **degenerate honestly** (one-compliant→degenerate:true, never pad strawman); full accounting (one file per queue id; len(option_files)+len(skipped)==len(resolution_queue); missing-body→skipped[]); **robust to variable queue**. Stays in lane (no decide/consequences/ADR/coverage/client/aPRD-reopen). Guards HALT on missing-02/01/frozen-aPRD/non-greenfield; empty queue→index w/ empty option_files+note. **Isolated PASS** (1 round, golden 6-item queue; fresh verifier 6/6 judgment + mechanical clean). **Windowed-e2e PASS (2 retries):** window DECISION-EXTRACT→TRIAGE→OPTION-GEN. **Retries fixed a REAL prompt defect** — compliance-gate loophole: first run a DP1 **microservices** option slipped in rationalized "to confirm non-viability" (own costs admitted "banned by INV6" = dead-on-arrival). R1 fix (forbid for-completeness/confirm-non-viability + keyword test) was **gamed** (runner reworded cost to dodge keywords, kept the DOA option). R2 fix re-anchored gate on **SUBSTANCE + "the decision question bounds the option set"** (a branch DECISION-EXTRACT already closed in `decision`/`fork_evidence` stays closed — don't re-open) → multi-service correctly excluded, fresh verifier 5/5 PASS. Interface threaded clean across a **variance-driven 7-item queue** (e2e run routed DP5+DP6 in-cut+cut_gap; OPTION-GEN consumed clean), no cascade (window tail). **Variance note (benign):** option-set membership/count vary run-to-run (DP4 ±MongoDB, DP6 ±GraphQL+SPA, DP7 single-provider vs generic-OIDC) — all defensible scope/compliance calls; schema/accounting/compliance hold every run. Golden `03-options/` (6 files: DP1=2 multi-service-excluded, DP2/DP4/DP6/DP7=3, DP10=3; 17 options; degenerate[]/skipped[]) regenerated from golden 01+02, promoted. **Next: EVALUATE-DECIDE (role 4/7) — score option sets vs CONSTRAINTS/AC/NFR + PICK + record consequences; alternatives stay live (D1/D3); enumerate the manifest, robust to variable option-set sizes.**

- **EVALUATE-DECIDE** (role 4/7, the decider — closes the fork) → per-DP `.adr/03-options/<DP-id>.decision.json` + manifest `.adr/03-options/decisions-index.json`. Per-DP: {id/decision/category/forced_by/cut_ref VERBATIM, options_ref, grounding_source, evaluation[{option, assessment(neutral,before-pick), decisive_factors[]}], decision_made(verbatim), rationale, rejected[{option, why_rejected→force}], consequences{positive[],accepted_cost[],follow_on[]}, traces[], degenerate_forced, degenerate_reason, lane_note}. Manifest: {decisions[{id,category,decision_made,options_ref,decision_ref,option_count,degenerate_forced,traces}], undecided[], decision_counts}. Four inputs: 03-options/index + per-DP option files + frozen aPRD (CONSTRAINTS/AC/assumptions-as-NFRs) + 06-cut (INV* hard floor). **Scores option sets, PICKS one, records consequences (§5.5).** Output schema/path decided: **per-DP decision files + decisions-index** (mirrors OPTION-GEN; §10 calls `03-options/` the home for "evaluation working files"; `04` reserved for RECONCILE). Load-bearing: **D1/D3 live-alternatives is THE rule** — `evaluation[]` assesses EVERY option neutrally as an option-property BEFORE the pick (reads same whichever won), then decision_made/rationale/rejected reference it (structure forces alternatives live, not reverse-engineered); **picks** (new vs upstream RM11) but **stays in lane** — no ADR render/id (SYNTHESIZE-ADR), no cross-decision conflict/coverage (RECONCILE — each point decided against FIXED forces INDEPENDENTLY → order-independent, conflicts stay visible; cross-deps NOTED in follow_on not resolved), no re-source/add/drop options (picks FROM given set), no aPRD/cut/triage re-open (D9 — unresolvable→undecided[]+Phase 0), no client (§9); scoring grounded in `bears_on`+`forced_by`+C*/AC*/A*/INV* (no invented criteria); weighting explicit (C2+A13 dominate at this size); rejections force-traced; traces = forced_by ∪ cited-by-name-in-prose (operational anti-padding); degenerate set→forced pick; full accounting; robust to variable manifest. Guards HALT on missing-index/aprd/cut + non-greenfield; empty option_files→empty index+note. **Isolated PASS (3 retries, in budget) — each retry killed a distinct GROUNDING-DISCIPLINE defect variant, converging on root (not identical repetition):** R1 dev-side market claim ("Ruby dev-pool shrinking 2025" rejecting Rails) → forbade recalled market/popularity args + C2≠labour-market-proxy; R2 trace-padding (DP2 uncited AC3/AC5 in traces) + verbatim break (DP6 "SPA frontend"→"single-page application frontend") → operational trace check (forced_by OR cited-by-name; no wholesale bears_on copy) + copy-by-paste/no-reword rule; R3 defect mutated user-side ("Google held more broadly than GitHub" as DP7 tiebreaker) → generalized ban to user-side adoption + added **"contract-indifferent → default among equals"** move (AC5 names Google/GitHub as equals → no contract force separates them → pick a default, say so honestly, never fabricate a tiebreaker; decision-layer analogue of degenerate-honestly). Retry-3 fresh separate verifier **7/7** + mechanical all-clean. **Windowed-e2e PASS** — window TRIAGE→OPTION-GEN→EVALUATE-DECIDE (full Phase-2 tail, head seeded golden 01+06+aprd). TRIAGE emitted **7-item** queue (variance: DP5 boundary-strategy in-cut+cut_gap — designed catch) → OPTION-GEN grounded 7 → EVALUATE-DECIDE decided 7 incl. NEW DP5; interface threaded clean, fresh verifier 7/7 on the variance set, no cascade (window tail). Golden decisions (DP1=monolith/DP2=Python-Django/DP4=PostgreSQL/DP6=MPA-SSR/DP7=Google-default-among-equals/DP10=PaaS + decisions-index) regenerated from golden 6-item 03-options, promoted. **Variance (benign):** picks vary run-to-run (DP2 Python/Node/Rails; DP1 flat/modular) — all defensible vs forces; live-alternatives discipline + accounting + lane + grounded-traces are the invariants, not the specific pick. **Next: RECONCILE (role 5/7) — first cross-set stage (conflicts + coverage); flags never re-decides; consumes decisions-index + decision files' traces; writes `04-conflicts.json` (§10 reserved).**

- **RECONCILE** (role 5/7, the coherence+coverage gate — first cross-set stage) → `.adr/04-conflicts.json` (§10 reserved slot). Four inputs: `03-options/decisions-index.json` (enumerate decided set) + each `<DP-id>.decision.json` (picks + traces + `consequences.follow_on`) + aprd.frozen.md (C* coverage target + id-space) + 06-foundation-cut.json (INV* hard floor + deferred[]). **Detects three things, FLAGS never re-decides (D9):** (1) cross-decision conflicts (two picks that cannot both hold — grounded via follow_on sibling-DP notes [honored vs contradicted] + pairwise INV* paradigm clash; emit only on a named mutual-exclusion, tension≠conflict, honored-dependency≠conflict — false positives thrash the re-decide loop); (2) constraint violations (pick breaching hard C*/INV* — the floor backstop); (3) coverage bidirectional (ADR→aPRD: every decision traces non-empty + ids real; aPRD→ADR: every in-scope C* bucketed covered/deferred/premise/gap). Schema {decisions_checked, undecided_carried, conflicts[], constraint_violations[], coverage{adr_to_aprd{all_traceable,untraceable[]},aprd_to_adr{in_scope_constraints,covered[{constraint,by}],deferred[],premise[],gaps[]}}, structural_defects[], verdict(coherent|blocked), blocking_count, reconcile_counts}. verdict blocked if ≥1 conflict/violation/gap/untraceable/defect → routes back (→EVALUATE-DECIDE re-decide; aPRD-defect gap→Phase 0); coherent → hands to SYNTHESIZE-ADR. **Load-bearing:** **coverage target = aPRD CONSTRAINTS (C\*) ONLY (literal D5/§5.6); INV\* are the violation-check hard floor, NOT coverage targets** — requiring INV* coverage manufactures false-positive gaps (most INV* are slice-level invariants Phase 3 honors, no foundational decision is *about* them; tracing them would be padding); **`covered.by` = literal string membership in `traces[]`, never topical relevance** (a "feels related" decision that lacks the id is a false traceability claim); C3=premise (greenfield class fact); FLAG-never-re-decide is THE lane line. Stays in lane (no re-decide/edit, no ADR render/id [SYNTHESIZE-ADR], no re-source [OPTION-GEN], no strawman/over-decided audit [CRITIQUE], no aPRD/cut re-open [D9], no client §9). Guards HALT on missing-index/aprd/cut + non-greenfield; empty decisions[] → empty 04 + coherent + note. **Isolated PASS (2 retries, in budget) — each killed a distinct coverage-scope defect, converging (not repetition):** R1 INV-coverage false positive (run flagged INV2 as a "gap" — no decision traced it, but INV2 is a slice-level invariant no foundational decision addresses) → narrowed coverage to C* only, INV* reframed as violation-floor-not-coverage-target; R2 `covered.by` topical mis-attribution (DP4/DP7 listed under C1 though neither traces C1) → literal-trace string-membership rule. Retry-2 fresh separate verifier graded A–H (criteria: schema/enumeration/conflicts/violations/traceability/coverage/counts/lane) — A-E/G/H clean first pass, F (coverage) PASS after the literal-trace fix; verifier independently re-walked all follow_on notes (0 conflicts), picks vs C*/INV* (0 violations), traces (all real), C1/C2 literal-trace coverage, C3 premise, gaps[] empty. **Windowed-e2e PASS** — window OPTION-GEN→EVALUATE-DECIDE→RECONCILE (full Phase-2 tail head; seeded golden 01+02+aprd+cut). Ran with run-to-run **variance** (DP1=traditional monolith, DP2=Python/Django — picks + richer traces ≠ golden) → RECONCILE consumed clean: coherent, C1.by=[DP1,DP2,DP6,DP10] literal-trace-correct (DP4/DP7 excluded — R2 fix held on fresh data), C2 all 6, C3 premise, 0 conflicts/violations/gaps; interface threaded clean, no cascade (window tail). Golden `04-conflicts.json` regenerated from golden decisions, promoted. **Variance note (benign):** clean golden yields all-empty conflict/violation/gap every run (the decided set IS coherent + C-covered by construction) — the detection *mechanisms* prove out on negative/variance inputs; the invariants are literal-trace `by` + INV-not-coverage-target scoping + FLAG-never-re-decide. **Next: SYNTHESIZE-ADR (role 6/7) — render each decision as a Nygard ADR + assign monotonic ADR-NNNN id; gated on 04-conflicts coherent verdict; transcribe-don't-re-decide.**

- **SYNTHESIZE-ADR** (role 6/7, the renderer) → per-decision `.adr/drafts/<NNNN>-<slug>.draft.md` (Nygard ADR, §6.1 frontmatter+body) + `.adr/adr-index.json` (machine index). Three inputs: `04-conflicts.json` (GATE — render only on `verdict:coherent`; blocked→HALT) + `decisions-index.json` (enumerate + deterministic id order) + each `<DP-id>.decision.json` (render content source of truth). **TRANSCRIBE never RE-DECIDE (D1/D6)** — only original prose is the `title`; Context=problem-not-answer from decision/forced_by/tensions (no chosen-option name, no new force), Decision=`decision_made` verbatim-substring, Alternatives=rejected[] options+assessments+force-traced why_rejected (degenerate-honest / default-among-equals, no fabricated discriminator), Consequences=positive/accepted_cost/follow_on transcribed. **DESIGN CALL (deviation from authoring-note, documented in YOU-ARE-HERE):** renders **DRAFTS (`status:Proposed`, `.adr/drafts/`) NOT immutable `.adr/log/` Accepted** — CRITIQUE (role 7) runs after + loops back to synthesize (§5.8), so the record must be mutable (D6); §10 has a `drafts/` dir; mirrors Phase-0 SYNTHESIZE→drafts→freeze precedent. Mechanical post-CRITIQUE freeze (non-LLM, no prompt) promotes drafts→log + Proposed→Accepted + writes `adr.lock`; SYNTHESIZE-ADR does NOT write log/lock/Accepted. **Id:** monotonic `ADR-NNNN` from 0001 in decisions-index array order (deterministic); `undecided[]`→no id/no ADR (noted, skipped). Stays in lane (no decide/re-detect/re-source/self-critique/freeze/client). Guards HALT on missing-04/blocked-verdict/missing-index/unreadable-decision_ref/non-greenfield; empty decisions[]→empty adr-index+note. **Isolated PASS (0 retries)** — fresh separate verifier A–G all clean (enumeration/ids, frontmatter fidelity, verbatim pick, problem-framed Context no-invented-force, live faithful alternatives incl DP7 default-among-equals, transcribed consequences, lane: no log/lock + coherent gate + correct counts). **Windowed-e2e PASS (1 retry):** window EVALUATE-DECIDE→RECONCILE→SYNTHESIZE-ADR (Phase-2 tail, head seeded golden 03-options+aprd+06). Ran with variance (DP2=Python/Django; RECONCILE coherent w/ DP4 literal-trace-excluded from C2). **The 1 retry fixed a REAL prompt defect:** e2e runner **paraphrased the option name in `## Decision`** ("…monolith with flat internal structure" vs `decision_made` "Single-deployment monolith (flat structure)") — lost string-auditability, risked drift; rule too soft to be reliable (isolated runner happened to comply). R1 fix sharpened to "**`decision_made` verbatim as a substring, copy-by-paste, option's words unaltered**" + CORRECT/WRONG pattern example → rerun all 6 carried the string verbatim, held; window threaded clean, no cascade (window tail). Golden drafts (ADR-0001..0006) + adr-index regenerated from golden decisions (golden inputs, fixed prompt), promoted to `_fixtures/greenfield-clean/.adr/{drafts/,adr-index.json}`. **Variance (benign):** titles + section phrasing vary (author's-prose title, Nygard restatement) — all faithful; invariants = transcribe-not-re-decide + verbatim option-string-in-Decision + verbatim traces/category + Proposed-drafts-not-Accepted-log + deterministic id order. **Next: CRITIQUE (role 7/7, last Phase-2 prompt) — hostile reviewer on the rendered drafts (strawman/untraceable/uncovered/contradicting/over-decided/unforced/not-yet); blocking→loop back to SYNTHESIZE-ADR; clean→freeze. Resolve overlap-with-RECONCILE lane line + anti-false-positive discipline; test both directions. After CRITIQUE: Phase 2 COMPLETE.**

- **CRITIQUE** (role 7/7, the hostile architecture reviewer — **THE last Phase-2 prompt; Phase 2 now COMPLETE**) → `.adr/05-critique.json` (spine-numbered after 04-conflicts; mirrors Phase-0 CRITIQUE→08). Five inputs: `adr-index.json` (enumerate the rendered draft set) + each `.adr/drafts/<NNNN>-<slug>.draft.md` (the ADRs to attack) + aprd.frozen.md (trace+coverage oracle: id-space + C* target) + 04-conflicts.json (RECONCILE verdict + in_scope_constraints + premise/deferred buckets) + 06-foundation-cut.json (FD*/deferred[]/INV* = over-decided/not-yet/unforced oracle). The adversarial final gate on the **RENDERED** drafts before the mechanical freeze. **Seven blocking categories** (blockers only): strawman-alternative (alt never live — DOA breaks hard C*/INV*, or fabricated discriminator), untraceable-adr (rendered traces[] empty/phantom id — D4), uncovered-constraint (in-scope C* no ADR traces + not deferred/premise — **C\* only; INV\* are floor not coverage target** — D5), contradictory-adrs (two Decisions mutually exclusive/jointly INV-violating), over-decided (actually local→Phase 3 — D2), unforced-decision (traces resolve but force no ≥2-way fork — gold-plating, D4), not-yet (foundational but belongs to a later slice per cut deferred[] — D11). Schema {adr_index_ref/aprd_ref/conflicts_ref/foundation_cut_ref, class, skeleton_id, adrs_reviewed[], verdict(clean|blocked), issues[{id,category,target_adr,finding,fix_hint}], issue_count, critique_counts{adrs_reviewed,issues,by_category{7}}}. Blocking→loops to SYNTHESIZE-ADR (§5.8 `Critiqued→Drafting`); clean→mechanical freeze (drafts→log, Proposed→Accepted, adr.lock — NOT CRITIQUE's job). **Load-bearing LANE vs RECONCILE (role 5):** NOT RECONCILE-twice — owns (a) the NEW checks RECONCILE can't do (strawman/over-decided/unforced/not-yet = legitimacy of decision-making) + (b) a backstop of trace/coverage/contradiction **on the RENDERED FORM** (the render is a new artifact — transcription can drop a trace / lose a covering ADR / clash two Decisions); does NOT re-run RECONCILE to override its `coherent` verdict. **Load-bearing ANTI-FALSE-POSITIVE (mirror Phase-0 resolution-test):** read WHOLE context before blocking; never re-litigate a logged pick (check real/forced/traced/coherent/in-cut, not the wisdom of the choice); **degenerate-forced ≠ strawman + default-among-equals ≠ strawman** (rejected option real + honest "default among contract-equivalent equals" is the correct move — block only a FABRICATED alt/discriminator); INV* not coverage targets; premise/deferred C* is covered (C3=premise≠gap); don't demand an ADR the aPRD doesn't force / that's local/not-yet (mirror failure of over-deciding); a real aPRD defect ROUTES to Phase 0 (§5.10, D9), never blocks the faithful draft; cosmetic/phrasing latitude not a defect. Stays in lane (no re-decide/re-render/edit-draft, no re-source, no RECONCILE-override, no freeze/log/lock/Accepted, no aPRD/cut/triage re-open, no client §9). Guards HALT on missing adr-index/aprd/04/06 + unreadable draft + non-greenfield; empty adrs[]→clean+empty+note. **Isolated PASS (0 retries), BOTH directions:** clean golden→`clean` (6 reviewed, schema clean); **planted-defect fixture (8 ADRs = 4 plants + 4 clean)→all 4 caught + correctly categorized + ZERO false positives** — strawman (ADR-0001 DOA microservices vs INV6), untraceable (ADR-0003 phantom `R88`), not-yet (ADR-0007 invoice-aggregation explicitly in cut deferred[]→S3), unforced (ADR-0008 observability gold-plating, A13/C2 resolve but force no fork); 4 clean ADRs untouched incl. **ADR-0005 default-among-equals correctly NOT flagged**; counts tally; full accounting (8 reviewed). **Fresh SEPARATE verifier independently re-walked all 8 → PASS** (0 missed, 0 FP, categorization OK, schema clean). **Windowed-e2e PASS (0 retries)** — window RECONCILE→SYNTHESIZE-ADR→CRITIQUE (full Phase-2 tail; head seeded golden 03-options decisions+index+aprd+cut): RECONCILE coherent (C1/C2 literal-trace, C3 premise, 0 issues) → SYNTHESIZE-ADR 6 Proposed drafts+adr-index (accounting clean) → CRITIQUE clean (6 reviewed, 0 issues, no log/lock written). Interface threaded clean, no cascade (window tail). Golden `05-critique.json` (clean, empty issues) promoted to `_fixtures/greenfield-clean/.adr/`. **Variance note (benign):** clean golden→`clean`/empty every run (rendered set IS sound by construction); the 7 detection mechanisms earn their keep on negative/planted inputs (proven). Invariants: the 4 new adversarial checks + rendered-form backstop + anti-FP resolution-test + FLAG-never-fix lane (loops to SYNTHESIZE-ADR, never patches a draft). **PHASE 2 COMPLETE — DECISION-EXTRACT→TRIAGE→OPTION-GEN→EVALUATE-DECIDE→RECONCILE→SYNTHESIZE-ADR→CRITIQUE end-to-end; freeze is mechanical (no prompt). Next: Phase 3 HLD / DERIVE-COMPONENTS.**

### 2026-06-07 — Phase 3 HLD start
- Resolved **D9** (Phase-3 skeleton-first / defer-increment mode split + baselined-`.adr/log/`+`adr.lock` input form + §5.1 load folded into head, no `00-inputs.json`). See Open decisions.
- **Seeded the frozen ADR baseline fixture** (mechanical freeze by hand on golden Phase-2 drafts): `_fixtures/greenfield-clean/.adr/log/<NNNN>-*.md` (6, status Accepted) + `.adr/adr.lock` (status:frozen + baselined adrs[] manifest {id,dp_id,title,category,traces,log_ref}). Pre-freeze `drafts/`+`adr-index.json` (Proposed) kept as Phase-2 goldens.
- **DERIVE-COMPONENTS** (Phase 3 role 1/8, head of HLD pipeline, **SKELETON pass**) → `.hld/skeleton/components.json`. Four inputs: `adr.lock` (frozen gate+manifest) + `.adr/log/*.md` (bodies; boundary-strategy ADR = Architectural-style/Boundary-strategy = cut driver) + `aprd.frozen.md` (R*=responsibilities, E*=entities) + `06-foundation-cut.json` (skeleton_seams seed + INV* floor). **First drawing stage: clusters the FULL requirement set into the full component graph (boxes + dependency edges) per the boundary-strategy ADR — the frame every slice extends + the build DAG (H7/H13).** Schema: components[{id,name,responsibility,owns_entities[E*],traces[R*],realizes_seam[],honors_adr[]}] + edges[{from,to,reason}] + coverage{bidirectional R + single-owner E + seam_realization} + structural_defects/frame_conflicts/aprd_defects + counts. **3-clause component test:** traces ≥1 R (else gold-plating→drop) AND honors frame (flat-monolith/INV6 → logical responsibility units in ONE deployable, NO distributed services/brokers/workers/scale-topology) AND responsibility-unit-not-a-layer. Load-bearing: **APPLY boundary-strategy ADR never invent cut (H2/H12)**; boundary-strategy ADR absent (deferred to slice)→style ADR governs; **skeleton clusters FULL R-set** (whole graph; walking-skeleton slice = thinnest PATH through it, MODEL-FLOWS' later concern); **bidirectional coverage** (component→R + R→component, full adversarial gate is RECONCILE/CRITIQUE role 8); **single-owner entities** (proposal; MODEL-DATA role 4 formalizes; shared infra box may own []); **edges = STRUCTURAL DIRECTION ONLY never contracts** (no kind/shape/failure — DEFINE-CONTRACTS role 2 owns seam spec; acyclic, cycle→structural_defects); **realize every present skeleton seam**; **name responsibility not implementation** (no NEW impl detail the frame left open; a frame-FIXED choice may be referenced — reference what an ADR decided, never decide what it deferred); **deterministic emission = topological build order, most-depended-upon first**. Escapes: HALT on missing-aprd/unfrozen-lock/missing-log/missing-cut/skeleton-already-frozen(=increment trigger, not authored)/non-greenfield; frame-unbuildable→frame_conflicts→Phase 2; requirement-unstructurable→aprd_defects→Phase 0 (route never patch, H10). Lane-clean (no contracts/local-ADRs/data-model/NFR/flows/cross-cutting/tests/build-DAG/audit/client). **Isolated PASS (0 retries)** — fresh separate verifier A–H all PASS (5-comp run). One behavior-preserving clarification after (Mandate 7: a frame-fixed vendor/style may be referenced as context). **Head-clamped windowed-e2e PASS (0 retries)** — DERIVE-COMPONENTS is the Phase-3 first authored step (no upstream Phase-3 producer) → window clamps to single step seeded from golden; re-ran final prompt → fresh verifier A–H all PASS (6-comp run w/ explicit Data Store). Golden `.hld/skeleton/components.json` promoted to `_fixtures/greenfield-clean/.hld/skeleton/`. **Variance note (benign — creative structural extraction):** component CUT varies run-to-run (5 vs 6 components; explicit Data-Store box vs persistence-folded-into-domains) — all defensible flat-monolith cuts; invariants (3-clause test, bidirectional coverage, single-owner, edges-structure-only, seam realization, frame-fidelity, topological emission, accounting) hold every run; verifier NOTE (Data Store tracing R8/R10) benign — mirrors governing ADR-0003's own R7–R10 trace set. **Next: DEFINE-CONTRACTS (role 2/8) — put kind/shape/failure_modes on each edge DERIVE-COMPONENTS drew bare; THE load-bearing stage (H1).**
- **DEFINE-CONTRACTS** (Phase 3 role 2/8, **SKELETON pass**, **THE load-bearing stage — H1 contracts-before-components**) → `.hld/skeleton/contracts.json`. Five inputs: `components.json` (PRIMARY — `edges[]` = seams to contract, `components[]` for grounding) + `aprd.frozen.md` (R* trace oracle, E* payloads) + `adr.lock` (frozen gate) + `.adr/log/*.md` (frame deciding KIND) + `06-foundation-cut.json` (INV* floor — INV6 forbids async; `deferred[]` = schemas not to invent). **Puts the contract on every bare edge DERIVE-COMPONENTS drew: `contracts[{id:CT*, between:[from,to], kind, shape, failure_modes[], traces[R*], honors_adr[], honors_inv[]}]` + bidirectional edge coverage + kind_distribution + accounting.** **Kind discriminator (derive-never-default):** `sync_api` = synchronous in-process call (flat monolith INV6 = one process, no network hop); `shared_data` = read/write durable state into the persistence component; `async_event` = **INV6-forbidden by default** (no queue/worker/broker) → an async edge with no frame-permitted forcing reason = `frame_conflicts[]`→Phase 2. Load-bearing: **one-contract-per-edge BIJECTION** (consume edges verbatim, CT* in edge order); **`shape` named-not-designed (RM11/§1.2)** — a schema REFERENCE (E*-set + responsibility), NEVER invented field detail (cut `deferred[]` defers project→S4/time-entry→S2/invoice→S3 schemas); **≥1 grounded `failure_mode`** — store-modes for shared_data, callee-error/not-found/no-valid-session for sync — **NO inter-process network-partition/message-loss on in-process sync seams** (single-process frame discipline); **≥1 verbatim R trace, no padding**; **frame-fidelity escape** (forbidden kind → frame_conflicts→Phase 2, never re-decide); **kind_distribution counted by enumerating each kind's CT* members independently** + sum==contracts. Lane-clean (no graph edit, no field-schemas [MODEL-DATA/slices], no local-ADRs/NFR/flows/cross-cutting/tests/build-DAG/audit/client). **Isolated PASS (2 retries, in budget — both killed the SAME miscount class, converging):** R1 kind_distribution summed to 12 vs 11 (per-contract kinds correct, tally wrong) → added sum-must-equal-contracts; **R2 the sum-check was GAMED** (runner shifted shared_data→sync_api so the wrong 8/3 split still summed to 11 — the "swap that still sums right" failure VERIFY warned of) → added count-each-kind-by-enumerating-its-CT*-members (sum alone gameable). Retry-2 fresh SEPARATE verifier A–H (bijection/kind-correctness/shape-named-not-designed/failure-modes-grounded/trace-fidelity/frame-id-fidelity/accounting/lane) **all 8 PASS** (golden 11-edge: 4 shared_data + 7 sync_api + 0 async). One behavior-preserving clarification after (member-enumeration in WORKING, emit exactly 3 integer keys — runner had materialized `*_members` audit arrays into the artifact; benign off-schema). **Windowed-e2e PASS (0 retries, clamped):** N=role 2, N-2 absent → clamp to phase-first → window DERIVE-COMPONENTS→DEFINE-CONTRACTS (head seeded golden frozen aPRD + ADR log/lock + cut). Ran with variance: head emitted 6-comp/**8-edge** graph with persistence FOLDED into Identity&Auth (no Data Store box — valid cut) → DEFINE-CONTRACTS consumed clean: **all 8 sync_api** (no store-component edge → no shared_data edges; runner DERIVED from topology, not rote-applied "store=shared_data" — discriminator robust to cut variance), bijection 8↔8, no defects. Interface threaded clean, no cascade (window tail). Golden `contracts.json` regenerated from GOLDEN 11-edge components.json with final prompt (schema-clean: 3 kind keys, 7 sync_api + 4 shared_data + 0 async, CT1–CT11 bijection), promoted. **Variance note (benign):** contract KIND distribution tracks the upstream component CUT (Data-Store box → shared_data store-edges [golden: 4]; persistence-folded → all-sync_api [e2e: 8]); both correct — discriminator derives kind from the graph's topology, not a fixed ratio. Invariants every run: bijection one-CT-per-edge, kind-derived-from-frame (async=0 under INV6), shape-named-not-designed, grounded-no-distributed failure modes, verbatim trace, frame-fidelity escape, independent-enumeration accounting. **Next: RESOLVE-LOCAL (role 3/8) — resolve each LOCAL fork the design forces + emit each as a `mode:slice` local ADR appended to the shared log (H3); FOUNDATIONAL surfacing here → escalate Phase 2 not resolve. Open: input form of local-decision queue (read 02-triage `deferred_queue[]`?) + how a local ADR appends to a FROZEN log + graceful empty-queue (skeleton pass may resolve few/zero locals; bulk arrives in increment mode).**
- **RESOLVE-LOCAL** (Phase 3 role 3/8, **SKELETON pass**) → `.adr/drafts/<NNNN>-<slug>.draft.md` (Proposed `mode:slice` local-ADR drafts, ids ADR-0007+) + `.adr/deferred-decisions.json` (queue-resolution ledger). Eight inputs: `02-triage.json` (`deferred_queue[]` = local work list) + `01-decision-points.json` (DP bodies) + `components.json` + `contracts.json` (emerging structure — owning-component lookup + escalation test) + `aprd.frozen.md` (trace oracle + forces) + `adr.lock` (frozen gate + id-continuation point) + `.adr/log/*.md` (frozen frame, may NOT re-decide) + `06-foundation-cut.json` (`deferred[]` re-defer grounding + INV* floor). **Drains the LOCAL-decision queue TRIAGE routed to Phase 3, resolves each fork the SKELETON forces + emits each as a `mode:slice` local ADR DRAFT (H3).** **3 dispositions (discriminator, derive-never-default):** `resolved` = skeleton forces it now (in-skeleton owning-component + skeleton-once-cross-cutting-not-box-internal [§1.2 defers class/fn/algo/library internals] + not slice-tagged) → pick option (live alternatives, force-traced) + Proposed draft; `re-deferred` = later-slice-owned / box-internal / slice-tagged → `defer_to`+reason, no ADR (**default when unsure**, RM9 anti-waterfall); `escalated` = actually-foundational (resolving forces cross-box change / changed contract kind / INV violation) → `escalations[]`→Phase 2, no ADR (thin-cut signal §5.4/§5.11). **3 open authoring calls RESOLVED:** (a) **input form** — reads `02-triage.deferred_queue[]` + `01` DP bodies directly; RESOLVE-LOCAL itself WRITES `deferred-decisions.json` (the §10 ledger), NOT a seeded input (D7 no-mint); (b) **THE load-bearing call — append to a FROZEN log** = drafts-then-freeze mirroring Phase 2: write Proposed `mode:slice` DRAFTS to `.adr/drafts/` (ids ADR-0007+ after baseline max), NEVER mutate `adr.lock` or immutable `.adr/log/`; local ADRs stay mutable through the Phase-3 role-8 gate (D6); the mechanical Phase-3 freeze (non-LLM, no prompt) promotes drafts→log + new lock AFTER the gate; (c) **graceful near-empty** — skeleton pass may resolve FEW/ZERO locals (most live inside per-slice internals); near-empty resolved + everything-else cleanly re-deferred is a complete deliverable; empty queue → empty ledger + note + zeroed counts. Load-bearing: drain-whole-queue+partition+accounting; verbatim DP-body carry; live-alternatives force-traced (D1/D3 — EVALUATE-DECIDE+SYNTHESIZE compressed, **no recalled market/popularity/adoption claim**, default-among-equals honest); NEVER-re-decide-foundational (reference-yes/re-open-no); monotonic id continuation (ADR-0007+, id only for resolved); traces verbatim no-padding; deterministic queue-order emission; **pinned bucket element shapes** (the R1 e2e fix). Lane-clean (no graph/contract edit, no foundational resolution, no data-model/NFR/flows/cross-cutting/tests/build-DAG/audit, no log/lock mutation, no freeze, no client). Escapes: HALT on missing/unparseable inputs / defect-carrying components∥contracts / unfrozen-lock / skeleton-already-frozen (=increment trigger, not authored) / non-greenfield; queued-local-actually-foundational → escalations[]→Phase 2. **Isolated PASS (0 retries)** — fresh SEPARATE verifier A–H (queue-drained+accounting, verbatim-carry, disposition-correctness, no-foundational-re-decided, live-alternatives-force-traced, trace-fidelity, frozen-baseline-integrity, lane) **all PASS, 0 defects** (golden 6-comp graph: 2 resolved [DP8 OAuth-lib→C2, DP9 config→C6] + 2 re-deferred [DP3 PDF→S3, DP11 test-strategy→S1]; lock+log untouched; verifier flagged DP9 as the most-borderline-but-defensible call). **Windowed-e2e PASS (1 retry, in budget):** window DERIVE-COMPONENTS→DEFINE-CONTRACTS→RESOLVE-LOCAL (real data-chain — components feeds both downstream, contracts feeds RESOLVE-LOCAL; head seeded golden frozen frame + Phase-2 queue 01/02). Ran with variance: head emitted **6-comp/14-edge** graph (C2=Auth, no Data-Store box) → DEFINE-CONTRACTS 14 contracts (4 shared_data + 10 sync_api + 0 async, bijection) → RESOLVE-LOCAL consumed clean, mapped owning components to the variant topology, MORE conservative disposition (1 resolved DP9 / 3 re-deferred incl. DP8→S1-build — the borderline OAuth-library call resolved anti-waterfall). **The 1 retry fixed a REAL PR2 schema-determinism defect:** the disposition BUCKETS had under-specified element shape — isolated emitted `resolved:["DP8","DP9"]` (bare ids), e2e emitted `resolved:[{dp_id,adr_id}]` (objects); two clean runs / two shapes for the field the role-8 audit consumes → violates PR2. Fix pinned each bucket's element shape (resolved=`{dp_id,adr_id}` / re_deferred=`{dp_id,defer_to}` / escalated=`{dp_id}`, never bare strings) → tail re-run produced pinned form, partition+accounting held; no cascade (window tail). Golden `deferred-decisions.json` + drafts `0007`/`0008` regenerated from GOLDEN 11-edge components.json + contracts.json with final prompt (2 resolved DP8/DP9 + 2 re-deferred DP3→S3/DP11, pinned shapes), promoted. **Variance note (benign — borderline cross-cutting locals):** which locals the skeleton "forces" varies run-to-run — DP8 (OAuth integration library) resolved-now OR re-deferred-to-S1-build; DP9 owning-component C6 OR C2 — both defensible (OAuth lib is borderline skeleton-wires-the-seam vs box-internal; config/secrets is cross-cutting with no dedicated owner box in a flat monolith). DP3 (cut_ref deferred:S3) re-defers every run (unambiguous slice-tag); DP11 (no owning component) re-defers every run (defer target varies S1/S3/DERIVE-TESTS — a role name is a slight imprecision but accurate destination; the no-owning-slice ambiguity). Invariants hold every run: full drain+partition+accounting, verbatim carry, live-alternatives force-traced, NEVER-re-decide-foundational, frozen-lock/log untouched (drafts-then-freeze), monotonic id continuation, traces verbatim no-padding, pinned bucket shapes, FLAG-never-fix escalation lane. **Next: MODEL-DATA (role 4/8) — logical single-owner data model from ENTITIES + persistence ADR; SKELETON pass = logical model + ownership, field-level schemas stay deferred per cut; ownership ambiguity = boundary defect routed back to §5.2/§5.3 not patched.**

### 2026-06-07 — Phase 1 client gate process note
- **Process note (no spec impact):** a continuation attempt via an `Agent` spawn with `to:<id>` in the body did NOT route to the existing runner — it spawned a FRESH agent that, given a near-empty prompt, went off-script and authored an unverified RE-RANK + fixtures + edited the tracker. Reverted all of it (`git restore` tracker, deleted the rogue RE-RANK + 3 fixtures). **Lesson: the Agent tool always starts a fresh agent; there is no SendMessage tool available to continue one. Test interactive Phase B by appending `[CLIENT REPLY] …` to a fresh runner's pasted prompt, not by trying to continue a paused agent.**
