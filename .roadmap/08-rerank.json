{
  "base_ref": ".roadmap/07-sequence-reviewed.json",
  "class": "agentic-delivery-pipeline",
  "roadmap_version": 25,
  "verdict": "re_ranked",
  "_note": "SELF-HOST repurposing of the RE-RANK 08 schema (D21): each entry = one remaining PROMPT-BUILD/CR, not a product slice. done_sentinel = the disk path whose presence+validity+class-discriminator == that build shipped (orchestrator STEP-0 derived-state key; D14/D20 analog). roadmap_version 6 = operator-directed ledger refresh (2026-06-11) after W6-REAUTHOR-REST 03-hld + 02-adr phases shipped + committed (6171502). roadmap_version 7 = operator-directed actual-state sync (2026-06-11): per-phase progress within W6-REAUTHOR-REST reflected (01-roadmap done + clean-room-verified; 00-aprd CLASSIFIER shipped 1/9) + drift-class lesson (incl. field-NAME variant) + 04-build spot-check TODO integrated into this ledger from now-deleted ephemeral memory (disk = source of truth, D20). Refresh moves all shipped CR-002 entries (W0, W1-W5, W6-REAUTHOR-04BUILD, W1b) into completed[]; remaining_sequence now = the genuinely-unshipped builds only. SCOPED to the CR-002 deterministic-spine wave + the CR-003 registry-fidelity fix; prior delivered waves (greenfield 10, bugfix 8) live on disk (D20), not re-listed. Disk is source of truth — this ledger is informational ordering, not the truth the loop derives from. roadmap_version 8 = operator-directed sync (2026-06-11) after 00-aprd GAP-DETECT re-authored + clean-room both-directions verified (5/9). roadmap_version 9 = operator-directed sync (2026-06-11) after 00-aprd QUESTION-GEN re-authored + clean-room both-directions verified (6/9). roadmap_version 10 = operator-directed sync (2026-06-11) after 00-aprd RECONCILE re-authored + clean-room both-directions verified (7/9). roadmap_version 11 = operator-directed sync (2026-06-11) after 00-aprd SYNTHESIZE re-authored + clean-room both-directions verified (8/9). roadmap_version 12 = operator-directed sync (2026-06-11) after 00-aprd VERIFY (LAST role, 9/9) re-authored + clean-room both-directions verified -> W6-REAUTHOR-REST CLOSED, moved to completed[]. DERIVED FRONTIER now = W7-ORCH-SWAP (orchestrator STEP-4.1 resolve+prefill+compute swap). VERIFY lesson: input SEEDED on disk -> field-NAME drift class N/A to input; my Step-1 full input-schema enumeration was re-spec (AB3, ECONOMY-AUDIT caught, trimmed to structure-only — same RECONCILE lesson). Genuine relocation = OUTPUT verification-block shape, landed clean (0 starvation). Planted-defect required corrupting ALL 3 deprecation homes (discriminator + Rule 3 + Rule 3.1) for a conclusive FAIL; defect output schema-VALID yet value-WRONG re-confirms value-parity-not-schema-validity catches it. DERIVED FRONTIER = W6-REAUTHOR-REST (in progress): wave sentinel now prompts/00-aprd/VERIFY.md (still pre-CR) -> wave not done; per-phase disk scan shows 03-hld + 02-adr + 01-roadmap re-authored (done) + 00-aprd 8/9 (CLASSIFIER/CRITIQUE/EXTRACT/EXTRACT-RULES/GAP-DETECT/QUESTION-GEN/RECONCILE/SYNTHESIZE), so the NEXT + LAST role = VERIFY. RECONCILE = SECOND markdown-vs-no lesson: its INPUT is seeded on disk, so the field-NAME drift class does NOT apply to inputs (runner reads real keys) — relocating output-SHAPE cues (kind enum, corroboration, reconcile_meta tallies) into steps 3/4/5 is the genuine home; a full input-schema enumeration in Step 1 is re-spec (AB3, ECONOMY-AUDIT caught it, trimmed). NEW DRIFT SUB-CLASS (latent-ambiguity-unmasked): a dropped inline schema can also remove a worked-example whose number was STALE (topics_reconciled:9 vs golden 10) AND whose surrounding cue was vague enough to let the runner re-derive the wrong value; fix = sharpen the cue's derivation source (count from INPUT rules[], not agreed[]), never re-add the schema/example. Both-directions tooling re-confirmed: a tier-precedence defect was INEFFECTIVE until ALL redundant homes ('tier 1>2>3' + 'executable canon is least-surprise default' + the 2 leads) were corrupted consistently. QUESTION-GEN is the FIRST markdown-output role re-authored (output 05-questions.md has NO registry schema): outputs[].schema=null, the inline ## Output template is KEPT as the sole structural home (no JSON ## Output schema to drop -> the example-loss/field-NAME drift class did not apply to output; proactively relocated the INPUT field-names into Task-step-1 prose since the dropped inputs: format-prose was their home). Drift class NOT triggered (clean first-pass, like GAP-DETECT). NEW LESSON (both-directions tooling, not a prompt drift): a value-defect planted in only Rule2+step3 was INEFFECTIVE because the ≤6 cap is restated in the ## Output template (3 redundant prose homes) and the runner honored the template; a conclusive planted-defect FAIL required corrupting the cap in ALL homes (cap=4 -> 4Q+9assumptions vs golden 6Q+7assumptions). W1b-REGISTRY-ENRICH shipped (schemas.lock v2: description on every property, element shapes tightened). The example-loss drift class surfaced during 03-hld (a dropped inline-schema EXAMPLE carried derivation value W1b's descriptions did not; relocate to prompt prose, never re-add a schema) is now applied proactively per phase. DRIFT CLASS (durable, integrated from ephemeral notes): dropping an inline `## Output schema` can lose either a derivation-value EXAMPLE or the SOLE prose-home of output field NAMES; both pass registry validation yet drift clean-room value-parity vs golden. Fix = relocate the cue (which-fields + derivation logic) into Task-steps prose; never re-add a schema, never assume registry descriptions substitute. Field-NAME variant verified on 00-aprd CLASSIFIER (runner invented SR-001/verbatim/wrong escape shape until prose named the keys + id format). OUTSTANDING: W6-REAUTHOR-04BUILD spot-check for the same drift (it shipped before the class was understood). roadmap_version 13 = operator-directed sync (2026-06-11) after W7-ORCH-SWAP shipped: orchestrator STEP-4.1 wired to resolve+prefill+compute+validate around the LLM fill (D25/D26), replacing parse-frontmatter. W7 moved to completed[]; DERIVED FRONTIER now = W9-ECON-RECONCILE (terminal economy sweep), then W8-PACK-GATE. W7 LESSON: this was a SPINE edit not a stochastic-island deliverable -> controller authored it directly (P3 'fix the spine once'), the RM11 'never hand-author the deliverable' carve-out applies (the deliverable in the normal loop is a role prompt; the orchestrator IS the control loop). Verify was the regression-guard (resolve/prefill/validate exercised end-to-end against real _fixtures both directions), not a single-role clean-room. The 2 inherited caveman-footer-dup (AB1) lint findings are baseline debt (the shipped orchestrator already carried them at the same lines) -> NOT a W7 regression, deferred to W9 per defer-not-chase; deleting either over-strips a load-bearing canonical-block reference (STEP-3 names the block IMPLEMENT authors against + RULES restates PR4 = two distinct homes, a probable lint heuristic mis-fire W9 should calibrate to routes_to:canon). roadmap_version 14 = operator-directed sync (2026-06-11) after W9-ECON-RECONCILE shipped + rescoped: lint 44/44 clean (C8 calibrated to exclude canon-bundle range-refs; C6 calibrated to count escapes-overlap only from FAILURE-exit lines — the prior W7-flagged _orchestrator C8 was indeed the mis-fire, now fixed at the checker) + _step-runner C4 'just' fixed via IMPLEMENT+clean-room + selftest 24-check both-directions proof. W9 moved to completed[]; DERIVED FRONTIER now = W10-ECON-SWEEP-FULL (full 44-prompt L2 reconcile), then W8-PACK-GATE (now depends on W10). W9 RESCOPE: Layer-2 audit on the 4 touched surfaced 15 INHERITED findings (0 from the W9 edit) -> operator chose close-W9 + roll-15-into-W10 over re-authoring content-untouched verified prompts (defer-not-chase + avoid W10 overlap). The 15 catalogued in .roadmap/09-economy-reconcile.json rolled_to.inherited_findings; ~half are defensible auditor mis-fires (dual-mode cross-ref / ADR-0010 emphasis / orientation header) so W10's FIRST job = calibrate _economy-audit Rule-2, THEN re-author genuine residue. PRIORITY: _step-runner I2 starvation (only UNDER-direction finding). roadmap_version 15 = operator-directed sync (2026-06-11) after W10-ECON-SWEEP-FULL shipped: full 44-prompt L2 sweep CLEAN (44/44) via _economy-audit Rule-2 calibration (5 sanctioned patterns, controller-direct P3/routes_to:canon) + 10 surface-preserving AB9 trims (the genuine residue); 11/15 W9-inherited findings were auditor mis-fires (sanctioned), 4 genuine + 7 new corpus findings re-authored. Operator-decided _step-runner I2: SANCTION condensed Register (AB7/8/9 = authoring canon, executor runtime register condensed-but-6-core-parts ok); guardrail still caught _step-runner's MISSING NOT/YES core part (genuine starvation -> restored). Both-directions PROOF holds (PLANTED dup BLOCKED, CONTROL pointer clean, calibration_discriminates:true); fresh-verifier re-audit clean; lint selftest 24/24 + corpus 44/44 unchanged. W10 moved to completed[]; DERIVED FRONTIER now = W8-PACK-GATE (the LAST unshipped build). roadmap_version 16 = operator-directed sync (2026-06-11) after W8-PACK-GATE shipped: tools/pack/pack.mjs GATE now runs ALL 14 tools/**/*.selftest.mjs (validator+resolver+graph-lint+det+emitters+economy-lint) + payload-prompt lint (5b ENABLED — corpus 44/44 clean post-W9/W10, so 'economy-clean in the tarball' is now mechanical); drain self-ref bootstrap fixed (pack skips its own dist/ sentinel — cannot gate on its own output). W8 moved to completed[]; remaining_sequence now EMPTY -> LOOP DRAINED, all unshipped prompt-builds + the pack gate built. done_sentinel re-pointed dist/adp-v5.tgz -> tools/pack/pack.mjs: the original was content-addressed (adp-v<git>+p<prompts8>.l<locks8>.tgz, NOT literal v5) AND dist/ is gitignored, so the durable committed discriminator = pack.mjs carrying the all-selftests+5b gate, not the regenerable tarball. W8 LESSON-1 (spine edit, W7/W9 precedent): controller authored directly (P3 'fix the spine once'), verify = both-directions regression-guard (4 proofs: clean pack ships / selftest-red HALTs 5a / prompt-violation HALTs 5b / manifest byte-deterministic), not a single-role clean-room. W8 LESSON-2 (discovery naming gotcha): economy-lint's selftest is bare-named selftest.mjs while det/io/emit use <name>.selftest.mjs -> a naive .endsWith('.selftest.mjs') SILENTLY skipped the both-directions discrimination proof (13 vs 14); guardrail = the count + a regex /(^|\\.)selftest\\.mjs$/ matching both conventions. 5c (self-host token grep) left DISABLED: trips on genuine un-remediated re-skin content (_step-runner.md canon ref + typescript.md x2) -> separate re-skin-remediation build, defer-not-chase. roadmap_version 17 = operator-directed CR-004 intake (2026-06-12, STEP-5 gate passed): brownfield-adopt feature — ADOPT role (prompts/00-aprd/ADOPT.md) + CLASSIFIER has_adp_artifacts guard + playbook grounding delta + brownfield-adopt oracle. 4 builds added to remaining_sequence (W11-W14); frontier = W11-ADOPT-ROLE. Q1-Q3 gate decisions: ADOPT = Phase-0 standalone, writes minimal aprd stub + inferred skeleton.lock + thin foundational .adr/ (ACs OPEN, NOT full reverse-eng); CLASSIFIER detects has_adp_artifacts=false -> HALT-with-guidance (NOT auto-chain); operator runs adopt first -> second normal run (two explicit invocations). Greenfield + prior brownfield flows unchanged. roadmap_version 18 = operator-directed CR-004 wave progress (2026-06-12): W12-CLASSIFIER-DELTA shipped (W12 moved to completed[]); remaining_sequence = W13-PLAYBOOK-GROUNDING + W14-BROWNFIELD-ORACLE. roadmap_version 19 = operator-directed W15-ORCH-SPAWN-FIX finalize (2026-06-12): CR-005 bugfix (3-home inline-verify prohibition in prompts/_orchestrator.md) operator-ACCEPT; W15 moved to completed[]; I6/I7/I8 deferred as rolled-forward findings (not folded into CR-005). DERIVED FRONTIER = W13-PLAYBOOK-GROUNDING. roadmap_version 20 = operator-directed CR-006 finalize (2026-06-12): CR-006 bugfix (STEP-6 on-accept scratch cleanup clause) operator-ACCEPT. NO new build id (CR-006 §Out-of-scope: 'no new build id needed, direct spine edit, P3 precedent W7/W9/W8/W15'). No completed[] row added — completed[] tracks build-ids that existed in remaining_sequence; CR-006 was never sequenced (pure loop-body edit, author-direct per P3, same as W7/W9/W8/W15). Version bump + note only = correct treatment. Consistency: W15 got a completed[] row because it WAS in remaining_sequence (position 18); CR-006 was not, so omitting the row preserves internal consistency. Stray _test_bench_cr006/ removed (CR-006 cleanup rule applied to its own delivery). DERIVED FRONTIER = W13-PLAYBOOK-GROUNDING (unchanged). roadmap_version 21 = operator-directed W13-PLAYBOOK-GROUNDING finalize (2026-06-12): CR-004 §C playbook has_adp_artifacts guard shipped — prompts/_playbooks/feature-add.md + prompts/_playbooks/bugfix.md promoted with has_adp_artifacts field (schema key + field binding both homes) + scratch cleanup (CR-006 rule applied). W13 moved to completed[]. DERIVED FRONTIER = W14-BROWNFIELD-ORACLE. roadmap_version 22 = operator-directed W14-BROWNFIELD-ORACLE finalize (2026-06-12): operator ACCEPT with deferred C8 lint-calibration finding. P3 spine build — _fixtures/brownfield-adopt/ (src/ + expected/ + defects/ + README.md) written in-place; done_sentinel = _fixtures/brownfield-adopt/README.md (on disk, confirmed). No prompt .md to promote (P3 precedent W8). Verify passed: both-directions discrimination confirmed across all 4 scenarios. C8 lint-block on expected/.aprd/aprd.frozen.md DEFERRED as lint mis-fire (repeated verbatim OPEN-tag across 5 ACs is sanctioned mandatory-verbatim-tag pattern per W9/W10 precedent; fixture oracle correct, linter not calibrated for this case; do NOT alter fixture). Deferred finding logged in coverage.deferred_findings. W14 moved to completed[]; remaining_sequence now EMPTY -> LOOP DRAINED, CR-004 brownfield-adopt wave complete. Scratch cleanup (CR-006 rule applied): _test_bench_defect/ + _test_bench_w13/ removed. roadmap_version 23 = operator-directed CR-007 intake (2026-06-12, gate Q1-Q3 answered): multi-stream workstreams feature — branch-per-workstream + auto-reconcile from main + enforce-non-master + new-stream dispatch path. 4 builds added to remaining_sequence (W16-W19); frontier = W16-ADR-0028. Branch feature/multi-stream-workstreams created off master. Q1-Q3 gate decisions: branch=feature/multi-stream-workstreams; scope=FULL CR cascade (aPRD CR->ADR->HLD->Build); reconcile=BOTH (auto-merge main at STEP 0 + enforce-non-master). New R-ids: R-MW-1 (branch isolation), R-MW-2 (auto-reconcile), R-MW-3 (enforce-non-master), R-MW-4 (new-stream dispatch). Existing greenfield + bugfix + brownfield-adopt flows unchanged. roadmap_version 24 = W16-ADR-0028 + W17-HLD-STREAM-COMPONENT shipped (2026-06-12); W18-ORCH-MULTISTREAM build complete + at STEP-5 gate. W16: ADR-0028 authored (D28, branch-per-workstream, local-only G1) + adr-index.json + adr.lock v8 (28 ADRs). W17: STREAM-MANAGER in components.json inline_orchestrator[] + step-0-precondition in contracts.json preconditions[] + skeleton.lock v7. W18: prompts/_orchestrator.md + MODES new-stream + STEP 0.0 enforce-non-master + STEP 0.1 auto-reconcile; L1 lint clean, L2 economy-audit clean (I1 duplicate-fact found+fixed — RULES local-only bullet deleted, fact owned in MODES), L3 both-directions PASS (FORWARD: all 4 properties present+correct; DEFECT: STEP 0.0 HALT removal detected). Pending operator gate (STEP 5 value/parity). DERIVED FRONTIER = W18-ORCH-MULTISTREAM (gate pending) -> W19-STREAM-ORACLE (after accept). roadmap_version 25 = operator-ACCEPT W18-ORCH-MULTISTREAM (2026-06-12): W18 moved to completed[]. W19-STREAM-ORACLE built + at STEP-5 gate. P3 spine build — _fixtures/multi-stream/ (src/ + expected/ + defects/ + README.md) written in-place; done_sentinel = _fixtures/multi-stream/README.md. Scratch cleanup: _scratch/ emptied (lint.json + economy-audit.json removed). L1 lint clean (README + all src/*.md). L3 both-directions: runner PASS (4 scenarios × 3 defects; all signals discriminating; README consistent). Verifier PASS (runner verdict confirmed; no-enforce-non-master step_0_0_halt: true→false; no-auto-reconcile step_0_1_halt: true→false; new-stream-auto-push remote_push_performed: false→true; brief.md fields present; defect invariants distinct). At STEP-5 value/parity gate — awaiting operator accept.",
  "completed": [
    {
      "position": 1,
      "id": "W0-CANON-CR",
      "name": "deterministic-machine canon CR (intake)",
      "status": "shipped",
      "unit": ".aprd/change-requests/CR-002.md + aprd.v5 + ADRs 0024-0027 + skeleton.lock v5 + all re-signed locks",
      "done_sentinel": ".aprd/change-requests/CR-002.md"
    },
    {
      "position": 2,
      "id": "W1-SCHEMA-REGISTRY",
      "name": "schema-registry + validate.mjs + selftest",
      "status": "shipped",
      "unit": "schemas/ (registry + _meta.json) + schemas.lock + tools/det/validate.mjs",
      "done_sentinel": "schemas/schemas.lock",
      "note": "v1 built THIN (0 description field-docs, loose element types — D24 §1 under-implementation); fixed by W1b."
    },
    {
      "position": 3,
      "id": "W2-IO-MANIFEST",
      "name": "io-manifest + resolve.mjs + selftest",
      "status": "shipped",
      "unit": "io/ (io-manifest.json) + io.lock + tools/io/resolve.mjs",
      "done_sentinel": "io/io.lock"
    },
    {
      "position": 4,
      "id": "W3-GRAPH-LINT",
      "name": "graph-lint: gen contracts.json, PR2 check",
      "status": "shipped",
      "unit": "tools/io/graph-lint.mjs + generated contracts.json",
      "done_sentinel": "tools/io/graph-lint.mjs"
    },
    {
      "position": 5,
      "id": "W4-DET-MODULES",
      "name": "det/ compute modules + selftests",
      "status": "shipped",
      "unit": "tools/det/{verdict,route,sequence,idgen,coverage,prefill}.mjs",
      "done_sentinel": "tools/det/verdict.mjs"
    },
    {
      "position": 6,
      "id": "W5-TIER1-EMITTERS",
      "name": "Tier-1 emitters; retire those prompts",
      "status": "shipped",
      "unit": "tools/det/emit/*.mjs + Tier-1 prompts retired (sentinel + components.json note kept)",
      "done_sentinel": "tools/det/emit/build-plan.mjs"
    },
    {
      "position": 7,
      "id": "W6-REAUTHOR-04BUILD",
      "name": "re-author 04-build prompts (drop schema+inputs)",
      "status": "shipped",
      "unit": "prompts/04-build/*.md re-authored against skeleton v5",
      "done_sentinel": "prompts/04-build/IMPLEMENT.md",
      "note": "Shipped + W1b-re-verified. OUTSTANDING: spot-check for the example-loss drift class (surfaced AFTER this shipped) — see memory w6-reauthor-04build-spotcheck-pending."
    },
    {
      "position": 8,
      "id": "W1b-REGISTRY-ENRICH",
      "name": "enrich schema registry to D24 fidelity + re-freeze",
      "status": "shipped",
      "unit": "schemas/**/*.schema.json (description per property + tightened element shapes) + schemas.lock re-signed v2 + validate.mjs selftest tightened",
      "done_sentinel": "schemas/schemas.lock",
      "note": "CR-003. Discriminator confirmed on disk: schemas.lock version v2, every property carries description, supersedes v1. Necessary but NOT sufficient for value-parity (descriptions != examples) — the example-loss drift class still required prompt-prose cue relocation per phase."
    },
    {
      "position": 9,
      "id": "W6-REAUTHOR-REST",
      "name": "re-author 03/02/01/00 prompts, per phase",
      "status": "shipped",
      "unit": "prompts/{03-hld,02-adr,01-roadmap,00-aprd}/*.md re-authored against skeleton v5 (no inputs:/inline-schema block, outputs[].schema id, clean-room value-parity both directions)",
      "done_sentinel": "prompts/00-aprd/VERIFY.md",
      "note": "Wave closed 2026-06-11 (roadmap_version 12). 03-hld (8 roles) + 02-adr (7) + 01-roadmap (7) + 00-aprd (9: CLASSIFIER/CRITIQUE/EXTRACT/EXTRACT-RULES/GAP-DETECT/QUESTION-GEN/RECONCILE/SYNTHESIZE/VERIFY) all re-authored + clean-room both-directions verified. VERIFY (LAST role; verified vs _fixtures/greenfield-canon/.aprd/03-grounding/rules-verified.json, both directions: known-good schema-VALID + EXACT value-parity — verify_meta byte-identical {current:6,deprecated:3,not-version-bound:5,flagged:3}, all agreed+conflict-pos statuses match, recommended_position unchanged, entries carried verbatim PASS; planted-defect = deprecation-detection suppressed in ALL 3 homes (discriminator deprecated-bullet + Rule 3 + Rule 3.1) -> semi/quotes/indent flipped to current -> status_counts {current:9,deprecated:0,flagged:0} MISMATCH FAIL): drift class NOT triggered — VERIFY's INPUT is SEEDED on disk so runner reads real keys; ECONOMY-AUDIT correctly flagged my Step-1 full input-schema enumeration as re-spec AB3 -> trimmed to input STRUCTURE only (same RECONCILE lesson). Genuine relocation home = OUTPUT verification-block shape {status,checked,finding,replacement,source_of_truth,confidence} + checked {tool,tool_version_pinned,setting} + 7 status_counts enum keys + top-level key-set, all landed clean (0 starvation). Both-directions KEYSTONE re-confirmed: defect output was schema-VALID (status enum permits current) yet value-WRONG -> value-parity-vs-golden, not schema-validity, catches it; defect required corrupting ALL 3 deprecation homes consistently. 8 inherited ECONOMY-AUDIT findings (I1-I4,I6-I9 byte-identical to pre-CR original) deferred W9."
    },
    {
      "position": 10,
      "id": "W7-ORCH-SWAP",
      "name": "orchestrator STEP-4.1 swap: resolve+prefill+compute",
      "status": "shipped",
      "unit": "prompts/_orchestrator.md STEP-4.1 = resolve+prefill+compute+validate around the LLM fill",
      "done_sentinel": "prompts/_orchestrator.md",
      "note": "Shipped 2026-06-11 (roadmap_version 13). Discriminator MATERIALIZED: STEP-4.1 now (1) resolve(role,state) via tools/io/resolve.mjs --json --root . REPLACES parse-declared-inputs (D25; prompt declares none) -> seed exactly resolved paths, when-predicate fires in orchestrator (clean-room intact); (2) runner gets prompt verbatim + flat path-hint path-grade injection; (3) prefill <schemaId> -> shell+holes, runner fills ONLY judgment holes, code owns verdict/route/ids/counts (FLAG-not-fix mechanical: shell has no fix field), Tier-1 role -> tools/det/emit/*.mjs whole-artifact NO runner; (4) validate.mjs then value-parity; both-directions + routing keystone preserved. 4 surgical edits: GIVEN (spine registered as existing mechanism, invariant #3), STEP 3 (prompt declares only outputs), STEP 4.1+ (the swap), SUBAGENT CONTRACT (resolved-inputs injection). Spine fix = controller owns control loop (P3), not a delegated deliverable. VERIFY (regression-guard, real tools vs _fixtures/greenfield-canon, role VERIFY): FORWARD resolve->seed->prefill(11 holes)->validate->value-parity = PASS; REVERSE schema-VALID/value-WRONG status_counts defect = validate.mjs VALID yet value-parity MISMATCH->FAIL (value catches what schema cannot, STEP-4.5 operational). Layer-1 lint: ZERO new findings; 2 caveman-footer-dup (AB1) identical to shipped baseline (load-bearing dual canonical-block refs) -> deferred W9 per defer-not-chase. Operator-accepted at value/parity gate."
    },
    {
      "position": 11,
      "id": "W9-ECON-RECONCILE",
      "name": "reconcile accumulated lint + ECONOMY-AUDIT findings (terminal economy sweep)",
      "status": "shipped",
      "unit": "lint 44/44 clean (C8+C6 lint calibrations + selftest regressions) + _step-runner C4 fixed (IMPLEMENT+clean-room) + both-directions proof; 15 inherited L2 findings catalogued -> W10",
      "done_sentinel": ".roadmap/09-economy-reconcile.json",
      "note": "Shipped 2026-06-11 (roadmap_version 14). Operator-RESCOPED: Layer-2 (ECONOMY-AUDIT) on the 4 touched prompts surfaced 15 INHERITED findings — ZERO from the W9 edit (the _step-runner:7 'just' drop audited clean). Resolving all = re-authoring 3 content-untouched shipped prompts + orchestrator, overlapping the deferred W10 full-sweep + W6/W7 regression risk -> operator chose CLOSE W9 + roll 15 into W10. Discriminator MATERIALIZED: 0 unresolved BLOCKING lint findings (44/44 clean after fixes), both-directions proof holds (selftest 24 checks: all 10 defect injectors still block + 2 calibration regressions + determinism; PLUS corpus demo — genuine C6 predicates-on-HALT + genuine C8 real-dup still BLOCK), 0 new L2 debt. 2 lint mis-fires fixed at the CHECKER (P3, analog routes_to:canon — fix checker not artifact): C8 now excludes canon-bundle range-refs (bundleRef 'PR1-PR4' / 'AB#..PR#' NAMES the canon set IMPLEMENT authors against, != a duplicated reminder); C6 now counts escapes-overlap ONLY from FAILURE-exit (HALT/write-nothing) lines, because a multi-exit role's mode/path/verdict success-exit vocabulary collides with mode-keyed escapes WITHOUT re-stating any guard PREDICATE (negated 'not-HALT' also excluded). Only genuine prompt debt = _step-runner C4 (1-word DELETE via IMPLEMENT; behavioral surface Hard-rules/Register/Final-reply byte-identical -> clean-room behavior provably unchanged). W9 LESSON: a 'terminal economy sweep' scoped to N touched prompts still inherits the full corpus-wide L2 backlog ON those N; the auditor over-flags dual-mode cross-refs + deliberate ADR-0010 emphasis + orientation headers (same false-positive CLASS as the ledger's MODEL-DATA Register-starvation) -> W10 must reconcile corpus-wide WITH _economy-audit Rule-2 calibration, NOT piecemeal re-author. The lint calibrations are the durable W9 deliverable; the prompt-debt reconcile is W10's."
    },
    {
      "position": 12,
      "id": "W10-ECON-SWEEP-FULL",
      "name": "full 44-prompt Layer-2 ECONOMY-AUDIT + reconcile inherited findings corpus-wide",
      "status": "shipped",
      "unit": "ECONOMY-AUDIT on all 44 prompts + 15 W9-inherited findings reconciled corpus-wide; _economy-audit Rule-2 calibrated (5 sanctioned patterns); 10 surface-preserving AB9 trims; both-directions proof holds",
      "done_sentinel": ".roadmap/10-economy-sweep-full.json",
      "note": "Shipped 2026-06-11 (roadmap_version 15). Discriminator MATERIALIZED: 44/44 L2 verdict:clean (.roadmap/10-economy-sweep-full.json audit_layer2). FIRST job = CALIBRATE _economy-audit.md Rule-2 (controller-direct, P3/routes_to:canon — analog of W9 lint C6/C8): 5 sanctioned patterns (a dual-mode delta / b named cross-ref pointer / c deliberate multi-home emphasis / d orientation-provenance header / e condensed Register conformant IFF all 6 core parts verbatim), each w/ its genuine-defect exception. Operator-decided PRIORITY (_step-runner I2): SANCTION condensed Register — AB7/8/9 fix-lines + economy sentence are AUTHORING canon mandated ONLY where prose authored/audited (IMPLEMENT, ECONOMY-AUDIT); condensed = accepted RUNTIME register. Of the 15 W9-inherited: 11 sanctioned (auditor mis-fires), 4 genuine; +7 new findings from the 40 previously-unaudited prompts. 10 prompts re-authored as surface-preserving AB9 trims (executable surface byte-identical -> behavior provably unchanged, W9 C4 precedent): SLICE-EXTRACT(no-objective DELETE), _orchestrator(dup->pointer + retry-budget disambig), _orchestrator.generic(retry-budget), _step-runner(restore NOT/YES core part — the guardrail firing, NOT the full authoring block), IMPLEMENT(restore full block, named prose-author pattern-e), 5x 02-adr(CRITIQUE/EVALUATE-DECIDE/OPTION-GEN/RECONCILE/SYNTHESIZE-ADR mandate-narration role-decoration trim). VERIFY: FRESH L2 verifier (separate spawn, no self-grade) re-audited the 10 -> all clean, no new defect; both-directions PROOF holds (PLANTED verbatim-4th-home dup -> BLOCKED duplicate-fact; CONTROL named pointer -> clean; calibration_discriminates:true); lint selftest 24/24 (W9 C6/C8 regressions + 10 injectors still block); corpus lint 44/44 unchanged. NEVER ADD to silence a finding (AB9 keystone). W10 LESSON: the auditor over-flags dual-mode cross-refs + deliberate-emphasis + orientation headers (~half of W9's 15 + the 02-adr role-context sentences) — calibrating Rule-2 at the CHECKER (not re-authoring 40 verified prompts) is the durable fix; the 02-adr mandate-narration genuine residue was the SEPARATE positional/prior-stage sentences (delete-test fails), distinct from the tight 'one load-bearing thing' slot that 03-hld/04-build roles use (sanctioned). DERIVED FRONTIER now = W8-PACK-GATE (LAST remaining_sequence entry)."
    },
    {
      "position": 13,
      "id": "W8-PACK-GATE",
      "name": "pack gate: wire all selftests, ship",
      "status": "shipped",
      "unit": "tools/pack/pack.mjs GATE runs all 14 tools/**/*.selftest.mjs + payload-prompt lint (5b); drain self-ref fixed; dist/adp-v<ver>.tgz reproducible",
      "done_sentinel": "tools/pack/pack.mjs",
      "note": "Shipped 2026-06-11 (roadmap_version 16). LAST build -> LOOP DRAINED. Discriminator MATERIALIZED: pack.mjs GATE step-5a discovers+runs ALL tools/**/*.selftest.mjs (14: validator+resolver+graph-lint+6 det+4 emit+economy-lint), first red -> HALT no tarball (retires spine-untested-in-dist); step-5b payload-prompt lint ENABLED (41/41 clean, blocked -> HALT). Drain bootstrap fixed: step-1 skips dist/-rooted sentinels (pack cannot gate on its OWN output). done_sentinel re-pointed dist/adp-v5.tgz -> tools/pack/pack.mjs: original was content-addressed (NOT literal v5) AND dist/ gitignored, so durable committed discriminator = pack.mjs gate, not the regenerable tarball. Both-directions x4 PROVEN: clean pack ships (exit 0); economy-lint selftest forced-red -> HALT 5a no tarball; planted register-compliance violation in payload prompt -> HALT 5b naming RE-RANK.md no tarball; manifest byte-deterministic across runs. Spine edit (W7/W9 precedent): controller authored directly (P3), verify = both-directions regression-guard not single-role clean-room. LESSON: economy-lint selftest is bare-named selftest.mjs vs det/io/emit <name>.selftest.mjs -> naive .endsWith silently skipped the discrimination proof (13 vs 14), fixed w/ regex matching both + count guardrail. 5c self-host token grep left DISABLED (trips on genuine un-remediated re-skin content _step-runner.md + typescript.md x2 -> separate re-skin build). Tarball gitignored (reproducible on clean tree, byte-identical manifest); shipped state = pack.mjs + regenerated manifest.json committed."
    },
    {
      "position": 14,
      "id": "W11-ADOPT-ROLE",
      "name": "Author ADOPT role prompt (brownfield intake)",
      "status": "shipped",
      "unit": "prompts/00-aprd/ADOPT.md",
      "done_sentinel": "prompts/00-aprd/ADOPT.md",
      "cr": "CR-004",
      "class": "feature-add",
      "note": "Shipped 2026-06-12 (roadmap_version 17). ADOPT role: standalone brownfield-bootstrap dispatch; writes .aprd/aprd.frozen.md stub + .hld/skeleton.lock stub + .adr/log/ ADR stubs from existing code. Guards: trees-already-present -> HALT; no-source -> HALT. All ACs OPEN-tagged (Rule 2). No .roadmap/ write (RE-RANK bootstraps on first normal run). Operator-accepted pre-gate (on-disk sentinel present, untracked)."
    },
    {
      "position": 15,
      "id": "W12-CLASSIFIER-DELTA",
      "name": "CLASSIFIER has_adp_artifacts guard + HALT-with-guidance",
      "status": "shipped",
      "unit": "prompts/00-aprd/CLASSIFIER.md delta: has_adp_artifacts=false -> HALT-with-guidance",
      "done_sentinel": "prompts/00-aprd/CLASSIFIER.md",
      "depends_on": ["W11-ADOPT-ROLE"],
      "cr": "CR-004",
      "class": "feature-add",
      "note": "Shipped 2026-06-12. Discriminator MATERIALIZED: Rule 2 'Check ADP foundation first' (before reading request), Task step 1 'Check ADP foundation (Rule 2)', escape entry 'has_adp_artifacts == false ... HALT-with-guidance', Stop condition 'ADP foundation absent (first escape) -> write nothing; emit guidance; state HALT: run adopt first; stop'. All 4 homes consistent. Gate ACCEPT. Scratch _scratch/CLASSIFIER.md promoted -> prompts/00-aprd/CLASSIFIER.md + payload/prompts/00-aprd/CLASSIFIER.md synced. L1 lint + L2 economy-audit + L3 clean-room both-directions passed pre-gate."
    },
    {
      "position": 16,
      "id": "W13-PLAYBOOK-GROUNDING",
      "name": "feature-add + bugfix playbook has_adp_artifacts pre-check",
      "status": "shipped",
      "unit": "prompts/_playbooks/feature-add.md + prompts/_playbooks/bugfix.md: has_adp_artifacts guard",
      "done_sentinel": "prompts/_playbooks/feature-add.md",
      "depends_on": ["W12-CLASSIFIER-DELTA"],
      "cr": "CR-004",
      "class": "feature-add",
      "note": "Shipped 2026-06-12 (roadmap_version 21). Operator ACCEPT at W13-PLAYBOOK-GROUNDING gate (CR-004 §C). Discriminator MATERIALIZED: has_adp_artifacts:true field in schema instance (both playbooks) + has_adp_artifacts field binding prose in Field bindings section (both playbooks) — 2 homes each, consistent. Scratch _scratch/feature-add.md + _scratch/bugfix.md promoted -> prompts/_playbooks/feature-add.md + prompts/_playbooks/bugfix.md. Scratch cleanup (CR-006 rule): lint.json + economy-audit.json + scratch .md removed. L1 lint + L2 economy-audit + L3 clean-room both-directions passed pre-gate (3 verify layers). DERIVED FRONTIER = W14-BROWNFIELD-ORACLE."
    },
    {
      "position": 17,
      "id": "W14-BROWNFIELD-ORACLE",
      "name": "brownfield-adopt oracle + both-directions e2e",
      "status": "shipped",
      "unit": "_fixtures/brownfield-adopt/ (src/ + expected/ + defects/ + README.md)",
      "done_sentinel": "_fixtures/brownfield-adopt/README.md",
      "depends_on": ["W11-ADOPT-ROLE", "W12-CLASSIFIER-DELTA", "W13-PLAYBOOK-GROUNDING"],
      "cr": "CR-004",
      "class": "feature-add",
      "note": "Shipped 2026-06-12 (roadmap_version 22). P3 spine build — fixture oracle written in-place, no prompt .md to promote (W8 precedent). Operator ACCEPT with one deferred finding: C8 lint-block on expected/.aprd/aprd.frozen.md (repeated verbatim OPEN-tag across 5 ACs) is a lint mis-fire vs sanctioned mandatory-verbatim-tag pattern (W9/W10 precedent); fixture oracle correct; linter needs calibration, not the fixture. Deferred finding logged in coverage.deferred_findings. Both-directions discrimination confirmed across all 4 scenarios. Scratch cleanup (CR-006 rule): _test_bench_defect/ + _test_bench_w13/ removed. LOOP DRAINED — CR-004 brownfield-adopt wave complete; remaining_sequence now empty."
    },
    {
      "position": 18,
      "id": "W15-ORCH-SPAWN-FIX",
      "name": "orchestrator inline-verify prohibition (CR-005 bugfix)",
      "status": "shipped",
      "unit": "prompts/_orchestrator.md — 3-home inline-verify prohibition (STEP 4.1.2 + STEP 4.1.4 + RULES)",
      "done_sentinel": "prompts/_orchestrator.md",
      "cr": "CR-005",
      "class": "bugfix",
      "note": "Shipped 2026-06-12 (roadmap_version 19). Operator ACCEPT at value/parity gate. 3 homes: (1) STEP 4.1.2 spawn-runner '(MUST be a separate spawn — not inline)'; (2) STEP 4.1.4 'Verifier MUST be a different spawn than the runner'; (3) RULES 'NEVER run Layer-3 verify inline. Runner + verifier MUST each be a step-runner subagent spawn (STEP 4.1). Inline execution = broken clean-room + context bloat. No exception.' L1 lint clean, L2 economy-audit clean, L3 both-directions PASS. CR-005 §Fix-scope corrected from 2 homes to 3 (STEP 4.1.2 covers runner procedure, STEP 4.1.4 covers verifier procedure, RULES is unconditional guard — each slot distinct). I6/I7/I8 DEFERRED as pre-existing inherited findings (roll forward, not folded into CR-005; same defer-not-chase as W9->W10). Spine edit (W7/W9/W8 precedent) — controller authored directly (P3); done_sentinel = prompts/_orchestrator.md (loop body IS the deliverable home; no scratch promotion needed for spine edits)."
    },
    {
      "position": 21,
      "id": "W18-ORCH-MULTISTREAM",
      "name": "orchestrator STEP 0.0 + STEP 0.1 + new-stream dispatch",
      "status": "shipped",
      "unit": "prompts/_orchestrator.md: STEP 0.0 enforce-non-master + STEP 0.1 auto-reconcile + new-stream dispatch arg path",
      "done_sentinel": "prompts/_orchestrator.md",
      "depends_on": ["W16-ADR-0028", "W17-HLD-STREAM-COMPONENT"],
      "cr": "CR-007",
      "class": "feature-add",
      "note": "Shipped 2026-06-12 (roadmap_version 25). Operator ACCEPT at value/parity gate. Spine edit (P3 precedent W7/W9/W8/W15/CR-005/CR-006). Delta: STEP 0.0 enforce-non-master (R-MW-3) + STEP 0.1 auto-reconcile (R-MW-2) + new-stream MODES dispatch (R-MW-4, G1 local-only). L1 lint clean, L2 economy-audit clean (I1 duplicate-fact found+fixed: RULES local-only bullet deleted, fact owned in MODES). L3 both-directions PASS: FORWARD all 4 properties present+correct; DEFECT STEP 0.0 HALT removal detected. W18 scratch cleanup: _scratch/lint.json + economy-audit.json removed (CR-006 rule applied)."
    },
    {
      "position": 19,
      "id": "W16-ADR-0028",
      "name": "Author ADR-0028 (branch-isolation decision)",
      "status": "shipped",
      "unit": ".adr/log/0028-branch-per-workstream.md + adr-index.json + adr.lock v8",
      "done_sentinel": ".adr/log/0028-branch-per-workstream.md",
      "cr": "CR-007",
      "class": "feature-add",
      "note": "Shipped 2026-06-12 (roadmap_version 24). ADR-0028 (D28): branch-per-workstream — branch isolation (R-MW-1), auto-reconcile merge --ff-only (R-MW-2), enforce-non-master HALT (R-MW-3), new-stream local-only (R-MW-4, G1: no auto-push). adr-index.json D1-D28. adr.lock v8 re-signed. Controller-direct P3."
    },
    {
      "position": 20,
      "id": "W17-HLD-STREAM-COMPONENT",
      "name": "HLD components.json + contracts.json delta (STREAM-MANAGER)",
      "status": "shipped",
      "unit": ".hld/skeleton/components.json + .hld/skeleton/contracts.json + skeleton.lock v7",
      "done_sentinel": ".hld/skeleton/components.json",
      "depends_on": ["W16-ADR-0028"],
      "cr": "CR-007",
      "class": "feature-add",
      "note": "Shipped 2026-06-12 (roadmap_version 24). STREAM-MANAGER in components.json inline_orchestrator[] (D28, no prompt, logic inline STEP 0.0/0.1/new-stream, _streams/<slug>/brief.md sentinel). step-0-precondition in contracts.json preconditions[]. skeleton.lock v7 re-signed. Controller-direct P3."
    }
  ],
  "remaining_sequence": [
    {
      "position": 22,
      "id": "W19-STREAM-ORACLE",
      "name": "multi-stream oracle + both-directions e2e",
      "status": "at-gate",
      "unit": "_fixtures/multi-stream/ (src/ + expected/ + defects/ + README.md)",
      "done_sentinel": "_fixtures/multi-stream/README.md",
      "depends_on": ["W16-ADR-0028", "W17-HLD-STREAM-COMPONENT", "W18-ORCH-MULTISTREAM"],
      "cr": "CR-007",
      "class": "feature-add",
      "note": "P3 spine build (W14-BROWNFIELD-ORACLE precedent). Built 2026-06-12. 4 scenarios: (1) clean-branch — STEP 0.0 pass + STEP 0.1 clean merge + frontier-scan proceeds; (2) master-halt — STEP 0.0 HALT + in-flight branches listed; (3) new-stream — branch + brief.md + STOP + no push (G1); (4) conflict-halt — STEP 0.1 HALT on non-fast-forward. 3 defects: no-enforce-non-master (STEP 0.0 removed → master proceeds silently); no-auto-reconcile (STEP 0.1 removed → conflict undetected); new-stream-auto-push (push added → G1 violated). L1 lint clean (README + src/*.md). L3 both-directions: runner PASS + verifier PASS (confirmed; all 3 defects discriminate). At STEP-5 value/parity gate — awaiting operator accept."
    }
  ],
  "dependency_check": {
    "acyclic": true,
    "legal": true,
    "against": "build_order",
    "cycles": [],
    "dangling_real_depends_on": []
  },
  "coverage": {
    "scope_note": "Scoped to CR-002 deterministic-machine wave + CR-003 registry-fidelity fix + CR-004 brownfield-adopt feature + CR-005 orchestrator spawn-fix bugfix + CR-007 multi-stream workstreams feature. Prior delivered waves (greenfield, bugfix) not tracked here; done-state on disk (D20).",
    "completed": [
      "W0-CANON-CR",
      "W1-SCHEMA-REGISTRY",
      "W2-IO-MANIFEST",
      "W3-GRAPH-LINT",
      "W4-DET-MODULES",
      "W5-TIER1-EMITTERS",
      "W6-REAUTHOR-04BUILD",
      "W1b-REGISTRY-ENRICH",
      "W6-REAUTHOR-REST",
      "W7-ORCH-SWAP",
      "W9-ECON-RECONCILE",
      "W10-ECON-SWEEP-FULL",
      "W8-PACK-GATE",
      "W11-ADOPT-ROLE",
      "W12-CLASSIFIER-DELTA",
      "W15-ORCH-SPAWN-FIX",
      "W13-PLAYBOOK-GROUNDING",
      "W14-BROWNFIELD-ORACLE",
      "W16-ADR-0028",
      "W17-HLD-STREAM-COMPONENT",
      "W18-ORCH-MULTISTREAM"
    ],
    "remaining_ranked": ["W19-STREAM-ORACLE"],
    "deferred_findings": [
      {
        "id": "I6",
        "source": "W15-ORCH-SPAWN-FIX L2 economy-audit",
        "status": "deferred",
        "note": "Pre-existing inherited finding. Rolled forward per defer-not-chase (same as W9->W10). Not folded into CR-005."
      },
      {
        "id": "I7",
        "source": "W15-ORCH-SPAWN-FIX L2 economy-audit",
        "status": "deferred",
        "note": "Pre-existing inherited finding. Rolled forward per defer-not-chase."
      },
      {
        "id": "I8",
        "source": "W15-ORCH-SPAWN-FIX L2 economy-audit",
        "status": "deferred",
        "note": "Pre-existing inherited finding. Rolled forward per defer-not-chase."
      },
      {
        "id": "C8-BROWNFIELD-ORACLE",
        "source": "W14-BROWNFIELD-ORACLE L1 lint",
        "artifact": "_fixtures/brownfield-adopt/expected/.aprd/aprd.frozen.md",
        "status": "deferred",
        "note": "Lint C8 flags repeated verbatim OPEN-tag across 5 ACs in the fixture. Operator-directed: mis-fire vs sanctioned mandatory-verbatim-tag pattern (W9/W10 precedent). Fixture oracle correct; do NOT alter fixture. Linter needs calibration for this pattern — treat as lint-calibration backlog. Route: next lint-calibration pass (P3, same as W9 C6/C8 fixes)."
      }
    ],
    "in_progress_note": "CR-007 multi-stream wave (2026-06-12). W16-ADR-0028 + W17-HLD-STREAM-COMPONENT + W18-ORCH-MULTISTREAM shipped. W19-STREAM-ORACLE at STEP-5 value/parity gate. I6/I7/I8 + C8-BROWNFIELD-ORACLE deferred (unchanged).",
    "missing": [],
    "duplicated": []
  },
  "rerank_counts": {
    "completed": 21,
    "remaining": 1,
    "reordered": 0,
    "deps_removed": 0,
    "deps_added": 1
  }
}
