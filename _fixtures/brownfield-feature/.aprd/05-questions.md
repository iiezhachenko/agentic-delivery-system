# Gap questions (feature-add CR-001)

> 3 gaps disposition `ask` (04-gaps). Asked client; no response received → defaults applied (07-assumptions, source `default-applied`). Recommended defaults below.

- **G1** (architecture): Tag = free-text field on existing project record (extend CT2, no new table), OR first-class Tag entity with own table + relation? **Default:** free-text field on project record, extend CT2.
- **G2** (scope): One label per project, OR multiple tags? **Default:** exactly one label (CR wording singular).
- **G3** (scope): Filter by single tag, OR multi-tag AND/OR? **Default:** single tag at a time.
