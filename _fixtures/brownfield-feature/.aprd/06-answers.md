# Gap answers (feature-add CR-001)

> No client response within window. Recommended defaults applied (07-assumptions source `default-applied`). Each gap → assumption A14–A16.

- **G1** → no answer → default: free-text label field on project record, extend CT2 (A14).
- **G2** → no answer → default: one label per project (A15).
- **G3** → no answer → default: single-tag filter (A16).
