# Gap questions (bugfix CR-bug-001)

> 1 gap disposition `ask` (04-gaps). Asked client; no response received → default applied (07-assumptions, source `default-applied`). Recommended default below.

- **G1** (scope): How should a null billable_rate render in the project list — blank placeholder ('—'), zero ('0.00'), or omit the rate field? **Default:** blank placeholder ('—'), no numeric format applied to a null value.
