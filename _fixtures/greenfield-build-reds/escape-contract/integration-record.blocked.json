{
  "build_record_ref": ".build/skeleton/build-record.json", "flows_ref": ".hld/skeleton/flows.json",
  "oracle_lock_ref": ".build/skeleton/oracle/oracle.lock", "oracle_ref": ".build/skeleton/oracle/oracle.json",
  "contracts_ref": ".hld/skeleton/contracts.json", "components_ref": ".hld/skeleton/components.json",
  "skeleton_lock_ref": ".hld/skeleton.lock", "adr_lock_ref": ".adr/adr.lock", "aprd_lock_ref": ".aprd/aprd.lock",
  "locks_verified": true, "class": "greenfield", "mode": "skeleton-build", "slice": "S1", "flow": "F1",
  "walking_skeleton_path": ["C6", "C2", "C1"], "status": "blocked",
  "escape": {
    "failure_signature": "test_f1_happy_path_oauth_login_establishes_session: flow cannot compose — CT1 (C2->C1) declares kind:async_event requiring a message broker to deliver the identity-write event, but no broker is wired; the OAuth callback has no synchronous acknowledgement to establish the session on.",
    "classification": "my-code",
    "diagnosis": "Wiring incomplete: need to stand up a message broker and an async consumer so the identity write event is delivered.", "route": "IMPLEMENT",
    "attempts": [
      { "signature": "flow compose error: CT1 async_event has no broker", "net_new_passes": 0 },
      { "signature": "flow compose error: CT1 async_event has no broker", "net_new_passes": 0 },
      { "signature": "flow compose error: CT1 async_event has no broker", "net_new_passes": 0 }
    ]
  },
  "verification": { "flow": "fail", "method": "static-trace", "contract": "pass-inherited", "acceptance": "not-run" },
  "provenance": { "integrator_role": "integrator", "built_against": { "oracle_lock": ".build/skeleton/oracle/oracle.lock", "skeleton_lock": ".hld/skeleton.lock", "adr_lock": ".adr/adr.lock", "aprd_lock": ".aprd/aprd.lock", "build_plan": ".build/skeleton/build-plan.json", "build_record": ".build/skeleton/build-record.json" } },
  "commits": []
}
