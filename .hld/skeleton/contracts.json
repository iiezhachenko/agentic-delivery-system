{
  "artifact": "contracts.json",
  "class": "self-host",
  "note": "Producer/consumer chain (PR2): each prompt writes the exact place+format the next reads; output schema of step N == input schema of step N+1. IDs thread R→AC→S→ADR→C→CT→F→commit. Precondition contracts (STREAM-MANAGER, D28/CR-007): STEP 0 entry enforces branch isolation before any frontier work begins.",
  "rule": "PR2",
  "preconditions": [
    {
      "id": "step-0-precondition",
      "enforcer": "STREAM-MANAGER (orchestrator STEP 0.0 + STEP 0.1)",
      "adr": "ADR-0028",
      "cr": "CR-007",
      "contract": "branch != master (OR no in-flight feature/* bugfix/* branches) AND reconcile_from_main == clean | HALT",
      "consumers": ["STEP 0 frontier-scan", "all subsequent STEP 0–6"],
      "violation_response": "HALT — list in-flight branches (R-MW-3) OR report merge conflict (R-MW-2); do not proceed"
    }
  ],
  "chain": [
    {
      "from": "00-aprd",
      "to": "01-roadmap",
      "contract": "00-aprd.outputs == 01-roadmap.inputs"
    },
    {
      "from": "01-roadmap",
      "to": "02-adr",
      "contract": "01-roadmap.outputs == 02-adr.inputs"
    },
    {
      "from": "02-adr",
      "to": "03-hld",
      "contract": "02-adr.outputs == 03-hld.inputs"
    },
    {
      "from": "03-hld",
      "to": "04-build",
      "contract": "03-hld.outputs == 04-build.inputs"
    },
    {
      "from": "04-build",
      "to": null,
      "contract": "terminal (accepted staging demo)"
    }
  ]
}
