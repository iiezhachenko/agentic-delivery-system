# M11.14 — CLOSING ACT: delete `_self-host-migration/` + whole-tree acceptance — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` (this is the part that DELETES it). Part **14 of 14** of M11 — **runs LAST, after M11.1–M11.13 ALL cleared**. Deleting the migration workdir is the migration's final, self-referential act. **NO COMMIT** (the dir is recoverable from the `pre-self-host` tag + git history — that is by design).

## Goal

Tree is fully caveman + canonical after M11.1–M11.13. Now (1) run the whole-tree acceptance that proves M11 (and the cleanup band) is complete, then (2) delete `_self-host-migration/` entirely — `migration-spec.md` + every `M0–M14` task file. The migration leaves no trace.

## Whole-tree acceptance — run BEFORE the delete

The "kept tree" = `.aprd .adr .hld .roadmap prompts code-canon docs .claude .kiro CLAUDE.md _fixtures .gitignore` (EXCLUDE `_test_bench/` = gitignored sandbox, and `_self-host-migration/` = about to be deleted).

### Check 1 — deleted-source path refs = ZERO
```
grep -rE '_decisions\.md|_rules\.md|_initial_design' <kept tree>   →  0 hits
```
(These were swept by M11.1 in `.hld`/`.claude`, by M11.2 across `.adr` bodies + `adr-index.json` + `adr.lock` `source` values, by M11.4 in `aprd.frozen.md`. If any hit remains, route back to the owning part.)

### Check 2 — migration vocabulary = ZERO (already true since M9; re-confirm)
```
grep -rE '_self/|_self-host-migration|migration-spec|_tracker\.md|_changelog\.md|_prompt-run\.md|freeze\.mjs|parity gate|bootstrap|seed-from-frozen|D-[1-6]\b|cutover' <kept tree>  → 0
```
**Carve-outs (legit, keep):** NFR `id: M1` and other `M*` NFR ids; domain `migration`/`parity`/`migration-guide` (project class + test type + doc type); engine `frozen`/`freeze the requirements`/`re-freeze upstream`/`skeleton`/`self-host` (the reflexive concept). The `M0`–`M14` milestone tokens must be ZERO outside those carve-outs.

### Check 3 — caveman register everywhere
- Register canon states the no-carve-out mandate in all 3 homes: `.hld/skeleton/coding-canon.md`, every `prompts/**/*.md` embedded `# Register` block, `CLAUDE.md` Register §. The OLD `Exception: artifact content … Caveman governs narration, not the deliverable` line returns ZERO hits across the tree.
- Spot-check: open ~5 rewritten files across `.adr`, `.aprd/specs`, `prompts`, `docs` — prose terse, no fluff/hedging/articles-as-filler; code comments caveman; structural data (JSON/YAML, schemas, ids, fenced code) intact.

### Check 4 — locks recompute == lock (all 3, sealed v3)
Recompute in Bash with the exact algorithm; compare to each `.lock` `content_sha256`:
- `aprd  = sha256( .aprd/aprd.frozen.md )`  vs `.aprd/aprd.lock` (`version v3`).
- `adr   = sha256( concat sorted(.adr/log/*.md) by name )`  vs `.adr/adr.lock` (`version v3`, `adr_count 21`).
- `skel  = sha256( concat sorted(.hld/skeleton/*) by name + .hld/skeleton.frozen.md )`  vs `.hld/skeleton.lock` (`version v3`).
All three must match (recompute == lock).

### Check 5 — behavior preserved
RE-RANK over `.roadmap/08-rerank.json` + `_fixtures/` sentinels still names next-unshipped **`P-BUILD-PLAN-SLICE`** (completed sentinels: `P-DERIVE-TESTS-INC`, `P-RECONCILE-CRITIQUE-INC`). All prose/comment edits were behavior-preserving.

### Check 6 — root = canonical members only
`ls` root shows exactly: `.adr .aprd .claude .git .gitignore .hld .kiro .roadmap CLAUDE.md _fixtures code-canon docs prompts` + `_test_bench/` (gitignored sandbox) + `_self-host-migration/` (deleted in T2 below). No stray.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T0 | Confirm M11.1–M11.13 ALL cleared | every prior part's acceptance MET | ☑ all 13 = 0 open boxes |
| T1 | Run Checks 1–6 above | all pass (0/0/clean/3×match/`P-BUILD-PLAN-SLICE`/canonical) | ☑ pass (Check 3 needed 2 fixes — see note) |
| T2 | **Delete `_self-host-migration/` entirely** (the closing act) — `migration-spec.md` + all `M0–M14*-tasks.md` + the relocated-doc originals if any remain | dir does not exist | ⏸ DEFERRED — wording wipes self-contained `M12-tasks.md`; M12 not done; delete = absolute final act AFTER M12 |
| T3 | Re-`ls` root | canonical members only; `_self-host-migration/` gone; `_test_bench/` remains (gitignored) | ⏸ DEFERRED — blocked on T2 (also `_authoring-improvements/` stray present, out of M11.14 scope) |

## M11 acceptance (the whole phase) — MET when

- [x] register canon flipped, caveman everywhere, no artifact carve-out (M11.1 + M11.6–M11.13; Check 3) — completed after sweeping 2 missed engine files (`_orchestrator.md`, `_step-runner.md`)
- [x] every survivor file (excl. `_test_bench/`) caveman — prose + comments; structural data untouched (M11.1–M11.13; Check 3)
- [x] deleted-source rogue refs swept; grep = 0 (M11.1/M11.2/M11.4; Check 1)
- [x] all 3 frozen artifacts re-locked, recompute == lock, `v3` (M11.1/M11.2/M11.4; Check 4)
- [x] RE-RANK names `P-BUILD-PLAN-SLICE` (Check 5)
- [ ] `_self-host-migration/` does not exist (T2) — DEFERRED until M12 done (delete = absolute final act)

## Done-checklist lines (whole M11)

```
M11 [x] register canon flipped: caveman everywhere, no artifact carve-out
    [x] every survivor file (excl. _test_bench/) rewritten to caveman — prose AND code comments; structural data untouched
    [x] deleted-source rogue refs swept; frozen artifacts touched re-locked (recompute==lock); RE-RANK still names next
    [ ] _self-host-migration/ deleted (closing act) — spec + all M-task files gone  (DEFERRED: runs after M12; must spare self-contained M12-tasks.md)
```

## Notes

- **NO COMMIT.** The deletion is recoverable from the `pre-self-host` tag + git history (by design — the spec was always destined to delete its own home).
- After M11.14 only **M12** remains (full validation: structural conformance + a second canon profile through the unchanged spine).
- Caveman governs this file too.

### Run log (2026-06-08)
- **T0/T1 MET.** Checks 1/2/4/5/6 passed first run. Check 4 = 3×match, `v3`, `adr_count 21`. Check 5 = `P-BUILD-PLAN-SLICE`. Check 1 = 0. Check 2 = 0 (lone `"id": "M1"` = NFR carve-out, legit).
- **Check 3 gap found + fixed.** M11.6–M11.13 swept deliverable prompts but missed 2 engine files still carrying OLD `Exception: artifact content … Caveman governs narration, not the deliverable`: `prompts/_orchestrator.md:13`, `prompts/_step-runner.md:21`. Replaced both with canonical no-carve-out line. Prose-only, non-hashed `prompts/` tree → Checks 4/5 unaffected. Check 3 OLD-line count now 0.
- **T2/T3 DEFERRED — spec self-conflict.** T2 says delete "all `M0–M14*-tasks.md`" but the note + `M12-tasks.md`'s own precondition put M12 AFTER M11.14, and M12 is SELF-CONTAINED in its task file. Deleting it pre-M12 = lose the next milestone's only input. Resolution: delete is the **absolute final act**, run AFTER M12 clears; it must spare `M12-tasks.md` until then (or M11.14 re-runs post-M12 to wipe everything). Premature delete was performed then **reverted** (uncommitted `git restore`); dir intact.
- **Stray vs Check 6.** `_authoring-improvements/` present in root — tracked (commit `1d01f92`), separate workstream, NOT a migration artifact, out of M11.14 scope. Left untouched; flag for its owner.
