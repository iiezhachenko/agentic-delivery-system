# M11.2 — `.adr` decision bodies: caveman + rogue-sweep + re-lock — tasks (SELF-CONTAINED)

> **Self-contained:** all context is in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **2 of 14** of M11. **Precondition: M11.1 cleared** (register canon flipped). Parallel-safe with M11.3–M11.13 (disjoint files+locks). **NO COMMIT.**

## Scope — the decisions tree

Caveman-rewrite the 21 ADR bodies + their drafts, sweep all dead-source path refs (the heaviest concentration: `_decisions.md`×92, `_rules.md`×32 across `.adr`), and re-seal `adr.lock`.

| Target | Action | Lock? |
|---|---|---|
| `.adr/log/0001…0021*.md` (21 bodies) | caveman rewrite + dead-ref sweep | **hashed by `adr.lock`** → re-lock |
| `.adr/drafts/0001…0021*.draft.md` (21) | caveman rewrite + dead-ref sweep | NOT hashed → no re-lock |
| `.adr/adr-index.json` | **structural untouched** EXCEPT dead-path tokens in values (see below) | NOT hashed → free edit |
| `.adr/adr.lock` | dead-path tokens in `adrs[].source` values + **re-lock** | self not hashed |

## CONTRACT A — caveman register (rewrite target)

Rewrite ALL prose to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. Applies to prose + code comments. **Stays literal:** JSON/YAML keys+values, schemas, ids (`R*`/`AC*`/`C*`/`ADR-*`/`D*`), code syntax.

## CONTRACT D — dead-source rogue-ref sweep (this part owns the `.adr` hits)

Repoint every occurrence (path-token edit, NOT prose rewriting):
- `_decisions.md` → `.adr/` ; the decision **index** is now `.adr/adr-index.json` (NOT a markdown header). E.g. `source: "_rules.md Conventions · _decisions.md Decision index"` → `source: ".hld/skeleton/ · .adr/adr-index.json"`.
- `_rules.md` → `.hld/skeleton/*` (design canon) or `CLAUDE.md` (standing rules).
- `_initial_design/` → `.aprd/specs/`.

**Structural-value sweep (subtle, but the M11 zero-hits acceptance covers the WHOLE tree):** `adr.lock` `adrs[].source` and any `adr-index.json` value strings still carry `_decisions.md`/`_rules.md` path tokens. These are JSON values (normally left literal), but a *dead path* in them trips the M11.14 acceptance grep. Repoint the path tokens (do NOT caveman the value — it is structured provenance). Neither `adr.lock` nor `adr-index.json` is a hash input → these are free edits, no re-lock cascade.

## CONTRACT C — re-lock `adr.lock` (after all log bodies final)

`adr = sha256( concat( sorted(.adr/log/*.md) by filename ) )`. Drafts + index + lock are NOT hashed. Procedure: edit all 21 log bodies → recompute sha in Bash → set `adr.lock.content_sha256`, bump `version v2`→`v3`, set `signed_at` to run time, keep `signer: self-host:agentic-delivery-pipeline`, `status: frozen`. Verify recompute == lock. (Current `adr.lock` = v2.)

## CONTRACT E — behavior check

Prose edits must not change ids/structure. RE-RANK still names next-unshipped `P-BUILD-PLAN-SLICE`. `adr_count` stays 21; all `log_ref`/`draft_ref` paths unchanged.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Caveman-rewrite + sweep all 21 `.adr/log/*.md` bodies (Contract A + D) | prose+comments caveman; `_decisions.md`/`_rules.md`/`_initial_design` → canonical; ids/decision substance untouched | ☑ |
| T2 | Same over all 21 `.adr/drafts/*.draft.md` | caveman + swept; no re-lock (drafts unhashed) | ☑ |
| T3 | Sweep dead-path tokens in `adr-index.json` + `adr.lock` `source` values (Contract D structural-value rule) | no `_decisions.md`/`_rules.md` token in either file; values still valid JSON | ☑ |
| T4 | Re-lock `adr.lock` (Contract C `adr` algo) | recompute == lock; `version v3`; `adr_count 21` | ☑ |
| T5 | Behavior check (Contract E) | RE-RANK names `P-BUILD-PLAN-SLICE`; 21 log/draft paths intact | ☑ |

## M11.2 acceptance — MET when

- [x] 21 log bodies + 21 drafts caveman; substance/ids invariant
- [x] `grep -rE '_decisions\.md|_rules\.md|_initial_design' .adr` = ZERO (bodies + index + lock)
- [x] `adr.lock` recompute == lock, `version v3`, `adr_count 21`
- [x] RE-RANK names `P-BUILD-PLAN-SLICE`

## Outcome (M11.2 done — NO COMMIT)

- T1/T2: 21 log + 21 draft bodies caveman-swept. Body prose (0001–0004) caveman + swept. ids/D*/ADR-*/decision substance invariant.
  - **Provenance-direction fix (2 rounds, CTO-flagged):** `source:` must point UPSTREAM of the ADR (`…→ADR→C/HLD`), so NO derivative artifact qualifies. (a) `.hld/skeleton/` rejected — skeleton is shaped BY ADRs. (b) `.adr/adr-index.json` rejected — index is the most-downstream artifact (catalogues the ADRs; entries = `{id,log_ref,draft_ref}`). Final `source:` values: **D1–D4 → `CLAUDE.md`** (standing-convention home); **D5–D21 → `reasoned`** (canon grounding vocab `existing_system|canon|reasoned` per OPTION-GEN/EVALUATE-DECIDE — these are greenfield reasoned design calls; old `_decisions.md` log content absorbed INTO `.adr/log/`, so self-recorded, no external source doc survives). Body "Indexed in .adr/adr-index.json" kept — accurate catalogued-in note, not a provenance claim.
- T3: `adr-index.json` + `adr.lock` `adrs[].source` values repointed to match frontmatter (4× `CLAUDE.md`, 17× `reasoned`). Both valid JSON (jq -e). No `source:` claims `_decisions.md`/`_rules.md`/`.hld/skeleton/`/`.adr/adr-index.json`.
- T4: `adr.lock` re-locked. Algo confirmed against v2 from git-HEAD (matched `9ff2a8…`). Final `content_sha256 = 267b885b4442fe245dd35a40e4d8c1fa84f611195b540ace793cfb63b87cb779`; recompute == lock ✓; `version v2→v3` (held v3 across all in-session corrections — one unshipped change); `signed_at 2026-06-08T14:15:26Z`; `signer`/`status:frozen`/`adr_count 21` kept.
- T5: `grep -rE '_decisions\.md|_rules\.md|_initial_design' .adr` = ZERO. RE-RANK frontier = `P-BUILD-PLAN-SLICE` (roadmap.md + 08-rerank.json). 21 `log_ref` + 21 `draft_ref` intact; adr-index structure untouched.

## Done-checklist contribution (spec M11 lines)

- partial: "every survivor file caveman" (`.adr` slice) · "deleted-source rogue refs swept" (`.adr` slice) · "frozen artifacts re-locked" (`adr.lock`).

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag.
- Caveman governs this task file too.
