# M11.11 — `prompts/03-hld/` (C: MODEL-DATA + DEFINE-CONTRACTS + MAP-NFR) caveman + flip — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **11 of 14** of M11 (Phase-3 split C). **Precondition: M11.1 cleared.** Parallel-safe. **NO COMMIT.**

## Scope

Caveman-rewrite prose + comments AND flip the embedded register block. **prompts/ carry ZERO dead-source rogue refs** and are **NOT hashed by any lock** → no sweep, no re-lock.

Files (`prompts/03-hld/`): `MODEL-DATA.md` (~36 KB), `DEFINE-CONTRACTS.md` (~36 KB), `MAP-NFR.md` (~35 KB). (~106 KB.)

## CONTRACT A — caveman register

Rewrite ALL prose + code comments to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. **Stays literal:** structural data (YAML/JSON keys+values, schemas, examples), identifiers (`R*`/`AC*`/`C*`/`ADR-*`/`M*` NFR ids), code syntax.

## CONTRACT B — embedded-block flip (mechanical, every file)

Flip the single carve-out line (`replace_all`); header + 5 bullets UNCHANGED:
- **OLD (delete):** `Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.`
- **NEW (replace):** `Applies to ALL prose: narration AND artifact bodies (spec/ADR/prompt/doc) AND code comments. Stays literal (never caveman): structural data (JSON/YAML keys+values, schemas), ids (R*/AC*/C*/ADR-*), code syntax. Caveman shortens prose, never breaks data/code.`
- Also flip inline carve-out notes: `(clean prose)`, `All prose fields are clean (caveman governs narration, not the artifact — PR4)`, kin.

## CONTRACT E — behavior check

`MAP-NFR` carries NFR ids `M1`,`M2`,… — these are **governing identifiers, KEEP literal** (NOT migration milestones). `DEFINE-CONTRACTS` emits the PR2 contract schema — rewriting prose must NOT change it. RE-RANK still names next-unshipped **`P-BUILD-PLAN-SLICE`**.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Flip embedded block + inline notes in all 3 files (Contract B) | OLD gone; NEW present; header/bullets intact | ☑ |
| T2 | Caveman-rewrite prose + comments (Contract A) | caveman; schemas/ids (incl. NFR `M*`)/examples literal | ☑ |
| T3 | Behavior check (Contract E) | RE-RANK names `P-BUILD-PLAN-SLICE`; NFR `M*` ids intact | ☑ |

## M11.11 acceptance — MET when

- [x] `MODEL-DATA.md` + `DEFINE-CONTRACTS.md` + `MAP-NFR.md` caveman; structural data + NFR ids untouched
- [x] embedded carve-out flipped in all 3; inline notes flipped
- [x] RE-RANK names `P-BUILD-PLAN-SLICE`

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.

## Evidence (M11.11 done)

- Contract B: old carve-out `0` remnants all 3 files; NEW register block `1` each; NEW inline note `2` each; register header+5 bullets intact; MAP-NFR bucket-sum clause preserved.
- Contract A: id multiset (R*/AC*/C*/E*/CT*/INV*/ADR-*/S*/M*/A*) **byte-identical** vs HEAD all 3 files → zero structural data touched. DEFINE-CONTRACTS WRONG/CORRECT `shape` examples literal. Prose/comments caveman, matches `DERIVE-COMPONENTS.md` exemplar.
- Contract E: MAP-NFR `M1` governing id intact (`RM11` rule-ref untouched); `pass: skeleton|increment` dispatch intact all 3; these prompts don't drive RE-RANK → next-unshipped `P-BUILD-PLAN-SLICE` unaffected.
