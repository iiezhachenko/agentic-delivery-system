# M11.4 — `.aprd/specs/04–06` + `aprd.frozen.md` caveman + re-lock — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **4 of 14** of M11. **Precondition: M11.1 cleared.** Parallel-safe with M11.2/M11.3/M11.5–M11.13. **NO COMMIT.**

## Scope

Caveman-rewrite the last 3 specs + the frozen aPRD body, then re-seal `aprd.lock` (the frozen body is its only hash input).

| File | Action | Lock? |
|---|---|---|
| `.aprd/specs/04-automated-build-pipeline-spec.md` (~40 KB) | caveman rewrite | unhashed — no re-lock |
| `.aprd/specs/05-automated-documentation-pipeline-spec.md` (~20 KB) | caveman rewrite | unhashed |
| `.aprd/specs/06-automated-architecture-review-pipeline-spec.md` (~20 KB) | caveman rewrite | unhashed |
| `.aprd/aprd.frozen.md` (~3.8 KB) | caveman rewrite + dead-ref sweep | **hashed by `aprd.lock`** → re-lock |
| `.aprd/aprd.lock` | **re-lock** | self not hashed |

## CONTRACT A — caveman register

Rewrite ALL prose to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. Prose + code comments. **Stays literal:** JSON/YAML keys+values, schemas, fenced examples, ids (`R*`/`AC*`/`C*`/`ADR-*`/`M*` NFR ids), code syntax.

## CONTRACT D — dead-source sweep (aprd.frozen.md carries these)

`aprd.frozen.md` historically pointed at `_initial_design/`/`_rules.md`. Repoint: `_initial_design/`→`.aprd/specs/`, `_rules.md`→`.hld/skeleton/*` or `CLAUDE.md`, `_decisions.md`→`.adr/`. (Dead paths in the requirements source mislead the Build phase — sweep them.)

## CONTRACT C — re-lock `aprd.lock`

`aprd = sha256( aprd.frozen.md )` — single file. Procedure: edit `aprd.frozen.md` → recompute sha in Bash → set `aprd.lock.content_sha256`, bump `version v2`→`v3`, set `signed_at` to run time, keep `signer`/`status`. Verify recompute == lock. (Specs are NOT in the hash — their rewrite doesn't affect the lock.) Current `aprd.lock` = v2.

## CONTRACT E — behavior check

NFR `M*` ids stay literal. No structure change. RE-RANK still names `P-BUILD-PLAN-SLICE`.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Caveman-rewrite specs 04–06 (Contract A); sweep any dead refs (D) | prose+comments caveman; ids/schemas untouched | ☑ |
| T2 | Caveman-rewrite + sweep `aprd.frozen.md` (A + D) | caveman; `_initial_design`/`_rules.md`/`_decisions.md` → canonical | ☑ |
| T3 | Re-lock `aprd.lock` (Contract C) | recompute == lock; `version v3` | ☑ |
| T4 | Behavior check (E) | RE-RANK names `P-BUILD-PLAN-SLICE` | ☑ |

## M11.4 acceptance — MET when

- [x] specs 04–06 + `aprd.frozen.md` caveman; substance/ids invariant
- [x] `grep -rE '_decisions\.md|_rules\.md|_initial_design' .aprd` = ZERO
- [x] `aprd.lock` recompute == lock, `version v3`
- [x] RE-RANK names `P-BUILD-PLAN-SLICE`

## Result (2026-06-08)

- **T1** specs 04/05/06 caveman-rewritten. Fenced content (mermaid/yaml/ascii/prompt blocks + schemas) byte-for-byte == HEAD (sha-matched). Zero IDs dropped (B*/P*/H*/RM*/D*/CT*/F*/M*/CMP*/REC*/FND*/SEC*/FACT*/DRIFT*/OQ-* all present). Counts held (goals/principles/stages/failure-modes/glossary/open-questions/reuse-rows).
- **T2** `aprd.frozen.md` caveman-rewritten. Dead-ref sweep = ZERO (no `_initial_design`/`_rules.md`/`_decisions.md` — already canonical pre-rewrite, confirmed post). IDs (D21/D6/ADR-0021/P8) intact.
- **T3** `aprd.lock` re-locked: `content_sha256 = 678590f9…a0e09c`, `version v2→v3`, signer/status kept. Recompute == lock ✓. Valid JSON.
- **T4** RE-RANK (`.roadmap/08-rerank.json`) names `P-BUILD-PLAN-SLICE` (4×). No structure change; NFR `M*` ids literal.
- **NO COMMIT** (per task).

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
