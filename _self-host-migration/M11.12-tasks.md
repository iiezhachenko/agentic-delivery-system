# M11.12 — `prompts/04-build/` (A: INTEGRATE + CRITIQUE + MATERIALIZE-ORACLE + DEMO-GEN) caveman + flip — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **12 of 14** of M11. **Precondition: M11.1 cleared.** Parallel-safe. **NO COMMIT.**
>
> Phase-4 (`04-build`, ~177 KB, 8 roles) split 2 ways: **M11.12 (A) · M11.13 (B)**. This is A.

## Scope

Caveman-rewrite prose + comments AND flip the embedded register block. **prompts/ carry ZERO dead-source rogue refs** and are **NOT hashed by any lock** → no sweep, no re-lock.

Files (`prompts/04-build/`): `INTEGRATE.md` (~26 KB), `CRITIQUE.md` (~25 KB), `MATERIALIZE-ORACLE.md` (~24 KB), `DEMO-GEN.md` (~23 KB). (~98 KB.)

## CONTRACT A — caveman register

Rewrite ALL prose + code comments to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. **Stays literal:** structural data (YAML/JSON keys+values, schemas, fenced code/examples), identifiers (`R*`/`AC*`/`C*`/`CT*`/`ADR-*`/ids), code syntax.

> Phase-4 prompts contain real fenced code examples (Python, JSON) + header-comment templates. Those are **code syntax + structural data** — leave literal. Only narration + free-prose around them goes caveman.

## CONTRACT B — embedded-block flip (mechanical, every file)

Flip the single carve-out line (`replace_all`); header + 5 bullets UNCHANGED:
- **OLD (delete):** `Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.`
- **NEW (replace):** `Applies to ALL prose: narration AND artifact bodies (spec/ADR/prompt/doc) AND code comments. Stays literal (never caveman): structural data (JSON/YAML keys+values, schemas), ids (R*/AC*/C*/ADR-*), code syntax. Caveman shortens prose, never breaks data/code.`
- Also flip inline carve-out notes: `(clean prose)`, `All prose fields are clean (caveman governs narration, not the artifact — PR4)`, kin.

## CONTRACT E — behavior check

`MATERIALIZE-ORACLE` + `CRITIQUE` are oracle/anti-cheat roles — rewriting prose must NOT weaken checks or change emitted schemas. RE-RANK still names next-unshipped **`P-BUILD-PLAN-SLICE`**.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Flip embedded block + inline notes in all 4 files (Contract B) | OLD gone; NEW present; header/bullets intact | ☑ |
| T2 | Caveman-rewrite prose + comments (Contract A) | caveman; fenced code/schemas/ids literal | ☑ |
| T3 | Behavior check (Contract E) | RE-RANK names `P-BUILD-PLAN-SLICE` | ☑ |

## M11.12 acceptance — MET when

- [x] the 4 files caveman; fenced code + structural data untouched
- [x] embedded carve-out flipped in all 4; inline notes flipped
- [x] RE-RANK names `P-BUILD-PLAN-SLICE`

## Outcome

- 4 files (`INTEGRATE`, `CRITIQUE`, `MATERIALIZE-ORACLE`, `DEMO-GEN`) caveman: frontmatter prose, role narration, rules, task steps, conventions, schema `//` comments rewritten terse. Articles/filler/hedging dropped.
- Literal kept: JSON/YAML keys+values, schemas, fenced code (Python/JSON examples, demo.md client-facing template), ids (`R*`/`AC*`/`C*`/`CT*`/`F*`/`E*`/`INV*`/`ADR-*`). Verified: stripped-comment diff vs HEAD shows ZERO schema key/value line changes — only prose + comments moved.
- Carve-out flip: OLD `Exception: artifact content…` → NEW `Applies to ALL prose…` in all 4 (header + 5 bullets intact). Inline notes flipped: `All prose fields are clean (caveman governs narration…)` → `Prose fields caveman too (keys/values/ids/schema literal — PR4)`; `(clean prose)` header labels → `(caveman)`. DEMO-GEN client-facing prose note flipped, client-facing English template preserved (recognition-over-recall, §9).
- Behavior (Contract E): schemas + checks unchanged. Frontier unchanged — P-RECONCILE-CRITIQUE-INC sentinel present (shipped), `P-BUILD-PLAN-SLICE` sentinel absent → RE-RANK names `P-BUILD-PLAN-SLICE` next-unshipped.

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
