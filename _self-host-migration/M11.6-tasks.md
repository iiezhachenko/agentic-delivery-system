# M11.6 — `prompts/00-aprd/` caveman + embedded-block flip — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **6 of 14** of M11. **Precondition: M11.1 cleared.** Parallel-safe with all other M11.2–M11.13. **NO COMMIT.**

## Scope — cheapest part class

Caveman-rewrite prose + comments AND flip the embedded register block in the Phase-0 prompts. **prompts/ carry ZERO dead-source rogue refs** (verified) and are **NOT hashed by any lock** → no sweep, no re-lock.

Files (`prompts/00-aprd/`): `CLASSIFIER.md`, `CRITIQUE.md`, `EXTRACT-RULES.md`, `EXTRACT.md`, `GAP-DETECT.md`, `QUESTION-GEN.md`, `RECONCILE.md`, `SYNTHESIZE.md`, `VERIFY.md` (9 files, ~117 KB).

## CONTRACT A — caveman register (rewrite target)

Rewrite ALL prose + code comments to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. **Stays literal (never caveman):** structural data (YAML frontmatter keys+values, JSON schemas, fenced examples), identifiers (`R*`/`AC*`/`C*`/`ADR-*`/ids), code syntax.

## CONTRACT B — embedded-block flip (mechanical, every file)

Each prompt carries a verbatim `# Register` block. Flip the single carve-out line (`replace_all`); header + 5 bullets UNCHANGED:
- **OLD (delete):** `Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.`
- **NEW (replace):** `Applies to ALL prose: narration AND artifact bodies (spec/ADR/prompt/doc) AND code comments. Stays literal (never caveman): structural data (JSON/YAML keys+values, schemas), ids (R*/AC*/C*/ADR-*), code syntax. Caveman shortens prose, never breaks data/code.`
- Also flip inline carve-out notes wherever they appear: `(clean prose)`, `All prose fields are clean (caveman governs narration, not the artifact — PR4)`, `caveman governs narration, not the artifact` → delete or "caveman governs this too".

## CONTRACT E — behavior check

Prompts are the deliverable — rewriting prose/comments must NOT change GIVEN/WRITES paths, YAML frontmatter, schemas, `escapes:`, or role logic. RE-RANK (over `.roadmap/08-rerank.json` + `_fixtures/` sentinels) still names next-unshipped **`P-BUILD-PLAN-SLICE`**.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Flip embedded block + inline carve-out notes across all 9 files (Contract B) | OLD line gone everywhere; NEW line present; header/bullets intact | ☐ |
| T2 | Caveman-rewrite prose + code comments (Contract A) | prose+comments caveman; frontmatter/schemas/ids/examples literal | ☐ |
| T3 | Behavior check (Contract E) | RE-RANK names `P-BUILD-PLAN-SLICE`; no path/schema change | ☐ |

## M11.6 acceptance — MET when

- [ ] all 9 `00-aprd` prompts caveman (prose + comments); structural data untouched
- [ ] embedded carve-out line flipped in all 9; inline notes flipped
- [ ] no lock touched; RE-RANK names `P-BUILD-PLAN-SLICE`

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
