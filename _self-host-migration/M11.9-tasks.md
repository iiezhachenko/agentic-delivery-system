# M11.9 — `prompts/03-hld/` (A: RECONCILE-CRITIQUE + RESOLVE-LOCAL) caveman + flip — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **9 of 14** of M11. **Precondition: M11.1 cleared.** Parallel-safe. **NO COMMIT.**
>
> Phase-3 (`03-hld`) is the densest phase (~307 KB, 8 roles) → split 3 ways: **M11.9 (A) · M11.10 (B) · M11.11 (C)**. This is part A.

## Scope

Caveman-rewrite prose + comments AND flip the embedded register block in the 2 largest Phase-3 roles. **prompts/ carry ZERO dead-source rogue refs** and are **NOT hashed by any lock** → no sweep, no re-lock.

Files (`prompts/03-hld/`): `RECONCILE-CRITIQUE.md` (~50 KB), `RESOLVE-LOCAL.md` (~43 KB). (~93 KB total — the 2 biggest single files in the tree; kept together but isolated to stay ≤150k.)

## CONTRACT A — caveman register

Rewrite ALL prose + code comments to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. **Stays literal:** structural data (YAML/JSON keys+values, schemas, examples), identifiers (`R*`/`AC*`/`C*`/`ADR-*`/ids), code syntax.

## CONTRACT B — embedded-block flip (mechanical, every file)

Flip the single carve-out line (`replace_all`); header + 5 bullets UNCHANGED:
- **OLD (delete):** `Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.`
- **NEW (replace):** `Applies to ALL prose: narration AND artifact bodies (spec/ADR/prompt/doc) AND code comments. Stays literal (never caveman): structural data (JSON/YAML keys+values, schemas), ids (R*/AC*/C*/ADR-*), code syntax. Caveman shortens prose, never breaks data/code.`
- Also flip inline carve-out notes: `(clean prose)`, `All prose fields are clean (caveman governs narration, not the artifact — PR4)`, kin.

## CONTRACT E — behavior check

`RECONCILE-CRITIQUE` is the adversarial gate role — rewriting its prose must NOT weaken its checks or change its verdict schema. RE-RANK still names next-unshipped **`P-BUILD-PLAN-SLICE`**.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Flip embedded block + inline notes in both files (Contract B) | OLD gone; NEW present; header/bullets intact | ☑ |
| T2 | Caveman-rewrite prose + comments (Contract A) | caveman; schemas/ids/examples literal; gate checks intact | ☑ |
| T3 | Behavior check (Contract E) | RE-RANK names `P-BUILD-PLAN-SLICE` | ☑ |

## M11.9 acceptance — MET when

- [x] `RECONCILE-CRITIQUE.md` + `RESOLVE-LOCAL.md` caveman; structural data untouched
- [x] embedded carve-out flipped in both; inline notes flipped
- [x] RE-RANK names `P-BUILD-PLAN-SLICE`

## Done — evidence

- **T1 Contract B.** Both files: OLD `Exception: artifact content…` line GONE (grep 0); NEW `Applies to ALL prose…` present (grep 1 each); header + 5 bullets unchanged. Inline notes flipped: `caveman governs narration, not the artifact` GONE (0); `Caveman governs this too` present; `Clean prose>` → `Caveman prose>`; RESOLVE-LOCAL draft note `prose clean and complete` → `prose caveman + complete`.
- **T2 Contract A.** All prose + `//` comments caveman (articles/filler/hedging dropped). Structural data literal: JSON/YAML keys+values, schemas, examples (`S4`/`ADR-0007`/`DP3`/`CT2`), `verdict` enum, 7/8 category names, id patterns all untouched. Gate checks intact — 7 skeleton categories + 8th `skeleton-fidelity` (H14), RE-DERIVE-from-primary-fields discipline, verdict schema (`clean|blocked`, deterministic from issues) all preserved verbatim in substance.
- **T3 Contract E.** RECONCILE-CRITIQUE adversarial mandate + verdict schema unchanged. Frontier unmoved: prose-only edits, no sentinel/schema change. `08-rerank.json` remaining_sequence: P-RECONCILE-CRITIQUE-INC (sentinel `_fixtures/greenfield-clean/.hld/slices/S4/reconcile.json` PRESENT = shipped) → next-unshipped = **P-BUILD-PLAN-SLICE** (sentinel `_fixtures/greenfield-build-reds/.build/slices/S4/build-plan.json` ABSENT).
- **NO COMMIT** (per task).

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
- If this part still over-runs 150k (these 2 are the densest files), split into 1-file sessions; no lock follows (prompts unhashed).
