# M11.5 — `docs/` caveman — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **5 of 14** of M11. **Precondition: M11.1 cleared AND M10 cleared** (the 4 manuals already relocated to `docs/` + de-migration-framed). Parallel-safe with M11.2–M11.4/M11.6–M11.13. **NO COMMIT.**

## Scope

Caveman-rewrite the 4 relocated operating manuals. None are locked → no re-lock. M10 already stripped migration framing; M11.5 only normalizes the register.

| File | ~size |
|---|---|
| `docs/generic-workflow.md` | ~12 KB |
| `docs/generic-usage-guide.md` | ~23 KB |
| `docs/self-host-workflow.md` | ~16 KB |
| `docs/self-host-usage-guide.md` | ~22 KB |

## CONTRACT A — caveman register (rewrite target)

Rewrite ALL prose to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]. [next step]`. Applies to prose + code comments + fenced shell/command narration. **Stays literal:** JSON/YAML keys+values, schemas, command syntax, paths, ids.

## Guard — do NOT re-introduce migration framing

M10 already removed it. Keep these GONE: `_self/`, freeze tool, "bootstrap", "seed-from-frozen", "migration-spec", `_self-host-migration/` paths, migration-process vocabulary. Keep legit reflexive content: Level-A/Level-B, prompt-as-deliverable (`prompts/`=`src/`, oracle=`_fixtures/`, stack=`code-canon/agentic-delivery-pipeline.md`), engine vocab (`frozen`, `freeze the requirements`, `skeleton`, RE-RANK, launchers).

## CONTRACT E — behavior check

Docs are not engine-read for RE-RANK; no sentinel risk. Just confirm no dead path introduced and the M10 token-set grep stays ZERO over `docs/`.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Caveman-rewrite the 4 `docs/` manuals (Contract A) | prose+comments caveman; substance kept; command/path/schema literal | ☐ |
| T2 | Re-grep migration token-set over `docs/` = ZERO (guard) | no migration trace re-introduced | ☐ |

## M11.5 acceptance — MET when

- [ ] 4 `docs/` manuals caveman; substance invariant
- [ ] migration token-set grep over `docs/` = ZERO

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
