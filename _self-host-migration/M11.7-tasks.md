# M11.7 — `prompts/01-roadmap/` caveman + embedded-block flip — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **7 of 14** of M11. **Precondition: M11.1 cleared.** Parallel-safe. **NO COMMIT.**

## Scope

Caveman-rewrite prose + comments AND flip the embedded register block in the Phase-1 prompts. **prompts/ carry ZERO dead-source rogue refs** and are **NOT hashed by any lock** → no sweep, no re-lock.

Files (`prompts/01-roadmap/`): `FOUNDATION-CUT.md`, `RE-RANK.md`, `SEQUENCE-REVIEW.md`, `SEQUENCE.md`, `SKELETON-IDENTIFY.md`, `SLICE-EXTRACT.md`, `VERTICALITY-CHECK.md` (7 files, ~107 KB).

## CONTRACT A — caveman register

Rewrite ALL prose + code comments to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. **Stays literal:** structural data (YAML/JSON keys+values, schemas, examples), identifiers (`R*`/`AC*`/`C*`/`ADR-*`/ids), code syntax.

## CONTRACT B — embedded-block flip (mechanical, every file)

Flip the single carve-out line (`replace_all`); header + 5 bullets UNCHANGED:
- **OLD (delete):** `Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.`
- **NEW (replace):** `Applies to ALL prose: narration AND artifact bodies (spec/ADR/prompt/doc) AND code comments. Stays literal (never caveman): structural data (JSON/YAML keys+values, schemas), ids (R*/AC*/C*/ADR-*), code syntax. Caveman shortens prose, never breaks data/code.`
- Also flip inline carve-out notes: `(clean prose)`, `All prose fields are clean (caveman governs narration, not the artifact — PR4)`, kin.

## CONTRACT E — behavior check

**RE-RANK lives in this phase (`RE-RANK.md`)** — be extra careful: rewriting its prose must NOT change its ranking logic, the `08-rerank.json` schema it reads, or sentinel semantics. RE-RANK must still name next-unshipped **`P-BUILD-PLAN-SLICE`** after the rewrite.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Flip embedded block + inline notes across all 7 files (Contract B) | OLD gone; NEW present; header/bullets intact | ☐ |
| T2 | Caveman-rewrite prose + comments (Contract A) | caveman; schemas/ids/examples literal; `RE-RANK.md` logic intact | ☐ |
| T3 | Behavior check (Contract E) — run RE-RANK | names `P-BUILD-PLAN-SLICE` | ☐ |

## M11.7 acceptance — MET when

- [ ] all 7 `01-roadmap` prompts caveman; structural data untouched
- [ ] embedded carve-out flipped in all 7; inline notes flipped
- [ ] RE-RANK still names `P-BUILD-PLAN-SLICE` (its own prose rewrite is behavior-preserving)

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
