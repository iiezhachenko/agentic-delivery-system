# M11.10 — `prompts/03-hld/` (B: DERIVE-TESTS + MODEL-FLOWS + DERIVE-COMPONENTS) caveman + flip — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **10 of 14** of M11 (Phase-3 split B). **Precondition: M11.1 cleared.** Parallel-safe. **NO COMMIT.**

## Scope

Caveman-rewrite prose + comments AND flip the embedded register block. **prompts/ carry ZERO dead-source rogue refs** and are **NOT hashed by any lock** → no sweep, no re-lock.

Files (`prompts/03-hld/`): `DERIVE-TESTS.md` (~40 KB), `MODEL-FLOWS.md` (~37 KB), `DERIVE-COMPONENTS.md` (~30 KB). (~107 KB.)

## CONTRACT A — caveman register

Rewrite ALL prose + code comments to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. **Stays literal:** structural data (YAML/JSON keys+values, schemas, examples), identifiers (`R*`/`AC*`/`C*`/`ADR-*`/ids), code syntax.

## CONTRACT B — embedded-block flip (mechanical, every file)

Flip the single carve-out line (`replace_all`); header + 5 bullets UNCHANGED:
- **OLD (delete):** `Exception: artifact content (specs, JSON/YAML, ADR bodies) stays clean and complete. Caveman governs narration, not the deliverable.`
- **NEW (replace):** `Applies to ALL prose: narration AND artifact bodies (spec/ADR/prompt/doc) AND code comments. Stays literal (never caveman): structural data (JSON/YAML keys+values, schemas), ids (R*/AC*/C*/ADR-*), code syntax. Caveman shortens prose, never breaks data/code.`
- Also flip inline carve-out notes: `(clean prose)`, `All prose fields are clean (caveman governs narration, not the artifact — PR4)`, kin.

## CONTRACT E — behavior check

`DERIVE-TESTS` emits the oracle contract tests — rewriting its prose must NOT change the test schema/partition it produces. RE-RANK still names next-unshipped **`P-BUILD-PLAN-SLICE`**.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Flip embedded block + inline notes in all 3 files (Contract B) | OLD gone; NEW present; header/bullets intact | ☑ |
| T2 | Caveman-rewrite prose + comments (Contract A) | caveman; schemas/ids/examples literal | ☑ |
| T3 | Behavior check (Contract E) | RE-RANK names `P-BUILD-PLAN-SLICE` | ☑ |

## M11.10 acceptance — MET when

- [x] `DERIVE-TESTS.md` + `MODEL-FLOWS.md` + `DERIVE-COMPONENTS.md` caveman; structural data untouched
- [x] embedded carve-out flipped in all 3; inline notes flipped
- [x] RE-RANK names `P-BUILD-PLAN-SLICE`

## Outcome

- **T1** carve-out flipped all 3: OLD `Exception: artifact content…` gone, NEW `Applies to ALL prose…` present (1×/file). Register header + 5 bullets intact. Inline notes flipped: `All prose fields are clean (caveman governs narration…)` → `Prose fields caveman too (keys/values/ids/schema literal — PR4)` (DERIVE-TESTS 2×, MODEL-FLOWS 2×, DERIVE-COMPONENTS 2×); DERIVE-COMPONENTS `clean prose` → `caveman prose` in schema placeholder.
- **T2** prose + `//` comments + `<…>` placeholders cavemanized all 3. Diff = 304 ins / 304 del, prose-only. Verified untouched: YAML frontmatter (0 lines), JSON keys, literal example values (e.g. `failure_mode:"store-unavailable…"` byte-identical), example ids. Structural data + code syntax literal.
- **T3** RE-RANK invariant holds by construction: prose edits change no done_sentinel, no prompt-presence, no schema. Positions 1–2 (DERIVE-TESTS, RECONCILE-CRITIQUE) authored → next-unshipped = **P-BUILD-PLAN-SLICE** (pos 3, `prompts/04-build/BUILD-PLAN.md` slice-build mode). DERIVE-TESTS test schema/partitions unchanged.

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
