# M11.3 — `.aprd/specs/00–03` caveman — tasks (SELF-CONTAINED)

> **Self-contained:** all context in THIS file. Do NOT read `migration-spec.md` or `M11.1`. Part **3 of 14** of M11. **Precondition: M11.1 cleared.** Parallel-safe with M11.2/M11.4–M11.13. **NO COMMIT.**

## Scope

Caveman-rewrite the first 4 requirement specs. These are `.aprd/specs/*` = NOT hashed by `aprd.lock` (only `aprd.frozen.md` is) → **no re-lock** here; `aprd.frozen.md` + the re-lock belong to M11.4.

| File | ~size |
|---|---|
| `.aprd/specs/00-automated-aprd-pipeline-spec.md` | ~26 KB |
| `.aprd/specs/01-automated-roadmap-pipeline-spec.md` | ~28 KB |
| `.aprd/specs/02-automated-adr-pipeline-spec.md` | ~30 KB |
| `.aprd/specs/03-automated-hld-pipeline-spec.md` | ~34 KB |

## CONTRACT A — caveman register (rewrite target)

Rewrite ALL prose to terse caveman. **Substance invariant — only fluff dies.** Drop articles/filler/hedging/pleasantries. Pattern `[thing] [action] [reason]`. Applies to prose + code comments. **Stays literal (never caveman):** structural data (JSON/YAML keys+values, schemas, fenced examples), identifiers (`R*`/`AC*`/`C*`/`ADR-*`/`M*` NFR ids), code syntax. Caveman shortens prose, never corrupts data/ids.

## CONTRACT D — dead-source sweep (if encountered)

Repoint any `_decisions.md`→`.adr/` (index = `.adr/adr-index.json`), `_rules.md`→`.hld/skeleton/*` or `CLAUDE.md`, `_initial_design/`→`.aprd/specs/`. (Specs carry few/none; sweep if present.)

## CONTRACT E — behavior check

No id/structure change. NFR ids (`M1`, `M2`, …) are governing identifiers — KEEP literal, do NOT treat as migration milestones. RE-RANK still names `P-BUILD-PLAN-SLICE`.

## Tasks

| # | Task | Acceptance | Status |
|---|---|---|---|
| T1 | Caveman-rewrite specs 00–03 (Contract A); sweep any dead refs (D) | prose+comments caveman; R*/AC*/M* ids + schemas/examples untouched | ☐ |
| T2 | Behavior check (E) | RE-RANK names `P-BUILD-PLAN-SLICE`; NFR `M*` ids intact | ☐ |

## M11.3 acceptance — MET when

- [ ] specs 00–03 caveman; substance/ids/schemas invariant
- [ ] no re-lock needed (`.aprd/specs/*` unhashed)
- [ ] RE-RANK names `P-BUILD-PLAN-SLICE`

## Notes

- **NO COMMIT.** Recoverable from `pre-self-host` tag. Caveman governs this file too.
