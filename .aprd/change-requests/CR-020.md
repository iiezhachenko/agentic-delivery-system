# CR-020 — DERIVE-TESTS split: extract DERIVE-BUILD-DAG + extract bugfix pass

> Parent: CR-016. Operator-directed (2026-06-12). Deferred — execute in subsequent build workstream after ADR-0037 acceptance. Caveman register.

## Ask

DERIVE-TESTS skeleton pass writes two distinct unrelated output paths: `.hld/skeleton/test-specs.json` (test oracle specifications, consumed by MATERIALIZE-ORACLE) + `.hld/skeleton/build-dag.json` (build dependency graph, consumed by IMPLEMENT). Separate discriminators, separate task-step sections, separate consumers. Block F5 (LC1). Additionally, bugfix pass (Part C) behaves as substantially different role: re-enters existing slice, mints reproduction test + regression layer instead of contract/flow/acceptance specs. Warn F9 (LC4).

Two extractions: (a) DERIVE-BUILD-DAG for build-dag.json; (b) evaluate bugfix pass extraction per ADR-0037 outcome.

## Findings

| id | criterion | severity |
|---|---|---|
| F5 | LC1 — 2+ unrelated output paths in skeleton pass | block |
| F9 | LC4 — bugfix pass behaves as different role | warn |

## Scope delta

### A — Modify DERIVE-TESTS

`prompts/03-hld/DERIVE-TESTS.md` — remove build-dag emission from skeleton pass. Remove bugfix pass (Part C) — extracted to DERIVE-TESTS-BUGFIX per CR-021. Post-both-CRs: DERIVE-TESTS = skeleton/increment test-specs only (no build-dag, no bugfix pass). Skeleton + increment modes only. Primary output: `.hld/skeleton/test-specs.json`.

### B — New role: DERIVE-BUILD-DAG

`prompts/03-hld/DERIVE-BUILD-DAG.md` — new role. Inputs: `.hld/skeleton/components.json` + `.hld/skeleton/contracts.json` + HLD data models. Produces `.hld/skeleton/build-dag.json` only. Dispatch signal: `class=greenfield, phase=hld-skeleton`. Runs as sibling step to DERIVE-TESTS (both in skeleton pass, can run in parallel or sequentially). Inherits build-dag task logic currently embedded in DERIVE-TESTS Part A skeleton pass.

### C — Spine updates

`tools/io/io-manifest.json` — add DERIVE-BUILD-DAG role entry (inputs: components.json + contracts.json; outputs: build-dag.json). Update DERIVE-TESTS entry to remove build-dag.json output. Update IMPLEMENT entry: depends_on build-dag.json — source now DERIVE-BUILD-DAG not DERIVE-TESTS.
`tools/io/components.json` — add DERIVE-BUILD-DAG component.

### D — Build-DAG consumer update

IMPLEMENT currently reads build-dag.json; source role changes from DERIVE-TESTS to DERIVE-BUILD-DAG. No schema change to build-dag.json itself. io-manifest IMPLEMENT entry updated to reference DERIVE-BUILD-DAG output.

## Affected downstream

- MATERIALIZE-ORACLE: reads test-specs.json — unaffected (DERIVE-TESTS still writes it).
- IMPLEMENT: reads build-dag.json — depends_on DERIVE-BUILD-DAG post-split.
- Greenfield skeleton sequencing: DERIVE-BUILD-DAG added as explicit step before IMPLEMENT.

## Class

refactor + extract. Modifies DERIVE-TESTS (remove build-dag emission) + adds DERIVE-BUILD-DAG.

## Status

Open (deferred). Execute after W30c-ADR0037-SIGN (ADR-0037 accepted + adr.lock v17). Note: CR-021 extracts DERIVE-TESTS-BUGFIX (Part C) — DERIVE-TESTS bugfix pass extracted, not retained. Both CR-020 (build-dag) + CR-021 (bugfix pass) execute.
