# CR-014 — Command-agnostic three-tier write surfaces: fix /evolve leak in shared spine (corrects ADR-0034)

> Operator-directed (2026-06-12), gate ACCEPT received before dispatch. Correction of CR-013/ADR-0034 (kills /evolve name leak in shared adp-orchestrator; establishes command-agnostic R-CW-1/2/3). Class: correction. Caveman register.

## Ask

CR-013/ADR-0034 introduced a new defect: R-CW-1 in `.claude/agents/adp-orchestrator.md` (the SHARED spine agent, invoked by BOTH `/evolve` AND `/deliver`) hard-coded "controller = `/evolve` session" — naming one command as the canonical controller definition in a file shared by both launchers. R-CW text in the shared spine must be command-agnostic: the rule is identical whether the controller invoked `/evolve` or `/deliver`. Also: `prompts/_orchestrator.generic.md` has NO R-CW coverage — `/deliver` runs entirely uncovered.

## Gate answer (D20 guarantee 6 — persisted, never re-asked)

ACCEPT — operator gate 2026-06-12. Dispatch CR-014 → ADR-0035, superseding ADR-0034. /evolve leak in `.claude/agents/adp-orchestrator.md` Run step 4. Generic loop body has no R-CW. Correct all targets. R-CW-1 = controller zero-write (command-agnostic — both launchers, neither canonical). R-CW-2 = orchestrator (shared adp-orchestrator agent) owns mechanical writes. R-CW-3 = step-runner authors + verifies.

## Scope delta

### A — Corrected R-ids (command-agnostic rewrite)

Prior R-CW-1 (ADR-0034 / .claude/agents/adp-orchestrator.md, now corrected): "controller = `/evolve` session." — names one command as canonical in shared-spine file.

**Corrected R-ids (command-agnostic):**

- `R-CW-1` (rewritten, id preserved) — **Controller zero-write.** Controller = the Claude Code session that invokes an ADP launcher — `/evolve` (self-host) OR `/deliver` (end-user delivery); NEITHER is canonical, the rule is identical for both. Controller writes NOTHING: no git ops, no spine tool shell-outs, no lock/index writes, no scratch→destination promotion, no prose, no code, no fixtures. Entire surface = (a) escalation gate to Operator, (b) break-glass route.
- `R-CW-2` (substance unchanged, id preserved) — **Orchestrator loop-control-only.** Orchestrator = the adp-orchestrator agent (shared by both launchers). PERMITTED writes: git ops (checkout/merge/commit/push) · spine tool shell-outs (`node tools/<tool>.mjs`) · lock re-sign + index updates per spine tooling · scratch→destination promotion. PROHIBITED: authoring any prose, code, or fixtures — all such authoring → step-runner.
- `R-CW-3` (unchanged) — **Step-runner authors + verifies.** Step-runner authors ALL artifact content; verifier = separate step-runner spawn (runner never grades own output).

### B — ADR-0035 (D35): command-agnostic three-tier write surfaces superseding D34

New ADR `log/0035-command-agnostic-three-tier-write-surfaces.md`. D35 records command-agnostic correction to D34 and generic-loop coverage gap.

ADR-0034 body stays frozen (immutability rule). adr-index.json: ADR-0034 entry gains `"superseded_by": "ADR-0035"`. New ADR-0035 entry added. `adr_counts.rendered` → 35.

### C — adp-orchestrator Run step 4 edit (`.claude/agents/adp-orchestrator.md`)

Replace R-CW-1/2/3 text in Run step 4: rewrite R-CW-1 command-agnostic (both launchers named in enumeration, neither as definition). R-CW-2 confirms this agent IS the orchestrator (shared by both). R-CW-3 unchanged substance.

### D — Self-host loop RULES edit (`prompts/_orchestrator.md`)

R-CW-1 bullet: command-neutral definition; scope note "/evolve" retained as scope context only, not as the controller definition. Update D34 → D35 citations in R-CW-1/2/3.

### E — Generic loop RULES add (`prompts/_orchestrator.generic.md`)

Add R-CW-1/2/3 citations to RULES section (currently absent). Command-neutral. Cites ADR-0035.

## Out of scope

- ADR-0034 body (frozen, immutable — immutability rule).
- Break-glass route R-id — deferred.
- Re-freeze `aprd.frozen.md` — R-ids appended via this CR doc (same pattern as prior CRs).

## Class

correction. Kills `/evolve` command-name leak in shared spine introduced by CR-013/ADR-0034. Adds R-CW coverage to generic loop body. No behavior change.

## Build sequence

| id | name | depends on |
|---|---|---|
| W28a-CR-014 | Author CR-014.md (this doc) | — |
| W28b-ADR-0035 | Author ADR-0035 body + update adr-index.json | W28a |
| W28c-ADR-LOCK | Re-sign adr.lock v15 (sha256 over 35 log bodies) | W28b |
| W28d-ADR-ORCH | Edit `.claude/agents/adp-orchestrator.md` Run step 4: R-CW-1/2/3 (D35) command-agnostic | W28b |
| W28e-ORCH-RULES | Edit `prompts/_orchestrator.md` RULES: R-CW command-neutral + D35 citations | W28b |
| W28f-GENERIC-RULES | Edit `prompts/_orchestrator.generic.md` RULES: add R-CW-1/2/3 citation | W28b |
| W28g-VERIFY | Selftests + economy-lint | W28c–W28f |

## Status

Open → Accepted (operator gate ACCEPT per 2026-06-12 gate decision).
