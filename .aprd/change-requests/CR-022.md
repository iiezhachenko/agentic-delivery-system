# CR-022 — orchestrator STEP 0: derive frontier from done_sentinel, not status flag

> Standalone. Operator-directed (2026-06-12). Loop-body correctness fix, NOT a SRP split (no CR-016 parent). Caveman register.

## Ask

`_orchestrator.md` STEP 0 (frontier derivation) read `08-rerank.json` `status` field to pick next unshipped item. Canon says otherwise: position = disk `done_sentinel` scan, tracker NOT source of truth (CLAUDE.md "Disk is source of truth"; roadmap.md L3 "position derived from disk (`done_sentinel` scan), never read from tracker"; R-LH-2/D20 "ledger is informational ordering, not the source of truth").

Drift surfaced 2026-06-12: W29i/W29j committed + sentinels present on disk (`13c7967`/`6535710`), but `status:"unshipped"` never flipped at promotion. Orchestrator trusted status → flagged both as "unshipped ahead of W30g". Stale flag mis-routes: next loop could re-target already-shipped work (best case no-op, worst case redundant re-author + churn).

## Findings

| id | criterion | severity |
|---|---|---|
| F1 | STEP 0 reads `status` flag, not `done_sentinel` on disk — violates disk-is-truth canon | block |
| F2 | no reconcile step: stale `status` (sentinel met, flag unshipped) never self-corrects | warn |

## Scope delta

### A — Harden STEP 0 frontier derivation

`prompts/_orchestrator.md` STEP 0 — frontier = FIRST `remaining_sequence` entry whose `done_sentinel` is ABSENT or schema-invalid ON DISK. Treat `status` as advisory display only, never the gate. Evaluate each entry's `done_sentinel` (path-exists / grep-pattern / schema-valid per entry shape) against the tree before selecting.

### B — Self-reconcile stale flags

When STEP 0 finds an entry with `status:"unshipped"` but `done_sentinel` satisfied on disk → flip to `shipped` (the W29i/W29j case), log the reconciliation, continue derivation. Inverse (status shipped, sentinel absent) → surface as a warning, do NOT silently rebuild.

## Affected downstream

- All `/evolve` + `/deliver` loops: frontier selection becomes disk-grounded; stale tracker can no longer mis-route.
- No prompt deliverable schema change; `08-rerank.json` schema unchanged (status field retained as advisory).

## Class

engine-fix (loop body). Modifies `_orchestrator.md` STEP 0 only.

## Status

Open. W29i/W29j ledger already reconciled by hand 2026-06-12 (this CR fixes the mechanism so it self-corrects). Verify: clean-room run where one entry has a satisfied sentinel + stale `unshipped` status → STEP 0 must NOT re-target it.
