# CR-001 — bring brownfield class bindings into deliverable scope

> Change request against self-host aPRD v3 (`aprd.frozen.md`). Bumps to v4. Re-triggers Phase 1 (roadmap re-rank). Caveman register.

## Ask
Frozen v3 scope = greenfield delivery pipeline only (specs 00–04). But deliverable spec (`specs/00` §4) defines a class taxonomy + per-class playbooks; CLASSIFIER already recognizes all 8 classes. Two brownfield class bindings now partly built OUTSIDE the tracked frontier:
- **feature-add** — playbook + per-role overlays + `_fixtures/brownfield-feature/` oracle (built, committed, untracked in roadmap).
- **bugfix** — playbook + class-dispatch wiring + intake overlays (BASELINE-MAP/GAP-DETECT/SYNTHESIZE) built (commits `8e0f8ea`, `60e8b99`); build-phase overlays + fixture oracle NOT built.

Untracked work is invisible to the self-host loop (state derives from `08-rerank.json` + disk sentinels, D20) → undrainable, divergent from disk. CR brings the **bugfix class binding** into scope as tracked prompt-builds so the loop drains it phase 0 → end.

## Scope delta (what v4 adds)
- Deliverable scope extends from "greenfield 00–04 only" to "greenfield 00–04 + brownfield class bindings (feature-add, bugfix) per `specs/00` §4 'new class → author playbook → register'".
- New tracked builds = the **bugfix spine**: build-phase overlays (DIAGNOSE defect-localize, DERIVE-TESTS, MATERIALIZE-ORACLE, IMPLEMENT, VERIFY-OUTPUT) + the `_fixtures/brownfield-bugfix/` both-directions oracle + e2e.
- Reuses existing spine + overlay + playbook pattern (P3). No new pipeline architecture → Phase 2/3 thin.

## Out of scope (this CR)
- feature-add spine (already built; retro-tracking deferred — separate CR if wanted).
- Other classes (refactor/migration/perf/integration/investigation) — still unplaybooked, HALT at CLASSIFIER.
- Sibling Spines D + A (still deferred per v3 scope note).

## Class
feature-add (adds capability to existing pipeline deliverable; reuse-conventions, BF2/BF5). Classified clean-room: `.aprd/01-classification.json` → SR1 feature-add 0.93.
