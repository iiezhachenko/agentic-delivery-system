# CR-015 — Audit class binding: audit-spine playbook + new roles (LENS-DEFINE / AUDIT-RUN / AUDIT-REPORT) + CLASSIFIER/BASELINE-MAP overlays

> Operator-directed (2026-06-12), gate ACCEPT received before dispatch. New delivery class. Class: additive. Caveman register.

## Ask

Introduce `audit` as a third brownfield class (alongside `bugfix` and `feature-add`). Operator defines named lenses (check sets with pass/fail criteria); system audits existing code against lenses; produces human-readable report + machine-readable JSON; optional promote path emits `.aprd/00-raw-request.md` shaped as ADP intake. Analogous to `feature-add` + `bugfix` spine but read-only (no code modification).

## Gate answer (D20 guarantee 6 — persisted, never re-asked)

ACCEPT — operator gate 2026-06-12. Dispatch CR-015 → ADR-0036 (D36). Promote `_test_bench/prompts/_playbooks/audit-spine.md` → `prompts/_playbooks/audit-spine.md`. Author three new roles (LENS-DEFINE / AUDIT-RUN / AUDIT-REPORT) + CLASSIFIER/BASELINE-MAP audit overlays in subsequent builds.

## Scope delta

### A — audit-spine playbook (`prompts/_playbooks/audit-spine.md`)

New config-engine file. One file = whole class binding. Not a role (no inputs/outputs/escapes frontmatter). Sibling to `bugfix.md` and `feature-add.md`. Selected when CLASSIFIER emits `class=audit`. Declares:

- `class: audit` — read-only assessment; no skeleton/foundation/scaffold stages (`active_stages` all off).
- `has_adp_artifacts: true` guard — ADP foundation must exist before playbook activates.
- `lenses: operator-defined` — LENS-DEFINE collects interactively; schema `{id: L*, name, description, criteria[{id: LC*, test, severity: info|warn|block}]}`; stored at `.audit/lenses.json`.
- `audit_scope: operator-defined` — glob patterns or paths; default = repo root; stored at `.audit/scope.json`.
- `report_format: {human: markdown-report, machine: audit-report.json}` — both always emitted.
- `promote_path` — AUDIT-REPORT writes `.aprd/00-raw-request.md` on request; block findings → `bugfix`-class requirements; warn → `feature-add`-class requirements.
- `new_roles: [LENS-DEFINE, AUDIT-RUN, AUDIT-REPORT]` — three net-new roles not yet authored.
- `prompt_overlays: {CLASSIFIER, BASELINE-MAP}` — existing roles that need audit delta blocks.
- `verify_method: lens-coverage-check + report-completeness + promote-artifact-well-formed`.

### B — ADR-0036 (D36): audit class binding decision

New ADR `log/0036-audit-class-binding.md`. D36 records the decision to introduce `audit` as a third brownfield class, its read-only contract, and the deferred new-role + overlay authoring.

adr-index.json: new ADR-0036 entry added. `adr_counts.rendered` → 36. adr.lock → v16.

### C — Roadmap update

`roadmap.md` + `08-rerank.json` updated: audit-spine wave entry (W-AUDIT-PLAYBOOK) added to ledger + roadmap.md note for downstream builds (LENS-DEFINE / AUDIT-RUN / AUDIT-REPORT + CLASSIFIER/BASELINE-MAP overlays).

## Out of scope (deferred — licensed by this CR)

- LENS-DEFINE role prompt (`prompts/00-aprd/LENS-DEFINE.md`) — net-new role, subsequent build.
- AUDIT-RUN role prompt (`prompts/00-aprd/AUDIT-RUN.md`) — net-new role, subsequent build.
- AUDIT-REPORT role prompt (`prompts/00-aprd/AUDIT-REPORT.md`) — net-new role, subsequent build.
- CLASSIFIER audit delta block — overlay, subsequent build.
- BASELINE-MAP audit delta block — overlay, subsequent build.
- components.json update (new roles) — after role prompts authored.
- `.audit/` directory structure + schemas — realized during LENS-DEFINE / AUDIT-RUN / AUDIT-REPORT builds.

## Class

additive. Introduces third brownfield class (`audit`). No modification to existing roles or existing playbooks. No code change.

## Build sequence

| id | name | depends on |
|---|---|---|
| W29a-AUDIT-PLAYBOOK | Promote `prompts/_playbooks/audit-spine.md` | — |
| W29b-ADR-0036 | Author ADR-0036 body + update adr-index.json | W29a |
| W29c-ADR-LOCK | Re-sign adr.lock v16 | W29b |
| W29d-ROADMAP | Update roadmap.md + 08-rerank.json | W29b |
| W29e-COMMIT | Git commit on feature/audit-spine | W29c, W29d |

## Status

Open → Accepted (operator gate ACCEPT per 2026-06-12 gate decision).
