# CR-009 — rename launcher command `/self-host` → `/evolve` (clean, no alias)

> Operator-directed (2026-06-12), gate Q1–Q2 answered before dispatch. Change against ADP harness wiring (launcher skill + orchestrator agent + doc command-refs). Class: refactor (surface-rename, no behavior change). Caveman register.

## Ask

Rename the Claude Code slash command `/self-host` → `/evolve`. Clean rename — `/self-host` stops existing, no backward-compat alias.

## Concept-vs-command boundary (load-bearing — defines blast radius)

"self-host" lives TWO ways in this repo. This CR touches ONLY the first:
- **Command surface** `/self-host` — the launcher slash command (`.claude/skills/self-host/`). RENAMED → `/evolve`.
- **Concept noun** "self-host" / "self-hosting" / "Self-Host Orchestrator" / "self-host loop body" — the idea of running the pipeline on its own project. UNCHANGED everywhere (titles, prose, filenames, frozen artifacts).

Rule for the edit pass: replace the literal slash-command token `/self-host` → `/evolve` in non-frozen files. Leave bare `self-host` (no slash) = concept, untouched.

## Gate answers (D20 guarantee 6 — persisted, never re-asked)

- **Q1 Branch:** `feature/evolve-rename`.
- **Q2 Right-size:** LEAN — ADR-0030 (naming decision) + controller-direct wiring edit + regression-guard. NO new R-id, NO new aPRD version, NO HLD delta. Command name is NOT a frozen requirement (no spec/aPRD requirement names the launcher/command surface) and behavior is unchanged → P3 direct-edit precedent (CR-006, W7/W8/W9/W15), not a feature-add build cascade.
- **Alias:** none (clean rename).

## Scope delta (what this CR changes)

### A — Decision (ADR-0030)

Naming decision: rename launcher command `/self-host` → `/evolve`, clean (no alias). Rationale: `/evolve` names the act (system improving itself) better than the implementation detail (`self-host`); the self-host *concept* noun is orthogonal and stays. No-alias = single invocation surface, no drift. Additive to D1–D29 (no superseding; this is a surface decision, not a model change).

### B — Harness-wiring edit (controller-direct, P3 — no stochastic prompt, RM11 carve-out)

- **Skill rename:** `git mv .claude/skills/self-host` → `.claude/skills/evolve`; frontmatter `name: self-host` → `name: evolve` (description + body concept-prose unchanged).
- **Command-token swap** (literal `/self-host` → `/evolve`) in NON-frozen files only:
  - `.claude/agents/adp-orchestrator.md` (launcher mentions in description + GIVEN/body).
  - `prompts/_orchestrator.md` (the launcher-selection note; title + "self-host control loop" concept-prose unchanged).
  - `docs/self-host-usage-guide.md` + any other non-frozen doc carrying the command token.

### Untouched (immutability + boundary)

- Every frozen artifact: `.aprd/*.frozen.md`, `.adr/log/<NNNN>.md` bodies, all `*.lock` (adr.lock re-signed additively for the NEW ADR-0030 only — existing bodies unchanged).
- The "self-host" concept noun everywhere (incl. `Self-Host Orchestrator` title, `self-host-*.md` filenames, frozen-doc concept references).
- `_fixtures/` oracles — none reference the command token (verified); no oracle change.

## Out of scope

- Kiro launcher `--agent selfhost` (a DIFFERENT harness's launcher, not a `/` slash command). Left as-is; optional follow-up CR if symmetry wanted.
- Renaming concept files/docs (`self-host-usage-guide.md`, `self-host-workflow.md`) — concept, not command.
- Generic launcher `/deliver` — unaffected.

## Class

refactor (surface-rename). No behavior change → regression-guard mandatory + sufficient: all `tools/**/*.selftest.mjs` + economy-lint corpus stay green; `/evolve` resolves, `/self-host` gone. No new oracle.

## Build sequence

| id | name | depends on |
|---|---|---|
| W24-ADR-0030 | Author ADR-0030 (naming decision) + re-sign adr.lock v10 + index | — |
| W24-EVOLVE-RENAME | Skill rename + command-token swap (controller-direct, P3) | W24-ADR-0030 |

(One logical build, ADR + edit; no separate role prompt to author — wiring edit, P3.)

## Status

Open. Branch `feature/evolve-rename` created (2026-06-12). Frontier = W24-ADR-0030.
