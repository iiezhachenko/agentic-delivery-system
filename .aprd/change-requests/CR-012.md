# CR-012 — R-CW-1 correction + ADR-0032 supersession: three-requirement split for controller zero-write

> Operator-directed (2026-06-12), gate Q1–Q3 answered before dispatch. Correction of CR-011 (clarifies R-CW-1 meaning; splits monolithic rule into R-CW-1/2/3). Class: correction. Caveman register.

## Ask

CR-011 introduced R-CW-1 as "controller zero-write for prose" — a monolithic statement combining (a) the overarching zero-write constraint, (b) the orchestrator's loop-control-only role, and (c) the step-runner's authoring+verifying responsibility. Monolithic rule mixes permitted-ops list with prose prohibition, making R-CW-1 read as a permitted-list rule rather than a guarantee. Operator directed: split into three precise requirements (R-CW-1/2/3); supersede ADR-0032 with ADR-0033; preserve R-CW-1 id so existing citations remain valid.

## Gate answers (D20 guarantee 6 — persisted, never re-asked)

- **Q1 R-ids:** REWRITE R-CW-1 (corrected meaning = controller zero-write guarantee, same id preserved for citation validity) + ADD R-CW-2 (orchestrator loop-control-only) + R-CW-3 (step-runner authors+verifies). R-CW-1 id preserved; meaning corrected.
- **Q2 ADR:** SUPERSEDE — D32 marked superseded_by ADR-0033; ADR-0032 body frozen/untouched; adr-index.json gains additive superseded_by field only; status stays Accepted.
- **Q3 routing CONFIRMED:** step-runner authors CR-012.md, ADR-0033 body, index/lock JSON edits, rule prose edits in `prompts/_orchestrator.md` + `.claude/agents/adp-orchestrator.md`. Orchestrator shells out `node tools/<tool>.mjs` ONLY for sha256 re-sign + lint/selftest verify. Orchestrator authors nothing itself.

## Scope delta

### A — Corrected R-ids (operating-model requirements, appended via this CR doc)

Prior R-CW-1 definition (CR-011, now corrected): "Controller zero-write for prose: controller never hand-authors rule prose, ADR bodies, CR docs. Permitted writes: git ops, spine shell-outs, lock re-sign, scratch→promotion." — monolithic, mixed concern.

**Corrected and new R-ids:**

- `R-CW-1` (rewritten, id preserved) — **Controller zero-write guarantee.** Controller (orchestrator) never authors or writes deliverable content. Permitted controller-direct writes = mechanical deterministic ops ONLY: git ops · spine tool shell-outs (`node tools/<tool>.mjs`) · lock re-sign + index updates per spine tooling · scratch→destination promotion. Everything else routes through step-runner clean-room. Binds `prompts/_orchestrator.md` RULES + `.claude/agents/adp-orchestrator.md` Run step 4. Overarching constraint; R-CW-2/R-CW-3 operationalize it.
- `R-CW-2` (new) — **Orchestrator loop-control-only.** Orchestrator role = pick / dispatch / verify / gate / promote. Never authors content at any layer. No exception for meta artifacts (rule text, ADR rationale, CR prose) — stochastic authoring → step-runner.
- `R-CW-3` (new) — **Step-runner authors and verifies.** All prose artifacts (rule text, ADR bodies, CR docs, prompt `.md` files) authored by step-runner clean-room subagent. Verifier = separate step-runner spawn (runner never grades own output). Controller dispatches; step-runner authors; separate verifier confirms.

### B — ADR-0033 (D33): corrected decision superseding D32

New ADR `log/0033-controller-zero-write-clean-room-separation.md`. D33 records three-requirement model (R-CW-1/2/3), why the D32 monolithic statement was imprecise, and the tradeoffs (keep monolithic — rejected; replace id — rejected; amend D32 body — rejected).

D32 body stays frozen (immutability rule). adr-index.json: ADR-0032 entry gains `"superseded_by": "ADR-0033"`. New ADR-0033 entry added. `adr_counts.rendered` → 33. `adr_count` → 33.

### C — Orchestrator RULES edit (`prompts/_orchestrator.md`)

Replace single R-CW-1 bullet (D32 citation) with three bullets:
- R-CW-1 (D33): zero-write guarantee (overarching, permitted-list)
- R-CW-2 (D33): orchestrator loop-control-only
- R-CW-3 (D33): step-runner authors + verifies

### D — adp-orchestrator Run step 4 edit (`.claude/agents/adp-orchestrator.md`)

Replace inline R-CW-1/D32 citation with R-CW-1/2/3 (D33) split, same line structure.

## Out of scope

- ADR-0032 body (frozen, immutable — immutability rule; correction = supersede, not edit).
- Generic orchestrator (`prompts/_orchestrator.generic.md`) — analogous rule implicit; explicit parallel deferred (separate CR if needed).
- Existing P3 precedents for mechanical wiring edits (unchanged — CR-006, W7/W8/W18 still valid; R-CW-1 merely clarifies the overarching guarantee).
- Re-freeze `aprd.frozen.md` (monolithic doc; R-ids appended via this CR doc same as CR-007/CR-010 pattern).

## Class

correction. Clarifies CR-011 R-CW-1 + ADR-0032 without changing behavior; splits monolithic rule into three precise requirements. Regression-guard = existing selftests + economy-lint corpus unaffected (no spine logic change, only rule prose + agent instruction prose).

## Build sequence

| id | name | depends on |
|---|---|---|
| W26a-CR-012 | Author CR-012.md (this doc) | — |
| W26b-ADR-0033 | Author ADR-0033 body + update adr-index.json (ADR-0033 entry + ADR-0032 superseded_by) | W26a |
| W26c-ADR-LOCK | Re-sign adr.lock v13 (sha256 over 33 log bodies) — controller-direct mechanical op | W26b |
| W26d-ORCH-RULES | Edit `prompts/_orchestrator.md` RULES: R-CW-1 rewording + R-CW-2/R-CW-3 added | W26b |
| W26e-ADR-ORCH | Edit `.claude/agents/adp-orchestrator.md` Run step 4: R-CW-1/2/3 (D33) | W26b |
| W26f-VERIFY | Selftests + economy-lint on both targets | W26c, W26d, W26e |

## Status

Open → Accepted (operator gate ACCEPT per 2026-06-12 gate decision).
