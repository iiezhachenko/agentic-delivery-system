# CR-019 — SEQUENCE split: greenfield only; extract SEQUENCE-FEATURE-ADD

> Parent: CR-016. Operator-directed (2026-06-12). Deferred — execute in subsequent build workstream after ADR-0037 acceptance. Caveman register.

## Ask

SEQUENCE has 2 separable output paths serving different downstream stages: `.roadmap/05-sequence.json` (greenfield — consumed by SKELETON-IDENTIFY/FOUNDATION-CUT/SEQUENCE-REVIEW) vs `.roadmap/08-rerank.json` (feature-add — consumed by slice build loop + RE-RANK). Greenfield path writes 05-sequence.json only; feature-add path writes 08-rerank.json only. Two branches behave as different roles with different primary outputs. Block F4 (LC1), warn F8 (LC4).

Split: SEQUENCE = greenfield only (05-sequence.json); new SEQUENCE-FEATURE-ADD = feature-add path (08-rerank.json).

## Findings

| id | criterion | severity |
|---|---|---|
| F4 | LC1 — 2+ unrelated output paths | block |
| F8 | LC4 — mode-switching with different primary outputs | warn |

## Scope delta

### A — Modify SEQUENCE

`prompts/01-roadmap/SEQUENCE.md` — remove feature-add branch. Greenfield only: reads HLD skeleton + components + contracts, produces `.roadmap/05-sequence.json`. Dispatch signal: `class=greenfield`.

### B — New role: SEQUENCE-FEATURE-ADD

`prompts/01-roadmap/SEQUENCE-FEATURE-ADD.md` — new role. Inputs: `.roadmap/05-sequence.json` (existing greenfield sequence) + `.aprd/aprd.frozen.md` + feature-add delta. Produces `.roadmap/08-rerank.json` with new feature waves appended. Dispatch signal: `class=feature-add`. Handles rerank insertion logic currently in SEQUENCE feature-add branch.

### C — Playbook updates

`prompts/_playbooks/feature-add.md` — update roadmap-sequencing reference from SEQUENCE → SEQUENCE-FEATURE-ADD.
`prompts/_playbooks/greenfield.md` (if exists) — SEQUENCE reference unchanged.

### D — Spine updates

`tools/io/io-manifest.json` — add SEQUENCE-FEATURE-ADD role entry (inputs: 05-sequence.json + aprd.frozen.md; outputs: 08-rerank.json). `tools/io/components.json` — add SEQUENCE-FEATURE-ADD component. Update SEQUENCE entry to remove feature-add output (08-rerank.json).

## Affected downstream

- Feature-add class dispatch: SEQUENCE-FEATURE-ADD replaces SEQUENCE for feature-add class.
- Greenfield class: SEQUENCE unchanged (05-sequence.json path unaffected).
- RE-RANK + slice build loop: still consume 08-rerank.json; source role changes, schema unchanged.

## Class

refactor + extract. Modifies SEQUENCE (remove feature-add branch) + adds SEQUENCE-FEATURE-ADD.

## Status

Open (deferred). Execute after W30c-ADR0037-SIGN (ADR-0037 accepted + adr.lock v17).
